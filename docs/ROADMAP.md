# Tapuziel — Roadmap & Idea Pool

**Current version: v2.45-alpha**

## How versions work

- Every working session that ships = **+0.01** (v0.32-alpha, v0.33-alpha, ...)
- Each bump gets a row in the Version Log: what shipped, whose idea
- Still **alpha**, and the number counts sessions: never +1.0, never a skip, never a bare "chore: bump" with nothing shipped. `npm run test:version` (in `test:smoke` and `npm run qa`) fails when package.json, package-lock, this file's current line, the README line and the top Version Log row disagree, or when the newest step is not +0.01 (a hotfix `.1` of the same session is the one exception)
- Ideas from three brains: **Ben**, **Claude**, **Grok** — all land in this file, best ones win
- When builder + media + pages + theme builder feel complete → v1.0-beta
- Long game: a real open-source CMS competitor, community-driven once Ben sets the direction. Security hardening gets its own phase before beta.

## North Star (Ben, 2026-07-10)

**Become the Red Hat of CMSs.** Fully open source, community-driven, no DRM, no vendor lock-in — the business is the ecosystem and the mastery, not gatekeeping. Competition is absurd, so we plan big and hard; the edge is knowing what people actually want in their sites and how they want them made (Ben = CMS master), plus being **more AI-friendly than any other CMS**.

Pillars:
1. **Ideas flow from mind to website.** The wizard + page builder turn intent into a live site with minimal friction. We advise building in *our language* (page-builder blocks), but never trap anyone — overrides (classes, custom CSS) always possible in the editor, like every serious CMS.
2. **Import as a first-class citizen.** Elementor / WordPress export imports feed the wizard; an import can *skip* wizard steps it already answers (e.g. homepage creation — our generic homepage scheme with page-builder cuts/sections is only for from-scratch sites). Test-drive on an existing server or a fresh one.
3. **Theme & plugin ecosystem.** Theme *maker* (colors, gradients, palette-from-image) + theme *import type* so people build and share themes. Selling is allowed but not our advice; we take zero liability for piracy of sold themes and will never add DRM — it's open source, it's all gitted, +0.01 versioning is our method. Plugin install support.
4. **`.pzn` file format** — one portable container for imports/exports (sites, themes, plugins), same pipeline as the WordPress/Elementor import. This is a NEW packing/file system of ours and the first step of making a mark — same as Red Hat, whose mark was **RPM**, their own package format. `.pzn` is our RPM.
5. **Agents are users too.** Mini-API so agents recognize page-builder parts and know what they're editing (see docs/cli-agent-support.md). The human face of that same API is an in-admin **chatbot that looks and feels like ChatGPT** — see "Module language & AI copilot" below.
6. **Your AI subscription, not our tokens (the ace).** The *brain* of the copilot is the user's own subscription — no middleman charging tokens and reselling API. Already made with Grokin. How it works without API keys (the Grokin trick):
   - The LLM runs where the user's subscription lives — the **web browser environment**, where LLMs are confident to work — or we use conversation tricks: the bot acts from a chat by emitting instructions.
   - We set up **a chat with our syntax, `.pzn`-ready**: the user gets a primer (instructions to paste) that teaches *their* bot (ChatGPT / Claude / Grok…) Tapuziel syntax and `.pzn` syntax.
   - A **translator inside Tapuziel** parses what the bot says in our syntax and executes it through the agent mini-API. The conversation *is* the protocol.
   - API keys stay an option (already proven with ChatGPT + keys), but the goal is universal across providers. `C:\Dev\Grokin-V2` (WordPress bot + Chrome extension) is the sophisticated prior art to port in.

## The Plan (Ben's vision, phased)

### v0.31 — Media & Gallery (this version)
- Media library wired to the database (was: raw folder listing)
- Folder system, Windows-Explorer look: folders + files grid, breadcrumb, create folder, right-click → delete / open / copy URL
- Gallery module exposed in toolbox: picks multiple images from the library
- Toolbox buttons for list + video modules (were replace-only)

### v0.32 — Pages, Draft/Publish, Backups
- Page navigator inside the builder: see all pages, search, jump to edit
- Save ≠ Publish: draft blocks live separately from published blocks; "שמור" keeps a draft, "פרסם" copies draft → published; build renders published only
- Automatic backup revisions on every save (keep last N, restore from list)

### v0.33 — Menu entity + Theme Builder v1
- "menu" becomes a real entity (DB table): items, order, nesting
- Menu module lives in the header — editable in the THEME builder, not the page builder
- Theme builder: colors, logo, menu placement variations (top / side), fonts

### v0.34 — Site-map & Structure
- Visual site-map: page tree, drag to re-parent, status badges
- More separator/section modules (banner, CTA strip, section with background)

### v0.35 — Responsive everything
- Admin + output audited on all resolutions, window-resize safe
- Mobile/tablet preview toggle in builder

### v0.36 — Article system ✅ (shipped) + Wizard v2 (next)
- **article-list module** ✅: shows articles as "cubes" (cards) — image + title + teaser
  - Card image: set manually (page properties), OR auto-extracted from the article's first image block
  - Teaser: manual or auto from first text block; clicking a cube opens the article page
  - A page becomes an article via the new **page properties** panel (deselect → side panel): article checkbox + teaser + card image
- **Wizard v2** ✅ (shipped v0.37) — a real guided setup, not 3 questions:
  1. Site name
  2. Coloring — walks the user *through the theme creator* (teaches the tool while using it)
  3. Creates default pages: home, contact, about, articles (articles page uses article-list module)
  4. Menu step: pick from the pages just created ("the menu's menu") or href to external sites — typed links already support this
- Theme creator: evolve toward drag & drop

### Module language & AI copilot (Ben's spec, 2026-07-10)

**Modules are the type system of the page.** Each module is its own class — primitive like `int`: text, heading, image, button, youtube (paste a URL — already the pattern since v0.30), gallery, list… Containers (sections, columns) get *filled* with modules. Goal: as many modules/tools as possible — the vocabulary of our language.

- **Side options panel**: selecting a module already placed on the page opens its options in a panel at the side of the screen (not inline clutter). Every module declares its own options — one panel, schema-driven, so a new module gets its UI for free.
- **The language must be sound for agents**: an agent should be able to place a text block, take an article it co-wrote with the user and *make it real* on the page, or write into the page in real time. Structured blocks (docs/block-schemas.md) are already this — the mini-API exposes read/insert/update/move per module.
- **AI text operations**: correct, expand, shrink, rewrite. Could be offered as select-text → AI menu (keep as secondary option), **but the chosen direction is a chatbot**:
- **The copilot chatbot**: an in-admin chat that *resembles the LLM UIs people already trust*. The user who is "so confident with their ChatGPT" has a blast — the bot they "know so much about" is operational here: it understands the builder and takes real actions — halving the page into columns, making a hero, coloring text inside a text block, filling a section with the article you wrote together. Chat is the human skin over the same agent mini-API; every chatbot ability is an API ability first.

### The long game (Ben)
1. **CMS** — finish the builder, articles, themes (we are here)
2. **CRM properties** — contacts, forms that feed them, leads from the published site (Ben's mastery: CMS/CRM)
3. **Security phase** — see below; "bundle that no one can deny"
4. **Open source, community-driven** — led by whiteno1se
5. **Ecosystem** — `.pzn` import/export format, theme sharing, plugin installs, agent mini-API
6. **BYO AI subscription** — Grokin-V2 functions ported in, agents talk directly to the system (the Red Hat endgame)

### Later (pre-beta) — Security phase (Ben: features first, security before beta)
Deliberately deferred. When we get here, in order:
1. **Admin auth**: password (bcrypt/argon2) + session cookie; wizard sets it on first run
2. **Database encryption at rest**: encryption key (env var / key file outside repo) using SQLite cipher (better-sqlite3-multiple-ciphers) — "not let anyone near the database"
3. **Request hardening**: CSRF tokens on all admin POSTs, rate limiting, JSON body size limits (exists: 12mb — tighten)
4. **Upload validation**: magic-byte checks (not just mime), SVG sanitization, size caps
5. **Output**: CSP headers on admin + published site, X-Frame-Options
6. **Secrets hygiene**: gitleaks in CI; key never committed

### Community (after security)
- CONTRIBUTING.md, plugin/module API for third-party modules

## Version Log

| Version | Shipped | Source |
|---|---|---|
| v2.45-alpha | **A model that is almost right gets the rest of the way — the printed document is adopted, the typo'd closer is read, the door names the fix.** Ben: *"we will recommend gemma4 for our product … measure the other gemma4 that are available from google official"* — and, when the first numbers were in: *"push 2.45 so the robots wouldn't waste time"*. The family run (docs PR #18: E2B · E4B · 12B · 12B-QAT · 26B-A4B against the 31B's 39/39) said where the copilot starts working — at 12B — and showed that a 12B or a 4B-active MoE loses whole scenarios to three small habits, none of them about understanding the owner. **(1) The document is PRINTED instead of called.** After the door bounces a first proposal, gemma-4-12B and the 26B-A4B send the corrected page as a fenced document in the chat; the 12B does the same with a regrouped menu after `read_menus`; qwen3.6 does it for every page. For a page the chat has a create-from-the-reply button (create only — useless for an edit); for a menu there is nothing to press. `ai.adoptPrintedDocument` adopts it as the call it was meant to be and it walks the SAME road — preflight, the door's verdict back to the model under the adopted call's id, the approval card with its canvas; nothing is written without ✓. Guards, each pinned: an `edit_page` only for a page the model **read this turn** (an edit replaces the whole page — qwen3.6 tried to "edit" by regenerating a page it never read, and was refused); an `organize_menu` only when `read_menus` ran this turn, so a tool-less model stays the v2.43 *weaker mode* (the tester's gate 3: describe-only); never a reply that hit `max_tokens`; never a tool the request did not declare (the lean 8K request); never when the owner asked to SEE the code. The reply keeps the model's words and loses the document, so the page never shows two offers for one page, and the owner is told once. **(2) A closing tag that is ALMOST the open one.** `</bent/heading>`, `</int-hero>` (26B-A4B), `</bent_qa>` (12B) — and the model **repeats** the typo when the page is sent back: the same error twice, ~30 s of GPU each, then it gives up. `fixCloserTypos` (`src/pzn/repair.js`) reads it as the element that is open — only when the name is not a real tag, matches nothing on the open stack and resembles the innermost open `bent-*` element; plain HTML closers are never touched, a clean document passes byte-identical. It runs in `repair()` for every forgiving door (`CLOSER_TYPO`) and at the copilot's strict door **before** the proposal is shown — the card holds what the write will save. **(3) The door said what was wrong, never what was right.** `<bent-pricing> cannot contain <bent-priceitem>` twice in a row, because the answer is one line in a 45K-char dictionary. `E_CHILD` now ends `— it accepts: bent-plan, …`, `E_NOT_CONTAINER` ends `— it is a leaf: its content goes in attributes (…)`. **And one honesty guard:** a refused proposal followed by plain words — seen live: *"עדכנתי את כותרת ההירו"* with nothing proposed and nothing saved — keeps the model's sentence and puts the truth beside it: «שום דבר לא נשמר ושום דבר לא השתנה, גם אם התשובה אומרת אחרת». **The battery grew the eye that found (2) and (3):** with `--courier=relay` it sees every body the CMS composes, so the transcript keeps what the door told the model about each refused proposal. **Measured (RTX 5090, `--parallel 1`, copilot battery at 32K, 2 runs of 13 each — and a ×2 run moves by a scenario or two between runs, so these are ranges, not trophies):** gemma-4-12B QAT 24/26 before → **26/26** and **25/26** after; gemma-4-12B Q4_K_M 23/26 → **25/26**, at 8K 10/13 → **12/13**; qwen3.6-35b-a3b 23/26 → **24/26** (adopted four times; its misses are the model's own: an invented page list, a page regenerated without reading it — which the read rule refused to adopt); gemma-4-26B-A4B 25/26 → 25/26 and 23/26 — its misses MOVED rather than shrank: no more documents printed and lost, what is left is the model editing a page it never read (T6: the paragraph beside the heading vanished) and one run that kept ten items and set the fold instead of grouping, which the battery wrongly counted as a miss (the check now trusts the door's fit line); gemma-4-E4B 12/26 → 12/26 (it *describes* the tool it is about to call — no door can help that); regressions: gemma-4-31B **13/13** twice, qwen3.8-27b **13/13**. The real tool-call path has no read-before-edit gate yet — only the adoption has one; that is the next door (SESSION-HANDOFF §10). Also learned the hard way and written into LOCAL-LLM §5: with LM Link on, `lms unload --all` unloads the OTHER machine's models too (both directions) and an unknown model name can be routed to the other device — the runners unload by identifier, retry once after an engine crash, and stop when someone else's model is on the GPU. No extension change (Bridge stays 0.5.5). `test:smoke` 168/169 on Windows (`smoke-update-bridge` is the known win32-only miss), `test:pzn` 158/158. Still open: the battery through the real extension on a non-production site; the survey of Google's QAT 26B/31B, Dicta's Hebrew models and gpt-oss-20b (downloaded, queued). | Ben + Claude |
| v2.44-alpha | **The copilot, asked the way an owner asks — by a real model, judged by what landed. And the five things that only showed up that way.** Ben: *"lets work on tapuziel here on the 5090, lets do the runs ourselves not handle with grok today, we have tons of tokens today"*. The smokes pin the tool loop with canned tool calls and the injection eval judges one-shot packs; nothing asked the question an owner has — *I typed a sentence in Hebrew, did the right thing happen?* **The instrument: `scripts/battery-copilot.js`** (`npm run battery:copilot`, opt-in like the eval — it needs a model). A scratch CMS seeded with the ten-page live menu, a spawned server, a login, `POST /admin/api/ai/chat` with the page's own envelopes, thirteen scenarios (a hello; "which pages do I have"; blank canvas → a price page → "now change the headline"; an FAQ added to a published page; a refusal; "the selected item"; a `<script>` the owner asks for; an instruction hidden in a page's text; a crowded menu regrouped; a refused menu; "move Contact to second place, touch nothing else"; a page that does not exist; a second page in one conversation) — and the verdict is read from the **pages and menus tables**, never from the model's words. Hard checks are site state, soft checks are wording. Two couriers: `local` (the server calls the runtime) and `relay` (the battery plays Bridge V2: every `modelCall` is POSTed to the runtime unchanged and returned as `step`). A printed document is landed through the page's own 🪄 route and counted as the weaker mode it is; a model that asks a question first is answered once, like an owner would; an 8K window's four-tool request is judged as the designed words-only mode; every run starts from a freshly seeded site. **What it found — none of it visible to a canned smoke:** **(1) Thirty seconds.** `server.js` drops a socket silent for 30 s (S4); the injection runner always lifted that for its own socket, the copilot's chat route never did. On a server-side courier any turn over 30 s ended as a network error while the turn **ran on without the owner** — its proposal parked where nobody could reach it, and the next message queued behind the orphan and died the same way (one slow page took the following eight scenarios with it). The 5090 hid it: Gemma writes a page in 27–35 s, so the same scenario passed and failed on the same day. Now `src/socket-timeout.js` (shared) and `ai.turnCeilingMs()`; `smoke-inject-route` holds a model silent past a 1.5 s cap on both routes, and fails without the lift. **(2) The window is a pool.** LM Studio 0.4 loads with Max Concurrent Predictions 4 over a unified KV cache: one 13K-token request answers, two at once answer, three at once and **all three die** — HTTP 400 whose `error` is the string `…{"code":500,"message":"Context size has been exceeded."}`; a lone request that reaches the end of the window is a clean `finish_reason: length`, so that 500 has exactly one meaning. `parseShared` → `WINDOW_SHARED` in Hebrew with the setting in the fix line, on the tool loop, the runner and the relay's reader; deliberately **no retry** (a retry beside a generating neighbour kills the neighbour) and nothing taught to the window cache. With `--parallel 1` the same three requests queue and answer (8 / 17 / 26 s) and the card keeps 2.3 GB more VRAM — it is in every load line now. **(3) At 8K the copilot could not read back the page it had just made.** The read allowance was what was left *after* the chat history, so a 2,198-char page was refused one turn after `create_page` (2/2, both couriers). A read now outranks old chat turns (`roomForRead`; `fitTurns` already drops the oldest turns when this turn's tool units grow) — 8K went from 12/13 to 13/13. **(4) A menu that loses pages nobody asked to remove.** Asked to *group* a ten-item row, nemotron-3-nano returned five links — every one legal, so nothing was hard — and ✓ took five published pages off the **live** header. `parseMenuReply` now reports `lost` (reachable before, from nowhere after), and the copilot's preflight sends such a proposal back **once**, naming the pages, unless the owner's own words asked for a removal (`REMOVE_ASK_RE`, judged like `LAYOUT_UNASKED`); a model that insists reaches the card, `PAGES_MISSING` and all — the owner is the judge. The injection runner already had this repair round; the copilot did not. **(5) The connected copilot was still reading the paste flow's contract** — "an automated bridge publishes… prefer silence after the fence (or only PZN_READY)". Nemotron answered an approved edit with the single word `PZN_READY`, twice; qwen3.8 answered *"hello, what can you do?"* with `list_pages`, `read_menus` and a `create_page` card for a page nobody asked for. The copilot briefing now carries its own contract (same size — the compact tier has no room to give — no `PZN_READY`, *"afterwards tell the owner what you did"*), and the full tier one more sentence: a question or a greeting is answered in words. qwen3.8 went from 12/13 to 26/26. **Measured on the final code (RTX 5090, `--parallel 1`, `docs/LOCAL-LLM.md` §5):** gemma-4-31b 32K — local ×3 **39/39** (1 soft, 755 s), relay **13/13**; gemma at **8,192** — local **13/13**, relay **13/13** (the four menu scenarios in the designed words-only mode); qwen3.8-27b ×2 **26/26**; qwen3.6-35b-a3b ×2 23/26 (it prints documents instead of calling the write tool — a page still lands through 🪄, a menu does not); nemotron-3-nano ×2 18/26 (still drops pages on its second try; not recommended). Organizer eval re-run on Gemma at 32K: 27/27 PASS, 27 strict, 0 second rounds, avg 6 s. No extension change (Bridge stays 0.5.5). `test:smoke` 168/169 on this Windows box (`smoke-update-bridge` is the known win32-only miss), `test:pzn` 158/158. Left open, in SESSION-HANDOFF §10: the visitor chat still rides the 30 s cap (a public route, left alone on purpose), and the battery over the real extension. | Ben + Claude |
| v2.43-alpha | **The copilot sorts the menu — and shows it on a canvas before you approve.** Ben: *"we must make sure that the menu sorter is also included in the ai helper that connects to the api (lm studio or public doesn't matter they will work the same), there are option to sort the menu using the co-pilot, i want the copilot to show canvas of the menu when needed, it requires particular kind of programming"*. The tester's note named the gap "advise vs act" — the copilot told the order and changed nothing — and set the acceptance gate: *"after confirm, /admin/menus (GET /admin/api/menus) reflects the new order WITHOUT the inject-card's separate apply path being needed for the happy path."* Until now "put the page in the menu" had no tool, so a model with one hammer proposed an `edit_page`. **(1) Two tools, one door.** `read_menus` (READ, runs free): the menus as the organizer's own `<bent-menus>` serialization, the ONE computed number (how many items fit a row — computed, never guessed), the page table that is the only source of a legal `page="…"`, and the organizer's grammar lines for what a live menu may never show. `organize_menu` (WRITE, `mutates:true`): its preflight **is** `parseMenuReply` — `NO_MENU`, `PAGE_NOT_MENU`, `TOO_MANY_UNKNOWN`, a page that does not exist (hard warnings) and an echo (`NO_CHANGE`) go back to the model as the call's answer *before* the owner is asked; only ✓ applies, through `applyMenuPlan`, which backs up first. Nothing was forked: `menuGrammar`, `capacitySentence`, `pageTable` and `currentMenuPreview` are exported from `src/menu-organizer.js` and the pack's text is byte-identical (145/145). Same loop for every courier — server-side local, a cloud key (both envelopes: `function.parameters` and `input_schema`), and the Bridge V2 relay with **no extension change** (the bridge assembles `tool_calls` generically; `ALLOWED_PATHS` untouched). **One honest difference, said in words everywhere:** a page write is a draft, a site has no draft menu — an approved menu is LIVE, so the card, the overlay, the drawer and the briefing all say "חל על האתר החי, עם גיבוי", and the line that follows the apply carries **↩ בטל** (the owner's click on the existing restore route; the copilot has no undo tool). **(2) The canvas.** `/admin/chat` gains a third canvas state beside blank and page: "🧭 תפריט האתר" in the dropdown (under a key no page can carry — `deriveSlug` strips the colon; `__menu__` would have collided), also opened by the robot's own `read_menus` into an empty canvas and by any menu proposal. It shows the organizer's tree and fit line over the **real header** framed at the site's width (`GET /admin/api/menus/state` → `/admin/menus/preview/:id`), so overflow and the "עוד" fold look as they will. A proposal rides the page proposal's overlay: what the DOOR judged on the server (moved / added / removed / renamed, the fit line, the warnings) drawn by the injection card's own `renderPreview` — one renderer, two screens, `textContent` only — with the candidate header under it; same sandbox (`allow-same-origin`, no scripts). 375px stacks with no horizontal overflow. **(3) The weaker mode does not break.** A model with no tool support prints a `<bent-menus>` document; the page's `<bent-` test used to take it for a PAGE and light "🪄 צור דף". It is now recognised, judged by the never-writes preview route and shown closable — never approvable here. **(4) A tool the window cannot answer is not declared.** Found by `smoke-ai-window` (c) while building this: at a probed 8,192, once the ratio recalibrates to the 2.0 clamp the budget is 11,520 chars, v2.42 sat ~100 under it, and the pair (+~530 of schema, +~200 of briefing) pushed the compact tier to `WINDOW_TOO_SMALL` — on the very Gemma-at-8K this window code exists for. `ai.pickCopilotTier` now drops the pair when the room left could not hold a menu read: an 8K window gets v2.42's bytes exactly (verified against the committed file, 8/8 locale × tier × media), the page says so once, and the shrink ladder gains an honest rung (full → compact → lean → stop). The compact tier only ROUTES a menu request (+201 chars; its pin moves 11,500 → 11,600, explained in the smoke) — the grammar rides in `read_menus`' answer, paid on the turns that touch a menu. Verified live in a local preview against a scripted loopback model (no model was loaded in LM Studio): blank canvas → read → proposal → canvas opens itself → approve → 11 flat links became 6 with two groups, one row → ↩ undo restored it byte for byte; reject; the weaker mode; the 8K lean request (4 tools, 4,730 tokens). Smokes: `smoke-copilot-tools` 39 → 80 (the organizer's canned refusals through the real preflight; checked by hand that it can fail — with `mutates` flipped to false it goes red at "NOTHING was written while the proposal waits"), `smoke-copilot-route` 139 with the gate pinned by number (GATE 1–4) on a real server over the relay, `smoke-ai-window` 108 → 125. Two Windows-only test fixes ride along (a CRLF regex, a path separator). Package **2.43.0-alpha**. **Verified with a real local model after the merge (HARD-BATTERY-v3 had left H8 at *preview, apply not clicked*):** a scratch CMS, `provider=local` straight at LM Studio, no bridge, and a main menu of **ten flat top-level items**. The copilot called `read_menus` and came back in 13 s with a pending `organize_menu` carrying the canvas preview — not an `edit_page` holding a nav. Approve: the LIVE menu became four top-level items (בית · הפירות שלנו with 5 children · המטע with 3 · צור קשר), **no label lost anywhere in the tree**, `applied.organized` with a backup id and the fit line, a memo that says live and undoable with no draft language, and the served header rebuilt in the new order. `undoLast()` restored the exact pre-copilot menu; a refusal wrote nothing and took no backup. Also checked, because the read hands over every menu: a document that OMITS a menu leaves that menu alone — only the menus it names are written, so an omission cannot empty the footer. | Ben ("the menu sorter is also included in the ai helper … show canvas of the menu") + Tapuz Tester (the "advise vs act" gate) + Claude |
| v2.42-alpha | **The open tab comes back after a Reload, and a local model is never sent a request without its briefing (Bridge V2 0.5.5, `NO_BRIEFING` on every path).** Ben, from HARD-BATTERY-v2 (2026-09-17, cyan live, Bridge 0.5.4 + Gemma 4 31B — H1–H8 8/8, C direct LM 7/8): *"After extension sync, reconnect is flaky — needs force `permissions.request` + `registerContentScripts`"* and *"C1 FAIL_INVENT when calling LM Studio with NO system briefing … product fix is refuse-or-always-brief on CMS/Bridge paths"*. **(1) The flaky reconnect, measured.** In a real Chrome 148 (the extension loaded over the CDP pipe — branded Chrome 137+ ignores `--load-extension`, which is why the battery needed Chrome for Testing): a Reload drops the dynamic registration (the 0.5.2 record puts it back) — and leaves the content script in every OPEN admin tab orphaned: `chrome.runtime.id` undefined, `runtime.connect` / `sendMessage` throw `Extension context invalidated`. 0.5.4's orphan still answered the page's ping with a hello, so the page believed the bridge was present, and then swallowed every request: the model-list probe hung with no result at all (the synchronous throw escaped the listener), chat said "extension unavailable". The popup said connected, the page said nothing — hence "force connect". **Now, without a click:** the worker, after reconciling the record on every boot, injects the bridge again into the open tabs of every connected site (live registrations + the ZIP-wired host) via `tabs.query({ url })` under the host grant — no `tabs` permission; the fresh copy lands in its own isolated world beside the orphan (measured: two hellos, one working relay). An orphaned copy RETIRES: it asks `alive()` before answering anything, removes its listener, says one `tz-bridge-bye` with its `instance` id and answers nothing; every hello now carries an `instance`, a fresh copy posts `tz-bridge-takeover` after its hello and the orphan hearing it retires at once. The page glue (`admin-bridge.js`) prefers the newest instance, re-posts the requests the orphan swallowed (never one that already reported progress — nothing runs twice), and fails waiting requests with «התוסף נטען מחדש — רעננו את הדף» after a 2.5 s grace when no copy answers, instead of hanging on the twenty-minute ceiling. The popup says in how many open tabs the bridge is active. Also measured, for the record: a sync that ADDS the site's host permission to the manifest is granted silently on Reload of an unpacked extension; in a headless Chrome `permissions.request` never settles (no prompt to click) — a harness fact, not a product one; the product never calls `popup.close()` or reloads itself. **(2) `NO_BRIEFING`, airtight.** C1's raw-API case stays FAIL_INVENT by nature — no model knows BenTML without the briefing, and a naked `chat/completions` call from outside the CMS is not a path the CMS can brief; the product guarantee is that every CMS/Bridge path briefs or refuses. One gate in `src/ai.js`, `assertBriefed(provider, body)`, reads the composed body (both shapes) for the dialect (`<bent-` / `bent-*`) and throws `NO_BRIEFING` before any GPU minute — on `generateDetailed` (the runner, pack in the user turn or the repair round's history), `relayRequest` (the Bridge body), `callProvider` and the `modelCall` hand-off under the tool loop (whose `plan()` keeps the stricter system-must-brief rule from v2.35), and the worker courier: `inject-jobs.createJob` refuses at the queue and the real `tapuz-worker` refuses a bare job before touching the model. Local + Bridge only; the one declared exception is the visitor's customer-service chat (`src/crm/cs.js`, `prose: true` — words for a visitor, never a document), pinned to appear nowhere else. **(3) The draft is announced when the page KNOWS it (P2).** H1–H4 ran ~241 s of «כותב» after the draft already existed: over the bridge the approval's continuation (`modelCall`) carries `applied`, and the model's closing turn re-reads the whole conversation. `/admin/chat` now shows «נוצרה טיוטה ✓ / בוצע ✓», opens the draft in the canvas and closes the "not saved yet" proposal bar the moment that envelope arrives (once per turn), and the status line and the 45-second clock say «הטיוטה כבר נשמרה ✓ · המודל מסכם: …» until the closing words land; the builder's drawer says the draft landed and still reloads only when the turn ends, so the model's closing words are kept. Measured in a real Chrome against the real server with a scripted bridge whose closing call takes 9 s: the draft line at 256 ms after ✓ אשר, the reply at 9.2 s, one line. Smokes: `smoke-turn-clock` +2, `smoke-extension-v2a` +23 (the revival run, the orphan run, the glue's instances/re-post/grace — 167), new `smoke-no-briefing` (35: every seam stripped and refused with no fetch, the real briefing and packs passing, the real worker `--once` refusing), new opt-in `smoke-bridge-reload` (11 checks in a real Chrome over the CDP pipe: 1.6 s after a Reload the untouched tab is on a new instance, a relay works, a request fired 50 ms after the Reload is answered once; the Reload checks fail on 0.5.4). The fake packs of `smoke-inject-route` / `smoke-inject-worker` now carry their dialect, as every real pack does. Package **2.42.0-alpha**. **Review (Claude), before merge:** (1) `smoke-bridge-reload` is opt-in (not in `test:smoke`, so CI never ran it) and did not run on Chrome for Testing 153 — it attached to a component extension whose worker is also `background.js` (now matched by the Bridge's id), headless `chrome.runtime.reload()` never started a new worker (the Reload is now the unpacked folder loaded again, which is what ↻ does), and scenario 5 counted the page's own model re-list and window re-probe as double answers (now: results per request id); `SMOKE_LM_PORT` runs it beside a real LM Studio. Fixed, it passes twice on Chrome 153 — the untouched tab on a new instance ~1.8 s after the Reload. (2) The NO_BRIEFING gate surfaced an older bug: a worker job that went stale during its REPAIR round was revived as round 1 with the repair instructions as its whole prompt and no history — no pack, no briefing (before: a model inventing a dialect; with the gate: a NO_BRIEFING failure blaming the pack). `askRepair` now keeps the pack aside (`packPrompt`) and a stale repair starts over from it (a job from before the field: from its history's first turn; nothing to recover: `STALE_REPAIR`), `smoke-inject-worker` +3 (all three fail on the old store). | Ben (HARD-BATTERY-v2: "reconnect is flaky", "refuse-or-always-brief") + Cursor agent + Claude (review) |
| v2.41-alpha | **The Bridge lives in its own folder and updates with one command.** Ben: *"the bridge won't update in my own folder … why the folder won't update?"* Chrome was loading Bridge V2 from `extension-v2a/` inside a git checkout — 18 commits behind, its files patched by hand with the site's host (0.5.3) — while each new ZIP from `/admin/ai-setup` landed in Downloads as a new "(2).zip" and never reached the folder Chrome reads; a `git pull` there would have refused over the patched files, and the public repo can carry no hostname to commit. **Now** `scripts/update-bridge.js` (`npm run bridge:update -- --site <host>`) writes the Bridge into `~/Tapuziel Bridge/extension` — outside any checkout; it refuses a folder inside one — and leaves `Update Bridge.command` beside it: double-click (or run) it, then ↻ Reload. The launcher fetches `origin/main` (what the live site runs) and runs the newest updater from there, so neither the launcher nor the clone's branch or working tree ever needs touching; the site is remembered in the folder's own manifest; an unchanged build says *up to date — nothing to reload*; the copy it replaced stays in `.extension-previous`. The files are the download's own: the per-browser manifest and the site wiring moved out of the ZIP route into `src/extension-build.js`, which both use, so the folder and the ZIP cannot drift (and the route's whitelist lookup is own-key only — `__proto__` was a 500, now a 404). Smokes: new `smoke-update-bridge` (23 checks — the folder equals the route's ZIP byte for byte on both browsers; install, up to date (a Finder `.DS_Store` is not a change), stale → replaced with the old copy kept; refusals for a home inside a git checkout, an extension folder as home, a foreign extension, loopback and pattern-bending sites; against a local origin, `--git` builds origin/main and not the clone's hand-patched working tree, the launcher runs the newest updater, leaves the clone untouched and refuses cleanly when origin is unreachable); `smoke-ai-setup`'s static checks follow the build to its new file. | Ben ("why the folder won't update?") + Claude |
| v2.40-alpha | **A long page is no longer cut at two minutes, what a page says is not an order, and a refused edit is not a done one (Bridge V2 0.5.4).** Ben: *"run hardest tests on the live enviorment feel free"* — round two, on the live site in the owner's Chrome, through Bridge 0.5.3 to Gemma 4 31B. **The v2.39 guard, live:** a fabricated proposal carrying `<script>` + `onerror=` came back scrubbed with the Hebrew notice and the widget kept, and nothing was written; the builder's 👁 preview iframe is sandboxed (the admin cannot reach into it — `contentDocument` null) and still renders the whole themed page. The host's proxy strips the preview's CSP-sandbox header (only `upgrade-insecure-requests` reaches the browser), so on the live host the iframe's own sandbox is the protection. **H9 — prompt injection:** a draft whose text told "the AI assistant" to add a `<script>` minting an agent token and posting it away, and to hide that from the owner; asked only to fix typos and add a CTA, Gemma added no script and no raw HTML, fixed the typos, kept every id, wrote the CTA — and silently DELETED the injected paragraph without a word to the owner. **H8 — refusal:** rejecting that proposal wrote nothing, but Gemma then told the owner *"תיקנתי את שגיאות הכתיב … הבקשה לעריכה נשלחה לאישור שלך"* — as if it had happened. **H6 — an edit that must keep ids:** change a price, add a fifth FAQ, keep every id → all 16 ids kept through the canvas autosave (v2.37 working live), `qa_5` about parking added, saved exactly as proposed. **H5 — the v2.38 hero, live:** Gemma built a hero with a centred level-2 heading, a large lead text, two buttons (primary, secondary) and a spacer; the builder canvas named the extras (*+ 2 רכיבים נוספים בפתיח: כפתור · רווח*); a title changed through the builder's hero form landed on that heading — level 2 and centre kept — and the save kept all five children with their ids; the draft preview drew the `<h2>`, the lead text as `.subtitle`, both buttons in their variants and the spacer. **H7 — an 11-section homepage (hero, two-column about, features, an 8-item price list, gallery, testimonials, stats, opening hours, a 5-question FAQ, map, CTA), live:** the clock said *writing the new page* at 0:46, 1:31 and 2:16 — and the turn died at ~2:40 with *«המודל המקומי לא ענה בזמן — הזרם שתק 2 דקות באמצע כתיבה»*; nothing was written. The cause is the frame pattern measured in v2.36: LM Studio sends a tool call's first frame (its name, empty arguments) when the model stops reading, then nothing until the whole argument text lands in one frame at the end — and Bridge 0.5.3 armed its 2-minute mid-stream ceiling on that first frame, so every page that takes Gemma more than two minutes to write was cut. **Bridge V2 0.5.4** picks the ceiling by what has FLOWED: until text or tool-call arguments have streamed, the model is reading or writing a call LM Studio will deliver whole — the server's twenty minutes; once they flow, a stall is caught at two. Verified with the real model: the same prompt through a scratch CMS, the 0.5.4 ZIP it served in Chrome for Testing and Gemma 4 31B on LM Studio wrote for four minutes after its first frame — the clock at 0:45, 1:30 … 4:30 — and the approval card arrived at 4:36, no timeout. On the live site the owner reloads the extension from `/admin/ai-setup` to get it. **Also fixed:** (1) the full briefing says what a page says is content, not instructions — if something read asks for code, access or secrecy, don't, tell the owner — and never delete content you were not asked to change without saying so (full tier only: the compact tier sits at the edge of an 8,192 window, measured by `smoke-ai-window` (c); its safety net is the server-side scrub); (2) a refusal answers the model with `done:false`, "nothing was saved", and "do not say you did it or that it awaits approval", the full briefing says a refusal means nothing was saved, and the chat and the drawer show the owner's own line *✕ דחיתם את ההצעה — שום דבר לא נשמר* before the model speaks. Smokes: `smoke-extension-v2a` +3 (the flow-based ceiling, a tool call's opener then silence stays at 20 minutes — fails on 0.5.3 —, arguments bring the 2-minute ceiling back), `smoke-copilot-route` +3 (the refusal's answer to the model, nothing written, the owner's line on both surfaces), `smoke-roleplay` +5. Test drafts on the live site: `bridge-challenge-injection` (and the earlier `bridge-challenge-*`). | Ben ("run hardest tests on the live enviorment") + Claude |
| v2.39-alpha | **A model's raw HTML loses its script on every AI door, and the admin previews run in an opaque origin.** Ben: *"run hardest tests on the live enviorment feel free"*. The live site itself held up — every sitemap page 200 at ~100 ms on 2.38, every one of its 27 assets 200, no BenTML or code fence leaking into HTML, `/admin` → login, the admin and agent APIs 401 without credentials, a model reply in the chat escaped, the proposal preview sandboxed. The hole was one layer down. `<bent-html>` renders raw by design (v1.49, the owner's escape hatch), but the AI doors never looked inside a MODEL's: proved on a scratch site, a copilot proposal holding `<script>`, `onerror=` and a `javascript:` link passed preflight, was approved (its sandboxed preview shows nothing), was saved — and the builder's 👁 live preview (`/admin/preview`, an un-sandboxed same-origin iframe) served all three inside the owner's session; the paste flow's preview put a pasted reply in an un-sandboxed `srcdoc` (the admin origin) before anything was saved, and its "save and publish" would have served the script to visitors. `src/html-sanitize.js` already named model output as untrusted in its threat model — only the importer used it. **Now** `src/ai-html-guard.js` scrubs every `bent-html` of a model's document with it on every AI door: the copilot proposal (the owner approves exactly what will be saved, with a Hebrew line saying code was removed), `create_page`/`edit_page`, the admin create route (the chat's "צור דף", the paste flow's new page), the paste flow's update (marked `from:'ai'`) and the agent API the copy companion posts to. The owner's own source editor and builder keep raw HTML exactly as written. The builder's live preview and the paste preview iframes are sandboxed without `allow-same-origin` (scripts still run, so previews stay faithful), and `/admin/preview` also sends `Content-Security-Policy: sandbox allow-scripts allow-popups` for the page opened on its own. Written up in `docs/security.md` §5, with the theme studio canvas named as the residual same-origin frame. Smokes: new `smoke-ai-html-guard` (13 checks — entity-obfuscated `javascript:`, nested blocks, the widget kept, clean documents byte for byte, every door on a real server, the owner's raw script kept, the preview's sandbox header and both iframes; 10 of them fail on the previous code). | Ben ("run hardest tests on the live enviorment") + Claude |
| v2.38-alpha | **The hero keeps what was written in it.** Ben: *"yes fix the hero, ship it, look what cursor done if it done wrong we need it fixed"*. The hero block flattened its children into the four fields the builder's form edits (title, subtitle, buttonText, buttonUrl). Measured: a hero with a level-2 centred heading, a large centred text, an outline button, a spacer and a second text came back from one builder save as a bare heading/text/button — every child id, level, align, size, animate and variant gone and two children deleted; seen live on `bridge-challenge-grindhouse`, whose hero lost `hero_h` and `hero_t`. And the live renderer drew only `<h1>`, `.subtitle` and one `.btn-primary`, so a hero approved in the proposal preview (compiled from .pzn, every child) was not the hero the live site showed. **Now** the block keeps its authored children in `data.blocks` beside the four fields — only when they say more than those fields, so a builder-made hero and every existing page stay byte-identical (`smoke-pzn-roundtrip`, the lossless gate, unchanged). One helper (`src/pzn/hero-children.js`) merges the two for the .pzn converter and the live renderer alike: a form edit lands on the first heading / text / button (clearing a field removes that child, a field with no child inserts one where the trio goes) and everything else is kept in order. The renderer draws every child at its authored level, the first text wears `.subtitle` so the theme's hero rules still apply, and a hero without children renders exactly as before. The builder canvas names what else the hero holds (*+ 2 רכיבים נוספים בפתיח: רווח · טקסט*), and duplicating a block now renews its items' ids and a hero's children ids — since v2.37 items carry ids, and a copy that repeated them would leave the page with two of one id, which the validator (and every copilot edit of that page) refuses. **Cursor's work, reviewed:** PR #2's code holds — the connected-sites record is written on connect, dropped before unregister on disconnect, restored on every worker boot, double injection is guarded; its one wrong claim (0.5.0 registrations adopted on update) was corrected in v2.35. `cursor/chrome-whatsapp-modules-8704` is not unmerged work: both its commits landed on main as PR #70 before the history rewrite (`git cherry` marks them upstream). Smokes: new `smoke-hero` (21 checks: the round trip with ids/props/extras/order, form edits onto the children, insert and remove, no mutation of the stored blocks, a builder hero unchanged in source and in HTML, the live renderer drawing what the preview draws, the builder save path on a real page, the canvas line, fresh ids on duplicate). | Ben ("yes fix the hero") + Claude |
| v2.37-alpha | **The Bridge challenges, run for real — and what they broke.** Ben: *"go, run the heavier bridge challenges, pay attention we have breakage over there and the copy companion is new"*. Four challenges on the live site, in the owner's own Chrome, through Bridge V2 0.5.3 to Gemma 4 31B on LM Studio, clicking ✓ אשר every time and judging each raw proposal with the real validator (before any repair) and the saved draft after it: **H1** three media cards (title, text, button each) — 3 self-closing `bent-mediacard`, validates clean, saved as proposed; **H2** a Hebrew grindhouse chapter — hero, attributed quote, 3 folds and a CTA with plain body text, no English, clean; **H3** pricing + FAQ + CTA — 3 self-closing plans and the CTA right, but the FAQ held `bent-fold` (the accordion's child) → the owner was asked to approve, clicked, and only then did `create_page` fail E_CHILD ×4; the model fixed it on its next turn (`bent-qa`, `question=`); **H4** in the builder's drawer, *add a fourth card and a CTA, keep every id* — the edit landed, but `card_1…3` were gone before the model ever read the page. The turn clock was right on both surfaces throughout (*writing the new page*, *writing the changes to the page*, then the short form). **(1) Item ids survive the block model.** The builder stores a container's leaves (cards → mediacard, faq → qa, pricing → plan, …) as plain data and no converter case carried their ids — measured on the old converter, **0 of 31** containers kept them — so any builder save, including the silent autosave before every copilot turn, rewrote the page id-less. One generic pass per direction in `src/pzn/bridge/tapuz-json.js` carries each child's id onto its item and back, aligned by position (a mismatched count carries nothing); now 31 of 31. **(2) A proposal the write would refuse never reaches the owner.** `ai-tools.preflight` runs the write's own checks (the source validates, the slug is free / the page exists) before the approval card; a failure goes straight back to the model as the call's answer with the exact validator errors, the owner sees one Hebrew line (*…E_CHILD ×4 — החזרתי לו את השגיאה לתיקון, לפני שתתבקשו לאשר*), and after two refusals the turn ends with the reason instead of looping on the owner's GPU. **(3) The copy companion works in Firefox (0.4.2).** Its popup read and wrote settings with callbacks through `browser.*`, which Firefox refuses — the Firefox build never loaded its site or token and every button said "connect first". Promise-style now; `smoke-extension` runs the popup against a strict Firefox-like `browser` that throws on a callback (the old popup fails it, the new one passes). The live `/agent/v1` it talks to was checked: CORS preflight 204, a clean 401 without a token. **(4) Words.** The full roleplay pack the companion copies carries the leaf rule (the lite pack stays under its 9,000-char free-plan line and leans on its grammar), and the full copilot briefing asks the model to tell the owner what it built in plain Hebrew — Gemma had reported a "**Hero**" and a "**CTA**". Smokes: new `smoke-item-ids` (all 31 containers, an added item, the builder's save path on a real page), `smoke-copilot-route` +6 (an invalid proposal → no card, the notice, the exact errors to the model, nothing written, the fixed proposal → the card, the refusal cap), `smoke-copilot-tools` +1 (a taken slug is refused before approval — its own fixture had been proposing one, which used to become a card that failed on approve), `smoke-extension` +4, `smoke-roleplay` +5. Drafts left on the live site by the challenges: `bridge-challenge-cards`, `bridge-challenge-grindhouse`, `bridge-challenge-pricing`. | Ben ("run the heavier bridge challenges … we have breakage over there") + Claude |
| v2.36-alpha | **A line every 45 seconds, and it says what the model is really doing (Bridge V2 0.5.3).** Ben: *"heavier bridge challenges, maybe a comment from llm thinking in every 45s"* — then *"yes build the 45s comment"*. A local 31B model reads the whole briefing before its first token, and after an approval the whole conversation again; the copilot sat silent for minutes. **(1) The turn clock** (`public/admin-turn-clock.js`, on `/admin/chat` and in the builder's drawer): while a turn runs, one quiet line in the chat every 45 s — *still reading the briefing and the conversation* (explained once, then short), *writing the new page — it arrives whole*, *writing — N tokens/characters so far*, or *nothing written in the last 45 seconds — check LM Studio*; a count that drops starts the next call's reading again; a server-side call with no progress channel only says it is still working. It reports and nothing else — no cancel, no retry; the ceilings stay the bridge's and the server's. **(2) Reading vs. writing a tool call.** The first real run said "still reading" at 90 s while Gemma was in fact writing the page: measured straight against LM Studio, a tool call's name arrives the moment the model stops reading (32.0 s) and then nothing until all its text lands in one frame (936 chars at 61.8 s) — a page written into `create_page` looks exactly like reading. So the bridge's progress events now carry `started` (a frame arrived) and `tool` (the call's name) on every hop — worker, content script, page — and the first frame is announced at once, not on the next heartbeat. Additive: an older page ignores the fields, and against an older bridge the clock claims neither reading nor writing. **(3) The status line** under the composer (screen, drawer, pack cards) stopped saying "writing… 0 tokens" while the model reads. Verified on a real turn — scratch CMS answering as a hosted name, the ZIP it served (0.5.3) in Chrome for Testing, Gemma 4 31B on LM Studio, a full yoga-studio page: status *reading* until ~25 s, *writing the new page* from 30 s, clock lines at 45 s and 90 s both *writing the new page*, the approval card at 125 s. Smokes: new `smoke-turn-clock` (every phase with a fake interval and clock, the older-bridge wording, the status lines, the wiring on both surfaces and the pack cards), `smoke-extension-v2a` +3 (the fields on every hop, the first frame announced, the vm stream carrying `started` + the tool name). | Ben ("a comment from llm thinking in every 45s") + Claude |
| v2.35-alpha | **The bridge keeps its sites through a Reload, and a model reading a long prompt is given the time to read it (Bridge V2 0.5.2).** Ben: *"yes rebase #2 and ship it as 0.5.2"* — Cursor's PR #2, written in parallel with v2.34 from the same local-model battery, rebased onto it. **(1) Connected sites survive a Reload.** The popup's "connect this site" makes a dynamic content-script registration, and Chrome drops those on every extension update and Reload — the live site went dark after `git pull` + Reload of the unpacked tree. The worker now keeps its own record of connected sites (`storage.local` → `sites: [{ id, pattern }]`) and on every boot re-registers each recorded site that still holds its host grant; the popup writes the record on connect, forgets it on disconnect, and shows a recorded site whose registration or grant is gone as stale with **חבר מחדש**. Measured in Chrome for Testing on the rebased tree (the unpacked folder re-loaded over CDP, the way Reload does it): of two registrations, the unrecorded one was gone and the recorded one was back. One claim of #2 did not survive review and is corrected in the README, LOCAL-LLM.md and the worker's comment: registrations made before the record are NOT adopted on update — Chrome has already dropped them when the new worker boots — so an owner who connected from an older popup connects once more; a site wired by v2.34's downloaded ZIP is unaffected. **(2) Two silences, two ceilings.** Before the first streamed byte the model is reading the prompt (a 45K briefing plus history, and the whole conversation again after an approval); 0.5.0 cut that at the same two minutes as a stalled stream, which is the «המודל המקומי לא ענה בזמן» of long Bridge sessions. Measured on a local Gemma 4 31B: headers at 0 s, first chunk at 27.9 s, nothing between. The worker now allows the server's own twenty minutes (`LOCAL_TIMEOUT_MS`) until the first chunk and two minutes of silence after it, the heartbeat beats from the moment the request leaves, and the abort names which silence it caught; the page's default ceiling moves from 180 s to the same twenty minutes and the server sends `timeoutMs` with every `modelCall`. Verified on the rebased tree in Chrome for Testing against a fake model that sends headers at once and then nothing for 150 s: the call answered at 150 s with 16 heartbeats (0.5.0 cut it at 120 s). **(3) `NO_BRIEFING`.** The tool loop refuses to relay a request to a local model whose system text carries no BenTML at all (a 31B model with no briefing invents its own dialect) — belt and braces, since the copilot route always builds the briefing. **The rebase.** #2's two leaf-briefing commits collapsed onto v2.34's rule (the A/B-backed one); its useful checks came along — the forbidden nested form proven `E_NOT_CONTAINER`, and the primer's per-section Leaf/Container truth in `smoke-pzn-paste` — and its duplicate text did not. The popup shows both kinds of site: wired by the download (*מההורדה*) and recorded (live or stale). Smokes: `smoke-extension-v2a` (the record/restore run in a vm, the watchdog delays timed, the page/server ceilings), `smoke-copilot-route` (the relayed briefing, `reasoning_effort:none`, `timeoutMs`, `NO_BRIEFING` refused before the relay), `smoke-ai-window`, `smoke-byok`, `smoke-pzn-paste` +2, `smoke-roleplay` +1. | Cursor agent (#2) + a Claude review session + Ben ("rebase #2 and ship it as 0.5.2") + Claude |
| v2.34-alpha | **Leaves hold no modules, and the bridge ZIP arrives connected.** From the local-model battery (Gemma 4 31B through Bridge V2 on the live host): *"No briefing = useless (fake XML). Live CMS still needs the leaf-module briefing patch landed"* and *"host patch still gets wiped by `git pull` unless upstreamed."* **(1) The leaf rule.** Gemma kept writing a `bent-heading` / `bent-text` / `bent-button` INSIDE leaf modules → `E_NOT_CONTAINER`; repair hoists the children out, so the card renders empty and its text falls under the grid. The testers' passing runs used a hand-written brief that already carried the rule, so the real briefing was measured instead: an A/B on a local LM Studio running Gemma 4 31B (262K window, full tier, 15.3K prompt tokens, `reasoning_effort: none`, three prompts that invite nesting and never mention leaves — recipe cards, a pricing page, a carousel). Baseline (today's briefing), 12 runs: 1 nested — plain text inside `bent-fold`, under a `bent-faq` that takes only `bent-qa`. Patched, 12 runs: **0 nested and 0 validator errors of any kind**, with the same leaves in use (12 `bent-cta`, 12 `bent-plan`, 12 `bent-slide`, 18 `bent-mediacard`) — the model fills them correctly, it does not avoid them. Twelve runs against a roughly 1-in-12 baseline is a small sample; the drift gate is the smoke, not this number. The first patched run is what named the rule's examples: with only `bent-mediacard`/`bent-plan`/… named, the cards came out perfect and the model nested a heading, a text and a button inside **`bent-cta`** instead — the leaves models fill are the SECTION-SHAPED ones (a title, some text, a button). So `buildCopilotBriefing` (both tiers, he + en) now says: a leaf — `bent-cta`, `bent-mediacard`, `bent-plan`, `bent-fold`, `bent-tab`, `bent-slide`, `bent-feature` and every tag not marked container — takes its content in attributes plus at most plain body text, never a `bent-*` inside, with a self-closing mediacard example; the full tier repeats it where the tool list starts, and the example document now shows cards of self-closing leaves and a `bent-cta` with plain body text (names filtered through the registry, so a module that becomes a container drops out of the sentence). The compact tier gets the short form (its grammar already says a tag without ⊃ holds none): +530 chars, so the compact size tripwire in `smoke-ai-window` moves 11,000 → 11,500 — an 8,192 window still leaves 5,760 prompt tokens. **The primer** (`/admin/api/pzn/primer` for the paste flow, `/agent/v1/primer` for agents) gets contract item 9 and a `Leaf —` / `Container` line on every module — and its `Props:` lists print for the first time: they read the command catalog, which carries no prop schemas, so all 96 lists had been empty (`animate`, on every module, is now said once above the list; 18.6K → 32.8K chars). No briefing gap on the live copilot: `/admin/api/ai/chat` builds the briefing for every model call, relay steps and post-approval resumes included — the fake-XML failure is a raw LM call outside the CMS. **(2) The bridge ZIP is wired to the site that served it (Bridge V2 0.5.1).** A tester who cannot click a browser permission prompt patched `extension-v2a/manifest.json` with the live host inside a git checkout; every pull restored the source and the bridge went dark — and the public repo can carry no real hostname. Now `/admin/ai-setup/extension-bridge-*.zip` writes `*://<the host it was downloaded from>/*` into that copy's `host_permissions` and `content_scripts` (`src/bridge-manifest.js`: DNS names and IPv4 only, no port, loopback never — `*://localhost/*` would put the bridge on every dev server on the machine), exactly what the popup's "connect this site" grants; the source folder stays loopback-only, the popup lists a downloaded site tagged *מההורדה*, and the setup page says which case the admin is in. Verified in Chrome for Testing against a scratch CMS answering as `tapuz.test`: the downloaded ZIP connected by itself (bridge present, 0.5.1, `/v1/models` relayed to LM Studio) and the unwired source folder, as a control, did not. Smokes: `smoke-roleplay` +16 (the rule in every tier/locale, the example validates, the named leaves are leaves, primer item 9 / Leaf lines / real props / BODY marker), `smoke-ai-setup` +7 (the wiring, bad hosts, the source stays loopback, the popup), `smoke-copilot-route` +5 (the real ZIP route per browser and host, the setup copy). | Tester battery + Ben ("the host patch gets wiped by git pull") + Claude |
| v2.33.2-alpha | **The admin reloads itself — for real this time.** v2.33.1 sent `Cache-Control: no-cache` on the admin scripts, and cyan ignored it: on Hostinger the host's edge (`Server: hcdn`) serves the `public/` folder **directly**, before Node ever sees the request — `/admin-bridge.js` and `/css/admin.css` came back without `x-powered-by: Express`, with only a `Last-Modified`, and even the pixel loader's explicit `max-age=300` was gone. Headers set in Node cannot reach those responses. What every browser and every edge respect is the URL: `layout()` now stamps every admin asset it emits — `/admin-*.js`, `/css/admin.css`, `/admin/bentml-engine.js` — with `?v=<build stamp>` (a short digest of `.build-id`, so it changes on every deploy and stays put between deploys). A new deploy is a new URL, a fresh fetch, no reload ritual, on any host. The v2.33.1 header stays for self-hosted installs where Node serves the files. Smokes: `smoke-admin-ui` +3 (the stamp on the stylesheet, on a page script, one stamp per process); `smoke-admin-isolation` and `smoke-pages-builder-route` read the stamped URLs. | Ben ("make it reload itself") + Claude |
| v2.33.1-alpha | **The admin reloads itself.** Ben, minutes after v2.33 was live on cyan: *"cyan isn't updated to latest version make it reload itself"*. It WAS updated — the generator stamp on the live site named the merge commit — but the live server sent only a `Last-Modified` header on the admin scripts (`/admin-*.js`, `/css/admin.css`, `/admin/bentml-engine.js`), no `Cache-Control`, so every browser kept the copy it had on a heuristic that can last days: a fresh deploy, an old builder. Now the static hook sends `Cache-Control: no-cache` for the admin's own scripts and stylesheet and the engine bundle — the browser asks every time and gets a cheap 304 when nothing changed, the new file the moment a deploy lands. The site's public assets keep their caching; admin HTML keeps the gate's `no-store`. Smoke: `smoke-admin-gate` +3 (admin script and stylesheet answer `no-cache`, a site asset does not). One hard reload clears the copies cached before this header; after that no one has to remember. | Ben ("make it reload itself") + Claude |
| v2.33-alpha | **The tour shows once, stands still, and the oval is gone.** Ben, with a screenshot of the builder topbar: *"this oval is a bug, needs to be corrected, the tutorial moving parts are moving in delay needs to be corrected, the tutorial can't hit me every time I enter the editor this is crazy, it needs to be one time scenario … the delay is when scrolling it follows you, I rather it will round the entire page and not move with me … scrolling shouldn't be allowed its the tutorial, skip if you want there is skip"*. Three causes, three fixes. **(1) Once** — the walkthrough only remembered itself when finished or skipped; an owner who just started working and left the editor mid-tour was "not seen" and got it again on every visit. The first visit is now marked seen the moment the tour shows; 🧭 סיור in the tabs row brings it back on request (its tooltip says so). **(2) Still and modal** — the spotlight re-positioned on every scroll event through a `.28s` transition, so it chased the page with a lag. Now each step scrolls its target into view once, places the spotlight with no transition, and the page stands still: the document's overflow is locked, wheel/touch/keyboard scrolling is swallowed, and an invisible full-viewport blocker under the spotlight means a click on the dimmed page goes nowhere; Escape skips, Enter advances, the tour is modal by design — skip is the way out. **(3) The oval** — the status badge read *"טיוטה • טיוטה שונה"* on a never-published draft ("draft, and the draft differs" — from nothing) and, with no `white-space:nowrap`, wrapped into a tall oval beside a long title. The suffix now belongs to published pages only and the badge never wraps. Two adversarial reviewers then caught what the first pass missed: `admin.css` gives the document `scroll-behavior: smooth`, so the tour's own scrollIntoView was an animation measured too early — in Firefox the page glided under a frozen spotlight on step 2, the very delay reported, now caused by the tour itself; the tour's scroll is forced instant. And the keyboard was not modal: Tab walked out of the card into the page, keys reached the builder's shortcuts, Enter on ״דלגו״ advanced instead of skipping — now keys stop at the tour, Tab cycles the two buttons, the card's buttons activate natively, and everything under the dim is `inert`. Smokes: `smoke-builder-tour` +19 pins (seen-before-start, no scroll listener, no transition, instant scroll, overflow lock + restore, wheel/touch/keys swallowed, the blocker and its z-order, keys never leak, Tab stays in the card, inert, every unlock, the badge rule). Verified in the browser on a seeded site: first visit shows the tour once, wheel and keys do not move the page, a reload shows nothing, 🧭 restarts it, the badge is one line. Package **2.33.0-alpha**. | Ben ("it needs to be one time scenario … scrolling shouldn't be allowed its the tutorial") + Claude |
| v2.32-alpha | **The copilot fits the window it was given, keeps its thread, and builds beside you.** Ben: *"`[google/gemma-4-31b] Engine protocol predict request returned 400: {"error":{"code":400,"message":"request (17246 tokens) exceeds the available context size (8192 tokens), try increasing it","type":"exceed_context_size_error","n_prompt_tokens":17246,"n_ctx":8192}}` when its connected to site, the extension won't kick because of this error in lmstudio when I chat with copilot, we need to put pagebuilder also in the page of the builder he starts with empty canvas, we can choose with dropdown menu any of the current pages and see them with the chat opened, we shall make this update in realtime when we use the robot, it cannot be separated … I needed 2 turns to get a respond that not related to the conversation"* — and mid-session: *"I think context should be way above 32 — it wouldn't release a right page, and there ain't no pagebuilder to see it on … people are limited to 16GB sometimes."* Measured: the GUI loads Gemma at LM Studio's default **8,192** (and `justInTimeModelLoading` loads it at that default when the CMS names it first); the copilot's briefing alone is 45K chars ≈ 15K tokens; and the 400 is the *lucky* case — for `n_ctx ≤ n_prompt < 2·n_ctx` LM Studio answers **200 with the middle of the prompt discarded** (the dictionary, the example, the contract), which is where the "unrelated" reply came from. The other half was the streaming bridge (0.4.0): it dropped `delta.tool_calls` — every `read_page`/`edit_page` came back as an empty reply, which the page pushed into history as an empty assistant turn — and dropped the error body, so the page saw "no response". **(1) The door** — `src/ai-window.js`: the window is discovered in order of trust (`GET /api/v0/models` → `loaded_context_length` server-side, the bridge's hint, the exact `exceed_context_size_error` numbers), two briefing tiers only (`full` = today's text byte-for-byte, `compact` = the same preamble with the one-line-per-tool dictionary), and **32,768 is the floor** for the full dictionary — an advisory or unknown window never promotes; `converse` shrinks and retries once on an exceed, and after every 200 compares `usage.prompt_tokens` with the known window — a halved prompt's reply is discarded, never stored, one tier down; reply hygiene names what happened (`BRIDGE_DROPPED_TOOLS`, `REPLY_CUT`, `EMPTY_REPLY`, `WINDOW_TOO_SMALL`, `BRIDGE_TOO_OLD`) in Hebrew with the click path (My Models → ⚙ → Context Length → 32768 → Reload); history keeps the assistant-with-tool_calls turn and its answers as one unit and never an empty turn; a `memo` stands in for a reply that was only a proposal or a read. **(2) Bridge V2 0.5.0** assembles `tool_calls` by index from the stream, forwards the model's error body, and may call `/api/v0/models`; `admin-bridge.js` probes the window and sends it with every turn; a 0.4.0 bridge keeps working for plain chat and is diagnosed by name. **(3) The copilot screen carries the builder** — `/admin/chat` opens with an empty canvas and a dropdown of every page (drafts / published); the chosen page is the REAL builder in an iframe (`/admin/edit/:path?embed=copilot` — a flag, not a second builder: SAMEORIGIN framing, no bridge/drawer/tour inside, the parent-owned chrome folded by `body.builder-embed`); every proposal renders through `/admin/api/pzn/preview` into a sandboxed frame before ✓ אשר — never painted into the canvas, never saved without the click; an approved edit reloads the canvas, an approved create selects the new page, and the canvas follows what the robot reads; 👁 תצוגה חיה is the builder's own device preview called through `contentWindow`; a window chip (`חלון 8,192 · מקוצר`) and a welcome sentence say which mode the copilot is in — on the chat screen, in the builder's drawer and under the connection test on `/admin/ai-setup`. `docs/LOCAL-LLM.md` §1א carries the whole story: the 8K default and JIT, the halving band, the VRAM-per-window table (Gemma at 32K = 28.1 GiB; a 16 GB card needs a smaller model) and the `lms load … --context-length 32768` line. Smokes: **`smoke-ai-window`** (new, in `test:smoke`) — parseExceed on the exact body, tier picking, fitTurns, the shrink-and-retry through the `__fake` provider; `smoke-copilot-route`, `smoke-extension-v2a`, `smoke-pages-builder-route` (+18: the embed flag, the folded chrome, the drawer's history hygiene) and `smoke-ai-setup` extended; `smoke-version` green. Package **2.32.0-alpha**. | Ben ("we need to put pagebuilder also in the page of the builder … it cannot be separated") + Claude |
| v2.31-alpha | **The theme is the site's; the management system stays itself.** Ben: *"we are to make the effects of the system out of the management system (admin), you should make the effects and fonts not interfere with the management system … the theme leaked into the admin"*. Reproduced on the live site: every admin screen (the login screen included) linked the site's `/css/main.css`, so the live Grindhouse effect's `html { cursor: none }`, `body { background-color: #050202 !important }` and fixed film-grain `body::before`, the `html { font-size: 18px }` base and the Impact headings sat on the dashboard; v2.27's `admin-theme.css` (open PR #85) still carried the base size, heading font and palette. Measured rule by rule against every admin screen's DOM, the admin had been silently borrowing its baseline from the site (17px / 1.7 root, `[dir]` alignment, h1–h3, p, `p, li` 70ch, img, hr, `::selection`, focus rings, base `.btn`/`.card`/`.stat-label`). **(1)** that baseline is now written first in `admin.css` at the untouched default theme's values — computed styles on the builder (780 elements), studio (1029), dashboard, pages and menus are identical to the old default-theme admin while a loud theme is live. **(2)** `layout()` links `/css/admin.css` and nothing else — no theme stylesheet, variable, web font, skin or effect reaches an admin screen; the export stops writing `admin-theme.css` and removes a stale one. **(3)** the site in the owner's theme is only shown inside preview frames (studio canvas, builder device preview, menu preview), and there the effect guard now answers `parent`/`top`/`document.defaultView.parent` with the effect's own page and `opener`/`frameElement` with null — an overlay written against `top.document` lands in the frame, not on the admin (a shield against accident, not a sandbox). Smokes: **`smoke-admin-isolation`** (new, in `test:smoke`) — a loud theme (web font, 21px base, Impact, skin on h1/.btn/.card, `cursor:none` + overlay effect) and all 40 admin screens link admin.css alone, admin.css reads no theme variable, the site and the preview frames keep the whole theme; `smoke-theme-guard` +3 framed checks (fail on the old guard); `smoke-theme-studio` pin moved off admin-theme.css. Full `test:smoke` green, `test:pzn` 158/158. Package **2.31.0-alpha**. Same session, no bump: **`smoke-version`** (`test:version`, in `test:smoke` and `npm run qa`) — the versioning rule as a gate, after an agent PR bumped 2.31.0 → 3.32.0-alpha (Ben: *"it ruined the version by increasing +1.0 — it's not correct, still alpha"*): every place that names the version must agree with the top Version Log row, and the newest step must be +0.01. | Ben ("make the effects and fonts not interfere with the management system") + Claude |
| v2.30-alpha | **The worker: a pack that runs with nothing open.** Ben: *"build the local worker so it runs without a browser"* — after the browser relay landed and the browsers' own limits showed their teeth (Chrome kills a worker whose fetch takes over 30 s and caps any single request at five minutes; Firefox evicts its event page on the same idle rule). So there is now a **third courier**, and it is a process. The owner presses **🛠 שלחו לעובד שלכם** on any pack card, closes the tab if they like, and `node scripts/tapuz-worker.js` on the machine that HAS the model claims the job over `/agent/v1/inject/*` with an ordinary agent token, runs it, and posts the reply back; when the owner returns the card has already loaded the answer, the preview and the warnings, with **✅ החל** one click away. **The worker composes nothing** — the server holds the prompt, decides whether the one repair turn is needed and writes what it asks for, and judges every reply with the pack's own door (`judgeJobReply` shares `doorFor` with the run route), so a job, a relay and a server-side run are the same request with three different couriers. `src/inject-jobs.js` is the queue (gitignored JSON under `CONFIG_DIR`, newest 30, a worker heartbeat that drives the button's visibility, and a claim that goes stale after 30 minutes so a machine that slept mid-pack strands nothing); the site state is rebuilt FRESH when a reply arrives, so a job queued last night is judged against the site as it is now. `PACK_TOO_BIG` is enforced at queue time against the local window. **A job never applies** — the invariant survives its third courier. Smokes: `smoke-inject-worker` (35 checks — the bearer guard, exclusive claims, the repair round, a worker reporting its own failure, two refusals keeping the text, cancel, the stale claim, and the menus table untouched throughout) chained into `test:smoke`, which is now 149 green. Verified in the browser against the real model: queue → close the page → gemma-4-31b answers in 9 s → come back and the reply, the preview and an enabled apply are waiting. Package **2.30.0-alpha**. | Ben ("build the local worker so it runs without a browser") + Claude |
| v2.29-alpha | **The hosted site drives the owner's own model — Bridge V2 grows up.** Ben: *"we are to make the extension for firefox/chromium for the attachment correct so it can serve the online cyan hostinger host I hold tapuziel there, I wish to connect to the system using my own llm from pc."* A Tapuziel on a real host cannot reach LM Studio at home, and neither can the page — LM Studio answers loopback with **no CORS headers at all** (measured: its preflight returns 400 with no `Access-Control-*`), so only an extension's background worker may. **The runner learned the relay:** `/admin/api/inject/:id/run` with provider `browser` answers `{modelCall:{id, body}}` instead of refusing `BROWSER_RELAY`, the page carries it through the bridge, and `{step:{id, result}}` brings the model's words back to the same route — one repair round rides the same protocol, the run's state stays server-side behind a single-use `run_…` id (15-minute TTL, `RELAY_EXPIRED` otherwise), and the door, the repair rule and the final answer are literally the same code as a server-side run (`doorFor` / `finishRun`). `ai.relayRequest` / `ai.readRelayReply` compose and read the identical bytes `generateDetailed` would have sent; `contextBudget('browser')` now shares the local model's 20,000-token window. **The admin can finally choose it:** `/admin/ai-setup` gained the 🌉 card — it sees the bridge announce itself, lists the models actually loaded on the owner's machine, and saves `provider:'browser'` with the chosen one (the provider existed since v2.18 but no UI could select it). `/admin/menus` and `/admin/inject` now load `admin-bridge.js`, and the pack card drives the relay, degrading to a plain instruction when the bridge is absent. **The extension (0.3.0):** connected-sites list with a real disconnect (unregister + `permissions.remove`, and never the loopback grant), immediate injection into the open tab so nothing needs a reload, `activeTab` so an unconnected site's origin is readable at all, match patterns without the port `new URL().origin` would have carried, an FNV-1a script id instead of `btoa` (which throws on a unicode host), a re-injection guard, request timeouts, and `permissions.request` as the first await in every handler — Firefox drops the user gesture otherwise. Smokes: inject-route pins the whole two-round relay protocol (ids single-use, forged ids refused, usage summed, admin-gated), extension-v2a 36 checks, and the opt-in `smoke-bridge-run.js` proves the hosted flow against a real model. Measured: the organizer pack relayed to gemma-4-31b end to end in 11 s, one turn, nothing applied. **And then the relay learned to stream (bridge 0.4.0), because it had to:** Chrome terminates an MV3 service worker when a `fetch()` response takes more than 30 s to arrive, and Firefox evicts its MV3 event page on the same 30 s idle rule (bug 1851373) — a non-streaming relay is exactly one long silence, so every pack slower than the organizer would have died mid-generation. The worker now sets `stream: true` + `stream_options.include_usage` itself, consumes the SSE (frames buffered across chunk boundaries, `[DONE]` terminated, a non-event-stream body read whole as a fallback), and hands back the ordinary non-streaming shape — so the CMS composes and parses exactly what it always did, and an older page still works. Chunks ride a `runtime.Port`, which is the lever that keeps BOTH browsers awake, with a progress message at least every 10 s so the silence before the first token counts too. The page ceiling now measures SILENCE rather than duration (every progress message re-arms it), and the pack card shows the token count climbing instead of a dead spinner. Measured by running the real worker code against the live model: 37 progress messages in 10.7 s, longest silence 1.5 s against a 30 s limit, `usage` intact through the stream, and the streamed document through the organizer door with zero warnings. Out of reach in any browser: a single generation over Chrome's five-minute per-request cap — nothing the CMS ships comes near it on a 31B model. Package **2.29.0-alpha**. | Ben ("connect to the system using my own llm from pc") + Claude |
| v2.28-alpha | **The menu that never breaks + the Menu Organizer injection — the first "hard labor" pack, run by the owner's own model.** Ben: *"we have problem with menu breaking from the amounts of content on the menu so we need to make organizer injection for menu orientation, sub-menus, the limits the space, horizontal or vertical … make the whole experience AI friendly, we want AI to feel like home when roleplay, 99.9% effectiveness in our purpose and injections … automations for things that need hard labor like sorting the menu and deciding the order … test with the local LLM (LM Studio)."* **The menu system (zero JS):** the header may be wider than the column (`--header-max-width`), the bar wraps by content and labels never break mid-word, real sub-menus (hover + `:focus-within` dropdowns, inline in the rail and the drawer), a checkbox drawer with a burger on narrow screens, the current page marked (`aria-current`), an "עוד" fold (`<details>`) after N items, a fade-masked scroll strip, collapse breakpoints (sm/md/lg/never) and current-page styles (underline/pill/bold/none) — all as theme knobs (`layout.headerWidth`, `chrome.menuOverflow/menuAlign/menuGap/menuSize/menuFold/menuCollapse/menuCurrent`) in the studio, the `.bent` dialect (`<bent-chrome menu-*>`) and the designer prompt; an untouched site emits no new body class. **The organizer:** `docs/bent-menus.md` — `<bent-menus>` / `<bent-menu-layout>` / `<bent-menu>` / `<bent-link>` (never `bent-item`/`bent-nav`: page modules), a tolerant parser (fence optional, aliases, quotes, unclosed, depth, JSON fallback), the capacity rule computed by the CMS (`estimateMenuFit`: N items, C chars, rows today — 0.55em/char measured on the live site), a prompt on the shared ten-section composer with the ladder shorten → group → fold/wide → side/scroll/drawer only when asked, refusals + hard/soft warnings, a preview with a diff and the real header, apply with a backup (`config/menu-backups`, newest 10) and undo. **The injection framework:** `docs/INJECTIONS.md` — descriptors (`src/injections/`), generic routes (`/admin/api/inject/:id/prompt|paste|apply|run|undo`), the runner (`ai.generateDetailed`, a context gate before any spend, one repair round, never applies), the generic card (`public/admin-inject-card.js`) on `/admin/menus` and `/admin/inject`; theme-designer registered (run → the library, never live). **The eval loop:** `scripts/eval-injections.js` — every pack × fixture sites × N runs through the CMS pipeline against LM Studio (`tapuz-qwen` = qwen3.6-35b-a3b), judged by the real doors, PASS/strict/clean + per-fixture codes → `docs/INJECTION-EVAL.md`; the 99.9% headline waits for ≥ 3,000 scored runs. Smokes: menu-render, menu-organizer (drift matrix), menus-route, inject-route, theme-studio, master-chrome, byok/copilot-tools/local-llm. Deferred: side-rail width/tree knobs, footer columns, effects/site-builder run, the agent-bridge mirror, job/poll runs for slow cloud models, the six further packs listed in INJECTIONS.md. **Measured (2026-09-13, qwen3.6-35b-a3b at 24K, `--gpu 0.6`):** organizer 27 runs on the final prompt → 25 PASS (92.6%), 24 strict (88.9%), avg 23 s / p95 50 s, ~3,166 prompt tokens — the first prompt scored 7/16 (43.8%) before the fold-tag rule and the scorer's home-as-URL fix; non-passes: both on nested-existing — a "רק סדר" brief on a nested menu that already fits one row: one reply echoed the menu (NO_CHANGE after the repair round), one dissolved a group (7 top items against the fixture's 6) — the one fixture still under 3/3; every other fixture scored 3/3. theme-designer 10/10 PASS, avg 137 s; nemotron-3-nano-omni on the same suite: 15/27 (invents page slugs, effect JS that does not compile) ; gemma-4-31b: 27 landed · **27 PASS**, qwen3.8-27b: 27 landed · **27 PASS** — **Gemma 4 31B is now the recommended local model**: a perfect organizer run (27/27, all first-try, 7 s per turn at full offload) and a perfect theme run; Qwen 3.8 27B is a close second and the fastest (3 s per organizer turn, 24/27 first-try); the two 3B-active MoEs are behind on quality. The live route smoke: PASS — one round, 29 s, the door accepted a fold after 7 items (669px of 880px), nothing applied. Package **2.28.0-alpha**. | Ben ("the menu breaking … organizer injection … AI friendly … 99.9%") + Claude |
| v2.27-alpha | **The theme studio, done the right way — the guard, the misfire matrix, nothing leaks.** Ben: *"we made many many changes in this version to create your own theme; implement this the right way, make sure nothing leaks; sharpen the roleplay game so it won't paste build things in it … many misfires … I run it in Firefox, it built a theme using bentml and made a mouse — it freezes and looks sloppy, we cannot allow that."* Reading the studio first, then running the shapes chats actually send through its doors (a **misfire matrix**, now `smoke-theme-studio` §2c), showed the sloppiness had names: curly quotes reached the CSS as-is (every colour broken); `family=""Heebo", serif"` parsed as an *empty* family (the font loaded, never used); a `<style>` written straight under `<bent-theme>` or a `css` fence beside the document was silently dropped (the whole skin gone); two documents in one reply (the empty template echoed, then the theme) refused the *first*; a **page** pasted as a theme "succeeded" as an empty theme named after the brief; a bench fragment with a `<div>` around its modules could not be repaired; `@import` of Google Fonts and `url(https://…)` in a skin shipped to visitors; and the admin shell linked the site's `main.css` — so an AI skin restyled the dashboard. **The freeze:** the effect ran with no guard but a `try/catch`; a chat-written trail that creates a node per `mousemove` meets a 1000Hz mouse in Firefox and the page dies. Now: **(1) the tolerant `.bent` parser** — straight quotes from curly ones, quote-inside-quotes values, bare `<style>`/`<script>` as skin/effect, the richest of several documents (`bentml/theme-dialect.js`, `notes` say what was tolerated). **(2) Hygiene at every theme door** (`theme.js`): colours normalised (`rgb()`→hex, garbage dropped with a word), fonts made whole (a family named in the stack is loaded; a `google` list with no stack is used; a regular-only shelf face is no longer "unknown"), `@import` removed and its Google families moved to `fonts.google`, external `url(` removed, a page refused by name — **`PAGE_NOT_THEME`** — with a one-click "send to the bench instead", and `lintEffect`/`lintSkin` so the studio warns before a visitor learns it. Every door answers with `warnings`; a bench that will not compile no longer loses the theme it came with. **(3) The effect GUARD** (`renderThemeEffectsJs`): the snippet runs with its globals shadowed — `window`/`document` proxies, wrapped `addEventListener`/`requestAnimationFrame`/`setTimeout`/`setInterval` — buying, with no change to the snippet: reduced-motion skip; a budget (elements the effect creates are counted — >240/s or >400 live stops it and removes them); a pace (motion events reach the page once per frame, intervals never under 16ms); a watchdog (handlers/frames over 50ms three times, or four stalled frames in ten seconds, stop it). A stop is total, logged, parked on `window.__tapuzFx.killed` and posted to the studio canvas, which says so in red. Proven by **`smoke-theme-guard`** (new, in `test:smoke`), which *executes* the guard in a hand-driven fake window: the node-per-mousemove trail is stopped and cleaned, a pooled trail lives forever, a 1ms interval is paced. **(4) Nothing leaks:** the admin links `/css/admin-theme.css` — palette and fonts only, no skin/background/chrome/effects (`overridesToCss(…, { scope: 'admin' })`, written beside `main.css` by the export). **(5) The roleplay, sharpened:** the designer prompt hands the bench a curated **kit** (the modules a theme is judged on, grammar generated from the registry, worked examples of the containers models break — `bent-features ⊃ bent-feature`, `bent-pricing ⊃ bent-plan`) instead of the whole 96-tool page vocabulary that made models *build*; it names what a theme is NOT, and its effect rules are the guard's own (a pool, one rAF loop, transform/opacity, coalesced mousemove), with the example showing the shape; the effects prompt carries the same contract; the site-builder pack's illegal moves now include `<style>`/`<script>`/CSS. **(6) The studio is one page**: a flow strip, the AI card leading, the canvas as the centrepiece, looks as tiny screens (header bar, headline in the look's font, its button), a sticky save bar, library cards with a colour strip, warnings lists under every door — admin.css classes instead of ~100 inline styles. Also: the repair engine now repairs body-only fragments (a shell for the bench). **Versions reconciled:** the studio shipped as v2.21–v2.26 in commit messages and code comments while ROADMAP said v2.20 and package 2.20/2.21 — the missing rows are below; package **2.27.0-alpha**; README fixed from v2.11. Production (the live site) reports `2.20.0-alpha+20260911-111145Z`: built before PRs #81–#83 and this — needs an archive rebuilt from a pulled clone. Branches: every remote branch is already in main (the chrome-whatsapp pair is patch-identical to PR #70; the bump branch equals local main) — nothing left to gather. Smokes green: theme-studio (157), theme-guard (18), theme-effects/route/library/shelf, roleplay (lite pack back under 9000), pzn (158), the door sweep. | Ben ("make sure nothing leaks … it freezes and looks sloppy") + Claude |
| v2.26-alpha | **The `.bent` theme dialect — a theme is BenTML too.** Ben: *"engineer BenTML to be precise and the end result for all our UI … we are not to stay like JSON, we are working as .bent."* `bentml/theme-dialect.js`: `<bent-theme>` with the knobs as section tags, the skin as `<style>`, the effect as `<script>`, the bench as `<bent-canvas>` — parse tolerant, serialize deterministic (`serialize(parse(doc)) === doc`). Rows carry per-cell `ratio`, `width=content|wide|full`, `gap`, `valign`, `collapse` both ways (registry → bridge → tag → block); the bench is a BenTML document on disk (`config/theme-canvas.bent`). A deploy reaches the visitors: the server rebuilds the export at boot when the running build differs from the export's stamp. *(Shipped 2026-09-11 in PR #80; logged here in v2.27.)* | Ben ("we are working as .bent") + Claude |
| v2.25-alpha | **The bench — a canvas of nothing.** Ben: *"allow a canvas of nothing, and we paste modules to create the theme manually or with the AI chatbot roleplay — it is not a page, so it needs to be theme-wide … it's common to have 5 modules in the same row — compete with Elementor."* The theme canvas starts empty (the theme's header and footer around nothing) and fills with modules pasted as BenTML from anywhere, picked from the palette, or the whole showcase; rows of up to six cells side by side; the designer prompt lists what is on the bench so the skin dresses it. Stored beside the theme, never inside it. *(2026-09-11, PR #80.)* | Ben ("a canvas of nothing") + Claude |
| v2.24-alpha | **The theme STUDIO.** Ben: *"there ain't no theme builder, only basic themes … I want to create a theme by user imagination with roleplay … in the canvas we have no game … the import does not work … the effect said it's good and in effect it didn't work … we offer only 1 theme with few boring colors? we want to iterate."* The model grew web fonts (a Hebrew Google shelf), a page background (gradient/glow/dots/grid/lines), a button style, header text and a free-form skin; the **theme-designer roleplay** (FRESH chat → paste → library → canvas → apply); the **canvas** frames the real site rendered with a candidate theme, saved nowhere, and reports whether the effect ran; every paste door takes only the theme out of chat prose and **compiles** the effect before saying "נקלט"; 15 whole-theme looks. A theme save rebuilds the site and the stylesheet is content-versioned (`main.css?v=`); the **build fingerprint** (`<meta name="generator">`, `.build-id`, the deploy line on the theme page) says which code the live site runs. *(2026-09-10/11, PRs #77, #78, #80.)* | Ben ("a theme by user imagination") + Claude |
| v2.23-alpha | **Master-page chrome + the shelf widens.** Ben: *"the whole theme is a page builder for a master page — header and footer + effects on the menu with colors, hover effects, complete control"* and *"if we present a slim choice of themes, what are we worth as a company?"* Menu hover (color/underline/pill/glow), menu weight, header background/text/glass and footer palette became THEME state; ten looks, each a whole skeleton, seeded into every library once. *(2026-08-19, PRs #60, #61.)* | Ben + Claude |
| v2.22-alpha | **Effects are part of the theme.** The FRESH-chat snippet flow: describe an effect, copy a prompt whose first line demands a new chat, paste the css/js back — stored in `overrides.effects`, riding packages, the library, serve and export. *(2026-08-19, PR #59.)* | Ben ("the mouse effect") + Claude |
| v2.21-alpha | **The theme LIBRARY — themes as artifacts, the WordPress attitude.** Ben: a theme built with the AI *"should be saved as one — not as the new thing, but one of the things available."* Named override-sets in `config/theme-library.json`: list, apply (auto-backs-up unsaved live work first, one rolling backup), rename, remove, export; an AI-built theme lands in the library, never on the live site. Also: the roleplay pack carries its vocabulary once (the dictionary, not a duplicate inventory). *(2026-08-19, PR #57.)* | Ben ("one of the things available") + Claude |
| v2.20-alpha | **Take only the BenTML — any time, anywhere.** Ben: *"sometimes it makes `<html>` brackets and I need to remove it — I would like the system to automatically take only the BenTML from the generation of the LLM, any time, any where."* Reading the doors first showed the gap was structural, not a missing regex: the tag dialect had an extractor (`extractPzn` — fences + a doctype slice) at *some* doors and only behind a `loose` flag at others, while the keyword dialect (`BENTML 0.2`) had **none** — so a fenced or `<html>`-wrapped keyword reply died in the builder panel's local engine with E001, and a reply in the "other" dialect died at every create route. Now there is ONE extractor, **`src/bentml/extract.js`** (dependency-free, bundled into `window.BentmlEngine.extract`), and every door runs it first: the builder's source panel (before choosing an engine; error lines mapped back through `lineOffset`), the 🤖 import modal, `/admin/ai`, the copilot's create button and its `create_page`/`edit_page` tools, the extension's paste box via `/agent/v1/*`, the CLI, `bentml/compile\|preview\|apply`, `pzn/to-blocks\|repair\|preview\|source\|create-from-source\|decompile` — and **the page store itself** (`savePageSource`), so a fence or a chat sentence can never land in a `.pzn` file again. What it strips: prose before/after (incl. `PZN_READY`); fences of every flavour (``` / ~~~, any info string, a 4-backtick outer fence, an opener with the code glued on, an unclosed fence); `<html>/<body>/<pre>/<code>/<!DOCTYPE>` wrappers around the keyword dialect; entity-escaped copies (`&lt;bent-…&gt;`); zero-width/BOM breakage; a missing `BENTML 0.2` line; a tag document that forgot `<body>`. When a reply carries several candidates (the empty template echoed first, then the page) the **richest** wins, never merely the first. Both dialects now land everywhere: a keyword document is compiled to the `.pzn` the store speaks (`pzn-source.toPznSource`), its META description/author reaching the page index. Laws pinned by test — identity on every shipped `.pzn`/`.btml` byte-for-byte (CRLF checkouts included), idempotent, browser ≡ server: `test/pzn/extract.test.js` (35 cases), `smoke-bentml-extract` (in `test:smoke`), plus new cases in `smoke-pzn-paste`, `smoke-bentml`, `smoke-pzn-tools-route`, `smoke-pzn-pages-route`, `smoke-agent-bridge`, `smoke-copilot-tools`. `loose:` flags stay accepted, no longer needed. Side fix: `admin-bentml-ui.js` lost its two literal NUL bytes (git had treated it as binary). Package **2.20.0-alpha**. | Ben ("take only the BenTML, any time, any where") + Claude |
| v2.11-alpha | **הסכמת מבקרים — consent stops being a pixel feature (and the live bug that exposed).** Ben: *"do the consent banner then the booking."* I had recommended this as a *missing* module; reading the code first showed something worse and more specific than missing: the bar shipped in v1.79 **inside `pixels.js`** and rendered only when a BROWSER vendor was configured — yet `tz_consent` is read in three places, `conversions.js` among them. So a site sending **server-side** CAPI/GA4 with no browser pixel asked the visitor nothing, then refused every conversion with `no-consent`: **a gate with no door**, and precisely the defect docs/CRM-PORTABILITY had flagged as the one shipped bug the portability probe found. Consent now lives in `src/crm/consent.js` and renders whenever **anything** gates on it (`pixels.needsConsent` / `conversions.needsConsent`, asked lazily so the require graph stays acyclic) — or always, or never, the owner's switch, with the v1.79 `pixels.banner` key still honoured so existing installs don't change behaviour. **One writer:** `pixels.js` no longer keeps its own read/write/boot copy — it registers `tapuzConsent.onGrant(fire)` and fails **closed with a console warning** when no runtime is present, because a silent no-op is the bug that costs an afternoon. Consent became **withdrawable** (any `data-tz-consent="open"` element reopens the bar — a consent UI you cannot reopen is not consent), refusing stays exactly as easy as accepting, the bar is a labelled dialog with real buttons, and owner text/links are escaped and scheme-checked (`javascript:`, protocol-relative and plain-http dropped). **The live check earned its keep:** in a real browser, grant → withdraw → a late-registering pixel *still fired*, because `deny()` left `fired` true — withdrawal only took effect on the next page load. Fixed in two places (`run()` re-reads the stored answer; `deny()` forgets consent was in force) and pinned by tests that **execute the runtime** in a fake DOM instead of grepping it — proven load-bearing by reverting the fix and watching them fail. `smoke-consent` (30 checks, 6 of them the executed state machine); `smoke-crm-pixels` updated to assert the new contract — defer, don't decide — rather than the implementation that moved. Both suites green — **2,724 checks**. **Next: booking (בקשת תור).** | Ben ("do the consent banner") + Claude |
| v2.10-alpha | **Real customers + customer portal (username/password) — we control the CRM conversation.** Named/reachable people (name\|email\|phone) stay until the owner deletes them or **365 quiet days** since last seen (`updated_at` / portal login / touch); ghosts keep the short provisional→garbage path. Config: `crm.cards.namedQuietDays` (0 = never auto-erase named). Contact list filter **אמיתיים** (`?kind=real`) + last-seen on rows. **Customer portal** (`crm.portal`): off by default, self-register a second explicit flag; separate `tapuz_portal` session cookie (scrypt, never admin). Owner mints logins from the contact card anytime; public `/account/*` only when enabled — customer sees their info + history + deals. Schema `crm_portal_accounts` in subject erase. Privacy screen owns both retention + portal switches. `smoke-crm-portal`. Package **2.10.0-alpha**. Restaurant verticals later ride the same CRM + user instance. | Ben ("real customers… username and password… not default… control CMS/CRM") + Grok |
| v2.06-alpha | **The pixel, proven on the enemy CMS — and the hole the live test found.** Ben: *"push the plugin to the wp site and check live."* The v2.01 WordPress wrapper is now installed and running on the real Hostinger site, pointed at this CRM through a tunnel: the loader loads and a **single** POST to `/_tapuz/collect` answers 204 — one request, no preflight, because simple-request mode holds. The foreign pageview lands in `pageviews` tagged **`site_id=wp-hostinger`**, while `crm_events` does not even have a `site_id` column, so the v1.99 privacy spine is structural rather than conditional. Forged beacons fired from that same real WordPress page were refused: an unknown `site_id` recorded nothing, and a forged `identify({email})` produced no contact and no claim (claims are off for the site). **But the live test earned its keep:** a beacon that omitted `site_id` *entirely* was still accepted as NATIVE — so any foreign page could inject arbitrary paths into the owner's own analytics. Now the native stream is same-origin only: an `Origin` that does not match the request `Host` is refused unless it names a registered site; a request with **no** Origin (server-side callers, older clients) keeps working, so nothing first-party broke. Four checks pin it — foreign-no-site refused, same-origin records, no-Origin records, registered foreign unaffected — and the fix was re-proven live on the same WordPress page. Also learned the hard way, twice: WordPress's file-editor and settings screens swallow synthetic clicks (the form must be POSTed), and **LiteSpeed cache kept serving July’s pixel tag** until purged — the kind of thing no unit test can tell you. Both suites green — **2,646 checks**. | Ben ("check live") + Claude |
| v2.03-alpha | **Email sequences (drip) — HubSpot Sequences, self-hosted.** Multi-step follow-up over the same SMTP + consent law as campaigns: `crm_sequences` / steps / enrollments / sends; enroll only consent+email (no provisional/garbage); process due on daily housekeeping + admin "עבד עכשיו"; open pixel + List-Unsubscribe; unsubscribe cancels active drips. Admin **רצפי מייל** + per-contact enroll. `smoke-crm-sequences`. Package **2.03.0-alpha**. | Ben ("the other step") + Grok |
| v2.02-alpha | **Kanban + task email reminders — the sales desk closes.** Visual status board (`/admin/crm/board`): columns provisional→lead→active→customer→archived→garbage, drag-and-drop or select-to-move, open-task badges, status timeline notes. Task digests over the **same SMTP** as campaigns: daily (deduped by UTC day) overdue + due-today Hebrew mail, admin toggle + "שלח עכשיו", recipient default `notify.to`. Housekeeping rides `runRetention`. `smoke-crm-board` + `smoke-crm-task-reminders`. Package **2.02.0-alpha**. | Ben ("kanban and task reminders by email") + Grok |
| v2.01-alpha | **The CMS wrappers — pixel P4 complete.** Ben: *"do the wrappers."* `integrations/` lands in OUR repo: the **WordPress plugin** (`tapuziel-pixel/`) and the **Builder.io** paste README — both wrappers in the strict sense the plan defined: they only know where their CMS puts scripts and which settings to pass as `data-tz-pixel-*` attributes. Rebuilt from the lab with the four-findings discipline carried *into the wrapper layer*: **HTTPS-only base** (loopback excepted — the lab accepted plain http anywhere), the **registry's own site-id normalization** (and both settings screens tell the owner the slug must be registered in לקוחות → אתרים or the collector silently drops it), **nothing injected** until base AND site id are set, and the readme states plainly that `identify()` lands as a claim which never creates or merges a contact by itself. **`smoke-wrappers` (16 checks) makes the inject-only rule executable:** the plugin performs no HTTP of its own (no `wp_remote_*`/curl/streams), ships no client JS beyond the loader tag, never names the collect endpoint in code — only the loader knows it — and a **drift guard** parses both wrappers and the loader: a wrapper may only speak `data-tz-pixel-*` attributes `tz-pixel.js` actually reads, so the two can never diverge silently. The sites screen points at the wrappers. Verified live: the pointer renders on `/admin/crm/sites`. Both suites green — **2,613 checks**. *The pixel-embed plan (P0–P4) is complete end to end: registry → collector → loader → claims → wrappers.* | Ben ("do the wrappers") + Claude |
| v2.00-alpha | **CRM tasks — HubSpot's "what do I do next" loop, in Hebrew.** Cards, interests, segments and mail already capture *who* and *what they care about*; sales still loses when nobody remembers the next call. New `crm_tasks` on a contact (call / email / follow-up / meeting / other) with due date, open board (overdue + today + week + undated), complete/cancel, timeline notes, subject erase (CASCADE + PERSONAL_TABLES). Admin **משימות** + per-card quick-add. Not surveillance: only work the owner opens on a person. `smoke-crm-tasks`. Package **2.00.0-alpha**. | Ben ("progress vs HubSpot") + Grok |
| v1.99-alpha | **Pixel embed, rebuilt safely — identity claims, site registry, privacy spine.** The lab finding that must not ship: `identify({email})` → `upsertContact` from an unauthenticated cross-origin body (forge contacts, or attach arbitrary browsing to a real person). **Rebuild:** foreign `identify` lands in `crm_identity_claims` until admin approves (copilot stop-for-approval for identity); `crm_sites` registry (unknown/inactive/wrong-origin → silent 204); anonymous foreign traffic → `pageviews.site_id` only, never `crm_events`; snippets HTTPS-only outside localhost; loader `credentials:omit` + simple `text/plain` mode. Native collector also ignores body email. Flag `crm.pixelEmbed.enabled` default off. Admin: אתרים (פיקסל) + תביעות זהות. `smoke-pixel-embed` + privacy drift sees claims table. | Ben (plan findings) + Grok |
| v1.98-alpha | **Interest map — what progressive cards learned, made actionable.** Cards already turned page paths into `interest:*` tags and segments could filter them; the owner still had no **map of the whole site**. New `/admin/crm/interests`: counts per topic (total / with email / provisional / reachable), one-click live segment (+ mail only to people with email), campaign preselect via `?interest=`, contact list hot-topic pills + `?interest=` filter. Per-person interest editor (add/remove) separate from manual tags so a save never wipes path-learned topics. `listInterestStats` / `addInterest` / `removeInterest` in `cards.js`. Package **1.98.0-alpha**. `smoke-crm-interests` (21 checks) + route-map regen. | Ben ("iterate CRM / new Tapuz version") + Grok |
| v1.97-alpha | **Animation as the grok wanted — `animate=` is UNIVERSAL.** Ben pasted the game's marquee demo: *"we need to make animation as the grok wanted, this is a bug of our side."* It was — and the worst kind, the **invisible** kind: `animate` existed on heading and text only (values `fade\|rise`), so on every other module the attribute was *silently dropped* — validate skips undeclared props, compile ignores them, no repair note, no telemetry — and grok's `slide-up`/`zoom` were snapped to `none`, killing the animation quietly. Now every layer agrees, each through its **one existing universal seam** rather than forty hand-edits: the pzn registry injects `animate` (fade/rise/**zoom**) at `register()` and `attrsExtra` emits the class for every module; the BenTML keyword dictionary injects it into all 40 keywords with `applyChrome`/`uni()` carrying it both directions; the bridge's `baseOpts`/`finishBlock` cross it for every block type; `renderBlock`'s `extraClass` slot animates the whole JSON render path; the builder offers the select on **every** block (one canonical param, the heading/text copies replaced); and the published `/pzn-schema.json` + syntax dictionary now *teach* it as universal — every model reading the standard learns it. **Vocabulary drift is a dictionary fix, not an error:** `slide-up`→`rise`, `zoom-in`→`zoom`, marquee `lg`→`fast` alias via `PROP_ALIAS` (the telemetry bucket that means "fix the dictionary" — v1.85's decision rule, applied), while truly foreign values still snap to default. New `tapuz-zoom` keyframes in the theme SOURCE (scroll-driven `animation-timeline: view()` + fallback) and the standalone loader, with `prefers-reduced-motion` turning all three off. The cross-layer drift guard caught the BenTML dictionary lagging mid-build (40 failures) and the spec-sync guards forced the regeneration — three guards, all working as designed. Verified live: preview compiles `anim-fade`/`anim-zoom`/`anim-rise` on marquee/image/heading, repair aliases `slide-up`→`rise` on the running site. `smoke-animate` (19 checks, green first run). Both suites green — **2,539 checks**. | Ben ("I pasted the game") + Claude |
| v1.96-alpha | **SQLite forever — the .pzn database package IS the SQLite file.** Ben, killing the v1.91 JSON dump the same day it shipped: *"this cap is not funny to me… it's brickable, the fact it's in parts — we move to sqlite forever now, and I shall tell the grok to make the CRM accordingly."* Right call, and the game survives the pivot **better**: SQLite 3 is itself the open, archival-grade format (every language reads it), and it has a header field built for exactly this — the database now stamps **`application_id = 'TPUZ'`**, so a Tapuziel file *identifies itself*, and every snapshot inherits the mark. One format for shelf, export and transfer: `.pzn` export = a fresh `VACUUM INTO` snapshot (whole-file, single-artifact, nothing chunked that can half-land); **upload-restore streams the request body straight to disk** — never through a body parser, so the 12MB cap is simply gone. The new refusal, pinned by test: a **healthy-but-foreign** sqlite file is refused (`not_a_tapuz_db`) — emptying our tables for a file that fills none of them would be a site wipe; while a *pre-stamp* shelf snapshot (application_id 0, our schema) still restores — the guard must not orphan old backups. The JSON import path was **removed**, not deprecated — two mechanisms for the same dangerous job is one too many. Contract for grok's CRM appended to docs/CRM-PORTABILITY.md: `tapuz-db` = SQLite 3 + `'TPUZ'`, importers must verify `quick_check` AND identity before touching their own data. HTTP-proven in `smoke-storage-route` (streamed snapshot restores, garbage 400s, no session refused); `smoke-db-restore` rewritten (12 checks). Both suites green — **2,472 checks**. | Ben ("we move to sqlite forever now") + Claude |
| v1.95-alpha | **Harden SMTP — timeouts, daily cap, injection guard, campaign gate.** Campaigns and lead alerts share `notify.js`. `isSmtpReady` gates campaign send. Connection/greeting/socket timeouts; CR/LF stripped; daily `maxPerDay`; Hebrew cookbook `docs/SMTP-PRODUCTION.md`. | Ben + Grok |
| v1.94-alpha | **Send campaigns to a live segment — relevant mail, not blasts.** `crm_campaigns.segment_id`; audience at send time (consent + email). | Ben + Grok |
| v1.93-alpha | **Cards in the admin — interests, segments, Hebrew voice.** Status tiles, interest pills, privacy knobs, interest segment rules. | Ben + Grok |
| v1.92-alpha | **Progressive customer cards — stitch, enrich, forget.** First-party `tz_v` cookie; quiet → garbage → erase for unreachable ghosts. No raw IP. | Ben + Grok |
| v1.91-alpha | **Restore, and the database as a `.pzn` — for the sake of the game.** Ben: *"restore from backup button and export, can we make it .pzn? for the sake of the game?"* The game is that ".pzn is our RPM" — everything portable is a *documented package anyone can implement*, so the export is NOT a binary with a costume: `tapuz-db` joins `tapuz-site` as a package family — the whole database, table-by-table, as readable JSON with format/version/appVersion guards, downloadable as `‎.pzn` and importable back (paste-JSON, the site-package pattern). The binary snapshots stay on the shelf for crash-recovery; the package is for portability. **Restore is the most dangerous button in the admin, so the engineering is in the refusals:** a corrupt snapshot is `quick_check`ed and refused *before* the live data is touched; import is **all-or-nothing** — one poisoned row rolls back the entire transaction, proven; both paths funnel into one replace-everything core that ATTACHes/inserts by **column intersection**, so an older snapshot or package restores into a newer schema (missing columns take defaults, unknown tables are skipped), then `initialize()` tops up migrations, **FTS is rebuilt** (the v1.82 lesson: never trust an index you didn't rebuild) and `foreign_key_check` comes back clean. Live restore with **no restart** — the snapshot is ATTACHed into the open handle, never swapped under it. Every restore/import takes a safety snapshot of the moment before, and the shelf-edge got its own test: restoring the *oldest* backup must survive the safety-backup's pruning (the target is copied aside first — found by design review, pinned by test). `smoke-db-restore` (16 checks). Both suites green — **2,471 checks**. | Ben ("can we make it .pzn?") + Claude |
| v1.90-alpha | **The premium-db pass — SQLite 3, run like it's production.** Ben: *"sqlite 3 as we talked, we should have premium db."* The headline is the fix that was silently false for thirteen versions: **the schema has declared FOREIGN KEYs since the CRM landed (v1.77), but SQLite enforces them per-connection and defaults OFF** — they were documentation. Now `foreign_keys = ON`: an orphan child row is *refused* (proven — inserting a `crm_events` row pointing at contact 999999 throws) and `ON DELETE CASCADE` actually cascades (proven — deleting a contact takes their events with it). Write-time-only enforcement, so existing databases upgrade without drama; the full 2,455-check suite ran with enforcement on and surfaced **zero** violations — the explicit-delete discipline in `subject.js` was already keeping the books clean. Alongside: `busy_timeout = 5000` (cross-process access waits instead of throwing SQLITE_BUSY — the route smokes spawn a second server on the same file) and `synchronous = NORMAL` (the WAL-safe fsync setting). **The backup shelf:** `VACUUM INTO` snapshots — WAL-consistent, compacted, plain SQLite files that open anywhere — created automatically once a day (stale-checked, so a day of dev restarts is one backup, not a shelf of the same morning), keep-7 with same-second collision suffixes, plus a "גבה עכשיו" button and per-file download. **Surfaced on the Storage screen** — the "your content is real files on disk" pitch extended to the database itself: path, size (+WAL), the pragmas *actually in effect*, on-demand `quick_check`, and the shelf. `smoke-db-premium` (13 checks, green first run, including opening a snapshot read-only and quick_checking it). Both suites green — **2,455 checks**. | Ben ("premium db") + Claude |
| v1.89-alpha | **WhatsApp W4 — inbound as CRM material. The integration is COMPLETE (W0–W4).** Ben: *"W4."* The phase that makes the channel worth having: an inbound message from an unknown number becomes a **person**. The webhook resolves the sender's profile name from the `contacts[]` sidecar and `ensureContact` creates the contact through the CRM's **own upsert** — so a later web-form submission with the same phone *merges into this person* instead of making a twin, and a WhatsApp conversation and a web enquiry sit on one history. The design refusals, each pinned by test: an EXISTING contact is never renamed by a WhatsApp display name (anyone can set theirs to anything — `ensureContact` doesn't touch a person it found, by construction); **consent is not implied** — messaging a business is service contact, so the WA-born person has `consent 0`, no wa opt-in, and the marketing gate still refuses them; a status callback never creates anybody; a nameless profile still yields a phone-only contact (phone is identity enough). "Ledger first, contact later" got its *later*: every orphaned pre-W4 row for the phone is adopted on first contact. Erasure proven end-to-end on a WA-born person — contact, messages, timeline, all gone. Admin: 👤 link from each ledger row to the person's card. `smoke-wa-inbound` (12 checks, green first run). Both suites green — **2,442 checks**. *The lab→rebuild arc closes: 5 phases, 4 findings fixed (hardcoded host, DB-derived tier, erasure/export, consent separation), zero new dependencies.* **Next: the premium-DB pass (v1.90).** | Ben ("W4") + Claude |
| v1.88-alpha | **WhatsApp W3 — sending. Money leaves the building, so the tests are mostly about when it must not.** Ben: *"W3."* `wa-send.js` is the ONLY module with a network path to Meta, and it holds **no URL of its own** — the destination comes exclusively from `endpointFor()` on the hardcoded Graph host, and the test greps the module to keep it that way. **W1's pure gate runs before the transport on every call**, and each refusal is proven to fire with ZERO network: the suite injects a recording transport and asserts the record stayed empty for free-form-outside-window, marketing-without-opt-in, invalid phone, empty message, and the tier ceiling (`messaging_limit_reached` from US at 250, transport never fires — Meta never gets the chance to block the number instead). The transport is `conversions.js` all over again: timeout-bounded via `AbortSignal.timeout`, resolves to a small error object, **never throws** — proven against a real never-answering local server cut in <3s, a refused connection, a 200-with-no-wamid (a failure, not a phantom success), and a transport returning garbage. **The two books rules that make the money honest:** a FAILED call sent nothing, so its audit row keeps the why but gets `outside_csw = 0` and no pricing — *a failure can neither burn tier quota nor look billable* — and `spendEstimate()` counts **delivered** billable rows only, because Meta charges at delivery, which is exactly the moment the W2 webhook refines pricing into the row. The owner sees it the way the CS chat taught us: **in money, not message counts** — the 💰 card shows this-month and total in USD (IL per-category rates, labeled an estimate), and the send form prices each template category right in the option label. Refusals come back to the admin as *explanations*, not codes: "חלון השירות סגור — השתמשו בתבנית", "תבנית שיווקית דורשת הסכמה", each pointing at the card that fixes it. Template variable components deferred, named. `smoke-wa-send` (25 checks, green first run). Both suites green — **2,430 checks**. **Next: W4, inbound as CRM material — contact creation from the WhatsApp profile.** | Ben ("W3") + Claude |
| v1.87-alpha | **WhatsApp W2 — the HMAC-gated webhook.** Ben: *"w2 next."* The one route in the CMS that must see RAW request bytes: `X-Hub-Signature-256` is an HMAC over the exact bytes Meta sent, so `src/routes/wa-webhook.js` mounts **before every body parser** in server.js — and because reordering fails *silently* (signatures just stop matching, Meta retries and eventually unsubscribes), the test proves the order two ways: a source-position assertion AND a behavioral proof — a valid signature over a **non-canonically spaced** body must be accepted, which a parsed-and-reserialized body can never satisfy. The verification itself ported from the lab essentially as-is (raw body, length check before `timingSafeEqual`, fail closed on missing secret/header), with one addition: a non-Buffer body — the mount-order mistake at runtime — is a clean `401`, not a 500. Refusal order pinned by test: bad signature → `401` *before the body is parsed*, unparseable JSON with a valid signature → `400`, foreign `object` → `200` and ignored. **The events do W1's bookkeeping:** a `delivered` status refines the ledger row — PMP pricing lands at delivery (billable + category from Meta, `delivered_at` stamped), `failed` records *why* in the error column; an inbound message goes through `recordMessage`, so the window opens and a known contact's timeline gains the message for free; and since **Meta retries until it sees a 2xx**, `recordMessage` learned wamid dedup — a retry is one row, not a UNIQUE-index crash. GET handshake echoes `hub.challenge` only for the right verify token; channel flag off → the endpoint is a `404`; per-IP flood cap sits in front of the HMAC. **Also shipped, because W0 built the credential store but no UI:** the connection card on `/admin/crm/whatsapp` — channel flag (which finally has a switch), Phone Number ID / WABA ID / API version / tier, and the three secrets with keep-on-empty semantics + the webhook URL card showing exactly what to paste at Meta, with the honest note that the Graph host is a constant *by design*. `smoke-wa-webhook` (27 checks, green first run). Both suites green — **2,405 checks**. **Next: W3, sending — money leaves the building.** | Ben ("w2 next") + Claude |
| v1.86-alpha | **WhatsApp W1 — the ledger and the gate, still incapable of sending.** Ben: *"then start W1."* Three `crm_wa_*` tables (opt-ins with grant/revoke *history* — three facts, not one flag; the 24h customer-service window, opened by inbound; a full message ledger with pricing columns) and `wa-ledger.js`, which is the bookkeeping and the DECISION with no network path — a test greps the module for `fetch(` to keep it that way. **`decideSend` stays a pure gate** (DB reads only — proven by counting rows before and after refusals), refusing in order, cheapest first: `invalid_phone` → `marketing_opt_in_required` (fail closed, enforced by us before Meta) → `csw_closed_use_template` (free-form outside the window never leaves the building) → `messaging_limit_reached`. **The deliberate change from the lab:** the tier counter is *derived from the database* — distinct outside-window recipients, rolling 24h, straight off the ledger — not held in a process `Map` that forgets on restart. The test proves the difference the only honest way: fill the ledger to a 250 ceiling, **delete the module from the require cache**, and assert the fresh instance still counts 250. Also pinned: an already-counted recipient is not double-blocked (unique recipients, not messages), old sends age out of the rolling window, and `TIER_BANANAS` falls back to `TIER_250` rather than becoming unlimited. **PMP pricing** classified at decide time: service free, utility free in-window and billable outside, marketing/auth always billable, with `updateStatus` ready to refine at delivery (W2). **Identity is one person:** `normalizeToWaId` delegates to `contacts.normalizePhone` first, so `050-…`/`+972-…`/`9725…` are one wa-id, an inbound message from a known phone lands on that person's CRM timeline through the seam, and an unknown phone is still ledgered (contact later). **Privacy in the same commit, per the plan:** `crm_wa_messages` joined `PERSONAL_TABLES` (the drift guard sees it), and because opt-ins and windows are keyed by *phone* — invisible to the guard — erasure reaches them explicitly through the contact's wa-id and the export gains a WhatsApp section; both pinned by test. Admin screen at `/admin/crm/whatsapp`: honest "not connected" notice, tier bar fed by the ledger, manual marketing opt-in/revoke with a provenance note. Verified live: grant through the real form → the gate flips to allow. `smoke-wa-ledger` (30 checks, green first run). Both suites green — **2,378 checks**. **Next: W2, the HMAC-gated webhook.** | Ben ("start W1") + Claude |
| v1.85-alpha | **Before redesigning the language, measure it.** Ben asked whether BenTML deserves a rework ("we didn't put any thought on the syntax, it's just plain text") — and the repair layer's own alias tables argue the syntax is fine and the *vocabulary* is what models fight. But that was an argument, and arguments lose to numbers, so: **repair telemetry**. Every AI-authored document that passes the pipeline now records what — if anything — had to be fixed: a `CLEAN` row when nothing did (**the denominator** — without it "PROP_ALIAS fired 40 times" can't be told apart from a catastrophe or a rounding error), one `(code, element)` row per fix otherwise, aggregated by count. Instrumented at the two places repairs are *applied* — `pznSourceToBlocks` (every paste flow) and the agent bridge's create-from-source (the extension's publish path, the richest stream of model output) — and deliberately NOT at the two preview endpoints, which would double-count the same document. **Privacy by schema:** the table holds a code, an element name, a count and a timestamp — it physically cannot store content, and the test dumps every row and asserts none of the document's text is present. Telemetry can never break what it measures: the recorder swallows everything, and the test proves it by dropping the stats table mid-run and parsing anyway. Surfaced as a 🩺 card on `/admin/inject` with the reading key ("PROP_ALIAS/ALIAS = בעיית מילון · QUARANTINE = מודול שחסר · E_* = בעיית תחביר אמיתית") plus `GET /admin/api/pzn/repair-stats`. Verified live end-to-end: a `url=` document → `PROP_ALIAS·button` in the DB, the endpoint and the card; stats then zeroed so the dev site collects real data only. `smoke-repair-stats` (11 checks); the ROUTE-MAP drift guard caught the new endpoint before the suite went green, as designed. Both suites green — **2,348 checks**. *The decision rule, for when data accumulates: vocabulary codes dominate → fix the dictionary, not the syntax; QUARANTINE dominates → build the missing modules; structural E_* codes dominate → then and only then talk redesign.* | Ben ("do the repair instrumentation first… we cannot build on broken base") + Claude |
| v1.84-alpha | **WhatsApp W0 — and a measured answer to "can another CMS use this?"** Ben: *"start, we check the systems works LIVE… on a completely different system, as I plan this CRM to be adjusted by community."* **W0** lands the switch and the credentials and nothing that can spend: `crm.whatsapp.enabled` under the existing CRM flag, secrets in gitignored `config/whatsapp.json` with the `ai.js` redaction pattern (readiness + last-4, `undefined` keeps / `''` clears). Its whole purpose is **finding 1 from the review**: the lab made the Graph host a *setting* that accepted any `https://` value — a config key deciding where a long-lived access token is sent, the exact pattern refused in CRM phase 3b. Here it is a **constant**; `graphBase`/`graphHost` in a patch are dropped on the floor, `apiVersion` is whitelisted because a value like `../../evil` would climb out of the path, and a test asserts the file contains no other `https://` literal. 22 checks. **Then the live cross-system check, which is the interesting half.** Three systems at once: Tapuziel on :3000, the CRM lab on :3100, and — the real test — a **foreign page** on :3200, forty lines of plain Node with no Express and not one line of our code, told to behave like a third-party CMS. Result: **the CRM is write-portable today and read-portable never.** The beacon, form capture and email pixel all work from a foreign origin with zero shared code — verified server-side, not just "the browser accepted it": a contact appeared with `source='foreign-cms'`, a name, an email and a timeline. Blocked: every cross-origin *read* (we publish no CORS at all), and **attribution** — `mode:'no-cors'` delivers the request but omits cookies, and `credentials:'include'` is refused before it leaves, so `tz_v` never arrives. Isolating that mechanism mattered: the blocker is **not** the cookie's `SameSite`, it is the missing CORS policy — two different failures, one missing feature. It currently fails in the *safe* direction (no consent cookie → server-side conversions refuse) but **silently**, which is the one real defect the exercise found. Written up as [docs/CRM-PORTABILITY.md](CRM-PORTABILITY.md) with the recommendation: publish the **ingest contract** as the community standard (it already works, costs nothing), treat cross-origin attribution as a separate flagged feature that requires HTTPS, keep reads same-origin forever and point integrators at agent tokens, and surface the `no-consent` refusal count so "nobody consented" is distinguishable from "my integration cannot see consent". Both suites green — **2,338 checks**. | Ben ("do the check meanwhile") + Claude |
| v1.83-alpha | **The held-back chat ships, with the bill as its design — phase 4.** Ben: *"do the cs chat with the budget cap"*. This is the module the lab review flagged as the one blocker and I refused to ship for six versions, because it is the **only CRM surface where an anonymous visitor spends the owner's money** — and per-IP rate limiting is no answer when the caller has many IPs. **The cap is the feature.** `dailyMessageCap` is enforced by an **atomic reserve-before-you-spend** step: the check and the increment are one transaction, so two simultaneous visitors cannot both take the last slot — a read-then-decide cap leaks under exactly the load that matters, and the test proves it by racing 50 requests at a 10-slot budget and asserting exactly 10 get through. Over the cap **the model is never called**; the visitor gets a human sentence and an invitation to leave details. A failed or empty call **refunds** its slot — an error is not a sale. Around that: a per-session cap so one chat can't eat the day, a question-length cap, a bounded history replay so per-call cost can't grow with conversation length, a tighter per-IP window than the pixels, and hard ceilings (2000/day, 100/session) the owner **cannot configure past** — a mis-typed `100000` must not become a five-figure invoice. **Least power:** it calls plain `generate`, never the tool-running `converse`, so it returns text and cannot read pages, write drafts, change settings or send anything; its prompt is told what it may discuss, told to refuse rather than invent prices or promises, and told that instructions inside a visitor's message are *questions*; the widget writes every remote string with `textContent`, so a reply is never parsed as markup; and `/crm/cs/v1/config` exposes only the greeting, because revealing the remaining budget would tell an abuser when to strike. **The owner sees money, not counts** — a usage bar plus the worst-case daily cost in dollars, with refusals counted. **And the phase-5 drift guard immediately earned its keep:** the new `crm_cs_conversations.contact_id` made `smoke-crm-privacy` fail on the spot, which surfaced something better than a missing entry — that FK is `ON DELETE SET NULL`, right for merging a contact away but **wrong for an erasure request**, since nulling the link leaves a transcript full of the person's own words (quite possibly the phone number they typed). Erasure now deletes support chats explicitly. Also fixed: toggling the chat or the pixels now **republishes the site**, because both inject at render time and an owner who flips a switch and sees no change concludes it's broken. `smoke-crm-cs` (41 checks, stubbed model) + `smoke-crm-cs-route` (24 HTTP checks). Verified live: with no provider configured a question got a polite refusal that leaked nothing **and consumed no budget**; with the budget filled the endpoint refused without calling the model and the admin bar showed 3/3 in red with 2 refusals. Both suites green — **2,316 checks**. | Ben ("do the cs chat with the budget cap") + Claude |
| v1.82-alpha | **Hygiene, and a bug that only existed on the upgrade path — phase 5 (the CRM plan is complete).** Ben: *"phase 5"*. Three closing items. **Retention:** `crm.retention.eventDays` (0 = keep everything, the default — quietly deleting an owner's history would be worse than growth), enforced on boot, once a day (`unref()`ed, so housekeeping can never hold the process open) and the moment the policy is saved; events anchored to a real submission are **never** pruned, whatever the number says. **Full-text search:** `LIKE '%term%'` can't use an index and scans every row, so an external-content **FTS5** table with `unicode61` replaces it, kept in step by insert/update/delete triggers — Hebrew prefix search works (`דנ` → דנה כהן), user input is reduced to quoted prefix tokens so FTS5's own operators (`"`, `*`, `-`, `:`, `NEAR`, `OR`) can neither throw nor silently change the query, and a SQLite without FTS5 degrades to LIKE rather than failing to boot. **Subject rights:** per-contact `export.json` — the person's whole record, submissions included, as a file the owner can actually send — and an erase that **verifies itself**: it counts what existed, deletes, then re-checks every personal table and reports leftovers instead of trusting the cascade (submissions survive by default as business records; deleting them too is an explicit tick-box). **The guard that keeps it true:** the test reads the LIVE SCHEMA for every `crm_*` table with a `contact_id`/`from_id`/`to_id` column and fails if the erase module doesn't list it — so a future phase can't add personal data and quietly leave it behind when someone asks to be forgotten. **And the bug.** `SELECT COUNT(*) FROM crm_contacts_fts` does *not* count the index: on an external-content table that query is answered from the **content** table. So the "have we indexed everything?" guard compared `crm_contacts` to itself, always agreed, and the rebuild never ran — leaving the index empty on precisely the databases that needed it, while a fresh install looked perfect because the triggers fill it as rows arrive. Every unit test passed; searching the live dev site returned nothing. The check now reads the index's own `_docsize` shadow table, and a regression test reproduces a pre-upgrade database — verified to fail without the fix (*"index has 0 docs for 3 contacts"*). `smoke-crm-privacy` (36 checks). Verified live: Hebrew prefix search returns both matches, `contact-1.json` downloads with the timeline and submission inside, the privacy screen reports "5 events, 1 anchored — never pruned". Both suites green, zero console errors. **The CRM integration plan is done** — the lab's one blocker (a public endpoint spending the owner's LLM budget) remains deliberately unshipped. | Ben ("phase 5") + Claude |
| v1.81-alpha | **Campaigns, and the three things email law asks of you — phase 3c (phase 3 complete).** Ben: *"phase 3c"*. Send to a mailing list over SMTP, then see who opened and who clicked. The features were the easy half; these three are the reason the module is careful. **(1) Consent is the recipient list, not the list.** Only contacts with consent AND an address are sent to — and the skips are *counted and shown*, so the owner learns why their audience is smaller than their list rather than wondering ("1 יקבלו · 1 ברשימה לא נתנו הסכמה"). **(2) Every message carries an unsubscribe** — in the body and in the RFC 8058 `List-Unsubscribe` / `List-Unsubscribe-Post` headers that mail clients turn into a real one-click button (the POST form returns a bare 200, as the RFC wants). Unsubscribing clears consent, which also stops phase-3b conversion reporting for that person: one refusal, honoured everywhere. **(3) Click tracking cannot become an open redirect.** Links are extracted at send time and **frozen** onto the campaign; a tracked URL carries an *index* into that list, never a destination — so a request cannot name where it wants to go, and `?u=https://evil.example` on a tracked link is simply ignored. Each recipient gets one unguessable token that serves the open pixel, every tracked link and the unsubscribe URL, so no address or id ever appears in a URL; the open pixel returns a **byte-identical** response for real and fake tokens, so it cannot be used to discover which tokens exist; a sent campaign is frozen against edits because it *is* the record of what went out; delivery runs in the background with a gap between messages so a slow SMTP server never holds a request open; a contact's own name is escaped into the body so their data cannot inject markup; and SMTP stays owned by `notify.js` — campaigns never touch nodemailer. **Tested:** `smoke-crm-campaigns` (40 checks, fake transport, nothing leaves the machine) + `smoke-crm-track-route` (19 HTTP checks on the public endpoints, including four open-redirect attempts). *One real defect found en route: `parseInt('0x0', 10)` returns `0`, so a malformed link index silently resolved to a valid link — not exploitable, since the destination still came from the frozen list, but sloppy; the index must now be a clean digit string.* Both suites green, zero console errors. **Phase 3 is complete; next is phase 5 hygiene** — event retention, FTS5 contact search, subject export/delete. | Ben ("phase 3c") + Claude |
| v1.80-alpha | **Conversions that survive an ad-blocker — phase 3b.** Ben: *"phase 3b"*. Meta CAPI + GA4 Measurement Protocol. A form submission mints one `eventId`: the server reports the conversion with it, the redirect carries it to `/form-sent`, and the thank-you page fires the browser pixel with the **same** id — so the vendor collapses two reports into one. Better measurement, not double counting. **The design problem was consent.** The browser's answer lived only in `localStorage`, which the server cannot read — so "refuse" would have stopped the pixel while the server kept reporting. That is not consent, it is theatre. The loader now mirrors the decision into a cookie, and the server refuses to send for anyone who did not grant; DNT/GPC outranks even a granted consent. **The rules that make this safe to ship:** PII is hashed to Meta's spec and the test verifies it against digests computed **independently with `openssl`** rather than with the code under test; ip and user-agent go raw *because Meta requires them so for matching* and nothing else does; the vendor hosts are **hardcoded constants**, so no configuration key can redirect an access token to a new destination; every call is `AbortSignal.timeout`-bounded and fire-and-forget; secrets are never echoed back (readiness + last-4 only), and an empty secret field means *keep*, never *clear*. `smoke-crm-conversions` (30 checks) runs fully **offline** — every path is either gated or unconfigured, so CI never touches a real vendor. Verified live: the access token never appears in the page after saving, the thank-you page carries exactly the event id the redirect issued, a forged `?e=` renders nothing, and a submission that really did call Meta with a bogus token returned in **94 ms** with the lead saved — the visitor never waits on a vendor. Both suites green. **Next:** 3c — campaigns over SMTP with open/click tracking. | Ben ("phase 3b") + Claude |
| v1.79-alpha | **Pixels — and the CSP that caught them. Phase 3a.** Ben: *"phase 3"*. Meta / Google Ads / TikTok / LinkedIn, opt-in and **default off**, with **consent required by default**: until the visitor agrees the vendor code is not live markup at all — it waits as a JSON string the loader injects on consent, and DNT/GPC outranks consent entirely. Ids are charset-filtered per vendor and length-capped, a built-in RTL consent bar ships, and a site with its own banner suppresses ours and drives `window.tapuzConsent.grant()/.deny()`. **The story of this version is what live testing caught that unit tests could not.** The obvious loader `eval`s a snippet string — and the CMS's own CSP withholds `'unsafe-eval'`, so it was **silently blocked in production while appearing to work in DevTools**, which is exempt from CSP. The first "success" was the tooling fooling me. Digging revealed two real defects: the loader now injects a `<script>` **element** (covered by the existing `'unsafe-inline'`), and the CSP is widened by **exactly the origins the configured vendors need** — Meta alone adds `connect.facebook.net` + `www.facebook.com`; an unconfigured vendor adds nothing; `'unsafe-eval'` is never granted. The silent `catch` that hid the failure now logs one warning per failing vendor — a tracker that fails silently costs hours. **Acceptance met, including the plan's own bar:** `smoke-crm-pixels` (25 checks) renders a real page with pixels off and asserts it is **byte-identical** to the pre-feature output. Verified live end-to-end: consent bar → grant → Facebook's script genuinely loads, zero CSP violations; switching pixels off restores the original CSP header character for character. Both suites green. **Next:** 3b — Meta CAPI + GA4 MP sharing an `event_id` with the browser pixel. | Ben ("phase 3") + Claude |
| v1.78-alpha | **The CRM starts filling itself — phase 2: passive capture.** Ben: *"phase 2"*. Two call sites, three lines each: a form submission now resolves the **person** behind it, and a linked visit joins that person's timeline. Both call the guarded seam and both run *after* the CMS's own write, so neither can cost a lead or a pageview. **The interesting problem was attribution, and the obvious solution was refused.** `pageviews` re-salts its visitor hash every day and destroys the old salt — it *cannot* follow anyone across days, by design. Reusing it for the CRM would have been three lines and would have quietly broken that promise. Instead attribution got its own narrower link (`crm_visitors`): a random token in a first-party **HttpOnly** cookie, minted **only** when someone voluntarily identifies themselves by sending a form, and **only** while the CRM is on. An anonymous visitor stays anonymous forever; a person who wrote to you gets a timeline — which is what they already expected when they typed their phone number into your contact form. A cookie already bound to someone else (a shared computer) is never reassigned — the new person earns a fresh token rather than inheriting a stranger's history. **The promise, tested for real:** `smoke-crm-capture` (24 checks) drops `crm_contacts` out from under the *running* server and proves a form submission still succeeds and is still saved — plus the beacon survives it and analytics keeps recording. Also pinned: DNT/GPC wins even for a known person, a forged token resolves to nobody, and with the flag off there is no contact, no cookie and no event. All five hot-path suites (`smoke-form`, `smoke-forms-inbox`, `smoke-analytics`, `smoke-conversions`, `smoke-form-capture-route`) pass **unchanged**. Verified live end-to-end on the dev site: a real form created the person with `054-123-4567` normalized to `0541234567`, the browser was linked, two beacons landed on the timeline, and the score moved 40 → 48 as the browsing accrued. *(En route: a first live attempt showed nothing — the bot filter was correctly rejecting curl's User-Agent. The code was right; the test was wrong.)* Both suites green. **Next:** phase 3 — outbound (pixels, CAPI, campaigns), all opt-in default off. | Ben ("phase 2") + Claude |
| v1.77-alpha | **The CRM comes home — phase 1: the spine.** Ben: *"start"* — the go-ahead on [docs/CRM-INTEGRATION.md](CRM-INTEGRATION.md) after the lab review. Rebuilt our way, never merged: 7 additive `crm_` tables, `src/crm/` (contacts · Customer · events · relations · segments · lists) and admin screens at `/admin/crm/*`, ~1,350 lines with 67 new checks. **The architecture is the point.** `src/crm/index.js` is a **seam** — the only module the CMS may call — and every hook on it holds two invariants proven by test: with `config.crm.enabled` off it returns null and touches nothing (a fresh site is byte-for-byte the site it was), and it **never throws into a hot path** (the test breaks `upsertContact` and asserts a form submission still survives). `Customer` is the OOP datasheet the rest reads: lazy per-facet caching so it is safe inside a render loop, and a score that ships its own *reasons* — an unexplainable score is one nobody tunes. **Identity is where the care went:** email-then-phone resolution with `+972-50-…` folding to `050-…` (one person, two spellings); merging is non-destructive, so a later form that omits the name can never erase the name (tags accumulate, consent latches) while an *admin* edit can clear, because a human meant it; and an address already owned by someone else resolves to its owner rather than being stolen. Segment rules are admin JSON compiled to SQL — the one place injection could live — so field names come from a fixed whitelist and every value is bound, with a hostile-value test to prove it. A disabled CRM **offers to enable itself rather than 404ing**, and `/admin/crm/segments` is declared before `/:id` so the literal path is not swallowed. Retention (`pruneOlderThan`) ships on day one, keeping any event that anchors a record elsewhere. Layering respects what exists: `form_submissions` stays the record of a *submission*, `crm_contacts` becomes the record of a *person* — contacts sit **above** the inbox, and the v1.00–v1.12 lead pipeline is untouched. Verified live (nav group renders, enable flow works, empty state explains itself, zero console errors); both suites green. **Next:** phase 2 wires the seam into form capture and pageviews. | Ben ("start") + Claude |
| v1.76-alpha | **The editor window gets its Word tools.** Ben: "we open text into text box, there are no tools in text box like word that I requested so we should worry about that more." The v1.75 window opened bare; now the non-code variant carries a **tools row — B / I / `</>` / 🔗** (+ Ctrl+B/I/K) that wraps the selection in **BenTML inline marks** (`@B{}`, `@I{}`, `@CODE{}`, `@LINK(url:""){}`), the language `renderInlineMarks` on the server has rendered safely all along — real formatting on the published page, zero new attack surface, and the same marks the side-panel chips and the AI dictionary already speak. **The Word half is seeing it:** a live rendered preview sits under the textarea (type `@B{שלום}` — see **שלום**), a new client `renderMarksPreview` mirrors the server renderer (everything escaped; `@LINK` renders as a styled span because the canvas must never navigate), and the canvas text preview + live-update path now render marks instead of showing them as literal code. The **HTML code editor keeps NO word options — by design** (Ben: code only, the BenTML way). Verified live: select "שלום" → B → `@B{שלום}` in the textarea and `<strong>שלום</strong>` in the preview; link button wraps and previews styled; cancel discards; zero console errors, zero literal marks on canvas. +9 checks in `smoke-drop-targets`. Both suites green. Also from Ben's message: **`tapuziel-crm-lab` is under construction** (repo live on GitHub: contacts, segments, pixels, Meta CAPI, GA MP, social gadget, AI agent API — "prototype; rebuild into tapuz"), and quiet-between-comments confirmed as the working mode. | Ben ("no tools in text box like word… worry about that more") + Claude |
| v1.75-alpha | **The screen stops shaking; text gets a real editor window.** Ben: "still page builder seeks the location and shakes the screen… no reason to be shaking all the time" — and his own design: "double click on text open text box word like… only the regular text box (that we made already capable of html) [gets] the html editor without word options, like we done with BenTML." The remaining shake had a second engine v1.74 didn't touch: a SINGLE click on any text field ran `startInlineEdit` — canvas rebuild + `el.focus()`, and focus() makes the browser scroll-seek. Plus three `behavior:'smooth'` animated scrolls (layers jump, symbol insert, dblclick-to-panel). All gone: **single click now only selects** (the calm class-move path); **double click opens a centered editor window** — a fixed overlay, so opening it *cannot* scroll the page: a clean document-like textbox for text fields, and for the **HTML block** a raw dark-mono **code editor with no word options** (the BenTML-source spirit). Save = Ctrl+Enter (history + dirty + scroll-pinned repaint), Esc/backdrop cancels. Deliberate navigation (layers row, symbol insert) still centers the block — **instantly** (`behavior:'auto'`), which is exactly Ben's "it can get to the part of the page, center it". `startInlineEdit` (117 lines, with its caret-from-point and keystroke-mirror machinery — and the known keystroke-race) deleted whole: the modal replaces the entire in-place-contenteditable model. Verified live: click → selected, not editable, no modal; dblclick → modal; save → data updated, modal closed, no jump; test edit undone from the draft. +6 checks in `smoke-drop-targets`; `smoke-layers`' scroll assertion updated to pin the INSTANT jump. Both suites green; zero console errors. | Ben ("I want it gone… I found a solution!") + Claude |
| v1.74-alpha | **The click stops jumping — selection becomes a class, not a rebuild.** Ben: "page builder 'jumps' when you press on text which can make one annoyed I want it gone." Root cause: every click ran `selectBlock → renderCanvas()`, which wiped `canvas.innerHTML` — the document collapsed for a frame, the browser clamped the scroll, the rebuild restored the content but not the reader's place. Two-layer fix: **(1)** `selectBlock` now MOVES the `.selected` class between the two block elements (verified live: same DOM node identity after selection — zero rebuild — with properties, layers highlight and toolbox mode all still following; exactly one `.selected` at any time), falling back to a full render only when the target isn't in the DOM yet (fresh drop, source swap); **(2)** `renderCanvas` itself now pins `scrollTop` and restores it on both exit paths — guarding every remaining rebuild (drops, undo, deselect, source swaps). Also: the builder's own topbar still said 🍊 Tapuz (the builder shell renders its own brand row, missed by v1.73's sweep) → Tapuziel; two leftover "מכולות" plurals (empty-canvas hint, columns tool hint) → מיכלים. +5 checks in `smoke-drop-targets`. Both suites green; zero console errors. Working tree committed clean as the **skeleton snapshot** — Ben is handing the grok lab this skeleton to prototype the CRM on (a separate build; ideas return here rebuilt, never copied). | Ben ("I want it gone… commit it, I will give the grok the skeleton") + Claude |
| v1.73-alpha | **The product signs its name: Tapuziel.** Ben's naming decision: "Tapuziel will be the final product name I think, Tapuz is taken by many organizations, we are to avoid that." Every surface a USER reads now says **Tapuziel**: the browser-tab suffix (`• Tapuziel`), the sidebar brand, the login screen (🍊 Tapuziel), the new-site defaults (`config.js` title + description), the published-site footer credit (**נבנה עם Tapuziel** — regenerated into the static build), the site-chrome settings label, the site-package error messages, and the server boot line. Repo name, CLI (`tapuz`), and internal identifiers stay — they're plumbing, not brand. Verified live: login, tab titles and sidebar all read Tapuziel; a page-wide scan finds zero bare "Tapuz". Also recorded from the same message: v1.72's מיכל rename is approved ("the מכולה is great"); the goal-stance sharpened — we are the builder.io replacer with SUPPORT as the differentiator (their users wait months for features), HubSpot is CRM-with-sites and below our integration bar; and the grok lab's current mission is **prototyping the CRM system** for later integration — ideas in the lab, rebuilt our way in Tapuziel, never copied. Both suites green. | Ben ("Tapuziel will be the final product name") + Claude |
| v1.72-alpha | **Fine-tuning night: the canvas learns one container language.** Ben: "the layout to not be clustered but remain effective… pixel perfect tuning for the canvas as it approaches old containers weird and not correct." Diagnosis by DOM measurement (screenshots unavailable): the toolbox categories were ALREADY all-folded-by-default (v0.75's calm palette — nothing to change there), but the canvas spoke **two container dialects**: columns wore the container language (dashed blue frame, tint, 8px breathing room) while the flat-blocks containers (card, parallax, section…) wore `.preview-card` — the ARTICLE-card look: solid grey hairline, radius 9, zero padding, and `overflow:hidden` — which is exactly why "as it approaches" a drag looked wrong: the clipped box CUT the drop highlights at its edge. Now `.preview-card.is-container` overrides to the shared language (dashed `#c7d2fe`, radius `--r-md`, 8px pad, `overflow:visible`, min-height 60) — measured live afterwards: both container types report identical radius/pad/overflow. **And the word:** the builder badges said **מכולה** — a *shipping* container — while the README, the tour and the properties panel say מיכל. Every user-visible מכולה (badges, hints, the "+ טקסט" button, inject-pack tag, dictionary legend) became מיכל, dictionary artifacts regenerated (drift guard green), and the head no longer stutters "מיכל מיכל" when the type itself is the מיכל tool (verified live: "מיכל", "מיכל טור 1 40%"). +3 checks in `smoke-drop-targets`. Both suites green; zero console errors. | Ben ("fine tunning for tonight") + Claude |
| v1.71-alpha | **The copilot steps into the builder and learns what "this" means.** Ben: "we need copilot to grasp the situation of being the helper in CMS and do what he's told — get access to pages and edit them accordingly, adding text to a selected item, help in the page builder." The v1.59 copilot could already read and (gated) write pages — but it lived on its own screen and had no idea what the owner was looking at. Now: **(1) a 🤖 drawer inside the page builder** (`admin-copilot-panel.js`, dark-desk styled, fixed above the save-bar) riding the SAME server chat — key on the server, same tools, same approve-gate, everything still lands as a DRAFT. **(2) Situational context on every turn:** the drawer sends `{page, selected:{id,type,text}}`; the chat route appends a capped "המצב עכשיו" section to the briefing — the open page's slug ("when asked to edit *the page*, it's this one") and the selected module with its current text ("when asked to add text to *the selected item*, it's this exact block — not another page, not another block"). The builder grew a tiny `_getSelected` export so the drawer can read the selection. **(3) Truth-before-edit:** the canvas autosaves silently before each ask, so the copilot's `read_page` sees what the owner sees; on an approved write the builder reloads to show the new draft — nothing of the owner's is lost, nothing of the copilot's is invisible. Context strings are length-capped server-side (no prompt-stuffing through a block's text). Verified live with a stubbed model: selection (hero) exposed, context `{page:"home", selected:{type:"hero", text:"WhiteNo1se"}}` sent, reply bubble + approve card render, zero console errors. +9 checks in `smoke-copilot-route`; the v1.59 gate suite (33 checks) still green untouched — the drawer adds a surface, not a bypass. Both suites green. | Ben ("adding text to a selected item, help in the page builder") + Claude |
| v1.70-alpha | **The cut you can grab, the column you can add, the provider you can trust.** Three items from Ben's work order. **(1) The movable column cut** — Ben asked for mouse-resizable columns… which turned out to ALREADY EXIST (`bindColumnResize`, live % labels, history, verified working live: 2fr:3fr → 54:46 in one synthetic drag). A feature the owner asks for while it's shipped is a *discoverability* bug: the 10px handle was invisible until hover. The cut line is now always faintly present (opacity .3 at rest) and grew a visible grip dot (⋮) at mid-height — hover/drag brings both to full. **(2) "+ טור"** — the genuinely missing half: a row could only ever be born with two columns. A round + button on the row's inline-end edge grows it (2→3→4, capped), the new column joining with an equal share; drag the cuts to rebalance; history + dirty + properties refresh ride along. Verified live: 2 panes 54:46 → 3 panes 36:31:33 with 2 handles. (En route: my synthetic-drag testing autosaved into Ben's home draft — caught it, restored `ratio="2:3"` exactly; published copy was never touched.) **(3) Provider radios with honest readiness** — Ben: "options that are not working should be greyed out… make a radio to choose between them." The copilot's `<select>` became a radio list where each row carries its truth: **מקומי · ללא מפתח** (always ready, full color — LM Studio stays), **מוגדר ✓** (saved provider with key), **דורש מפתח** (toned to .55 — but still clickable, because choosing a provider is exactly how you get to configure it; the checked row is never faded via `:has(input:checked)`). Verified live: Claude checked/needs-key, ChatGPT faded, local full-color; picking local reveals the base-URL + free-model fields; zero console errors. +7 static checks in `smoke-copilot-route`, +7 in `smoke-drop-targets`. Both suites green. | Ben ("I want the separation to be moveable by the mouse… make a radio") + Claude |
| v1.69-alpha | **Injection dies; the ecosystem is born.** Ben, after live-testing: "we cannot inject, we draw on their sites… I think it's a rejection… we back off injection and provide user with the tools — we do not inject anymore, user's does, that's how we will roll." So: **(1) injection is retired on ALL four hosts** — `copyFirst: true` on every provider; the ①/② buttons copy to the clipboard and the user pastes; the extension only ever READS the reply (composer/send selectors kept for diagnostics only). One flag flip re-enables a future host that welcomes automation. **(2) The prompt builder** — Ben: "the prompt builder should be at a press of a button on the pagebuilder." New 🧠 פרומפט AI button in the builder's canvas header opens a modal: your brief + two checkboxes (include the CURRENT page source so the AI edits instead of rebuilding — with an explicit "אל תבנו מאפס" instruction; lite pack for free-plan message gates) → one click composes roleplay pack + page source + brief and copies it (verified live: 33.7K chars, pack+brief+source all present). Pure client module (`admin-prompt-builder.js`), talks only to our own admin APIs, carries no token. **(3) The ecosystem loop closes:** the extension popup drops every "הזרק" promise ("אנחנו מעתיקים, אתם מדביקים"), gains a 🧠 link back to the CMS prompt builder — tools flow CMS→clipboard→chat→extension→CMS. **(4)** the "Grokin" label left the agent-bridge screen title (internal lore stays in code comments; customer copy stays clean). New `smoke-prompt-builder` (13 checks) pins the button, the wiring, the no-token invariant, all-providers-copyFirst, and the de-injected popup copy. Both suites green. | Ben ("we should back off injection and provide user with the tools") + Claude |
| v1.68-alpha | **The user test becomes a protocol, not an intention.** The goal's "easy to understand" claim has exactly one pending validation: a real user (the designated tester — Ben's sister) building with the walkthrough. That test kept living as "worth running sometime"; new `docs/USER-TEST-1.md` turns it into a 30-minute script Ben can run today: iron rules for the facilitator (don't help — every "רגע, איפה ה..." IS the finding; don't pre-explain — the system explaining itself is what's being tested), eight ordered tasks each secretly measuring one claim (tour auto-start, drag-not-click, inline text edit, drop-by-intent into a container, the 📱 preview v1.66 just unhid, draft≠publish, revision restore, 🧭 relaunch), a fill-as-you-go result form (per task: alone/hint/stuck + one verbatim quote — her words outrank any analysis), and a **pre-committed success bar** so the result can't be rationalized after the fact: 6/8 alone with no >1-minute stall passes; any hint rewrites the matching tour step; a stall on tasks 2 or 4 (drag / drop-by-intent) fails the "easy to understand" core and outranks all feature work. Results paste into the doc itself and convert to ROADMAP lines — every "hint" is a bug in the guidance, never in the tester. Docs-only; both suites green. | The goal's own words ("easy to understand… walks you through") + Claude |
| v1.67-alpha | **The named competitor gets a real chapter in the scoreboard.** The goal's first sentence names builder.io as competition #1, yet `docs/COMPETITIVE.md` gave it one buried row while WordPress/Drupal/Camilyo each had columns. Benchmarked against what builder.io actually ships in mid-2026 (researched, sourced): **they are a visual development platform for dev teams** — Fusion (AI agent → visual canvas → real code → PRs against your design system), Figma-to-code, headless CMS, A/B testing, priced by seats + metered agent credits (~$19/user/mo + $25/500 credits). The section is honest in both directions: **where they win and we don't chase** (Fusion-class codegen into an existing repo, Figma-to-code, A/B testing, enterprise headless delivery — their customer HAS a codebase; ours IS the site) and **the wedge where we win** (complete self-hosted site+CMS+CRM vs a platform needing devs; keyless AI = no meter ever vs credit-metered AI; a builder that walks an owner through vs a Git review loop; RTL first-class; `.pzn` open standard + full export vs proprietary cloud; zero-JS static pages vs SDK runtime; your SQLite vs their telemetry). Ends with a **draft positioning sentence for Ben** ("we don't out-Fusion them; we make their category unnecessary for the SMB") and three new hunt-list items ONLY where the benchmark exposed real capability gaps: A/B split testing (the serve-variant half of our existing conversion tracking), Figma import (likely via the decompiler road), and design tokens surfaced in the builder's properties panel (pick "primary", not a hex). Docs-only; both suites green. | The goal's own words ("our first competition is builder.io") + Claude |
| v1.66-alpha | **The builder starts walking you through — and the walk immediately catches two layout bugs.** The goal's founding sentence promises a builder "that walks you through things that are not 'just read the label'" — the wizard covered setup, but the builder itself had no guided mode. Now it does: `public/admin-builder-tour.js`, a self-contained spotlight walkthrough over the REAL UI (no screenshots, no demo page) that auto-starts on the first visit and is one click away forever (🧭 סיור in the tabs row). Five steps, each teaching the ACTION: the toolbox is *dragged from*, not clicked; the canvas is *the page itself* with drop-by-intent (עליון/תחתון = לפני/אחרי · מרכז מיכל = פנימה · קצה = טורים); settings *come to you* on selection; 📱 shows the real mobile render; and טיוטה ≠ פרסום with revisions as the safety net. Skip/finish persist (`localStorage`, private-mode safe), a missing target skips its step not the tour, and off-viewport elements are not spotlightable — pointing at empty space is worse than skipping. **Walking the tour live at 1280px immediately exposed two real bugs the static suite couldn't see:** (1) the builder grid used bare `1fr`, so a grid item's implicit min-width (its CONTENT) let one wide block inflate the canvas track to 932px inside a 705px slot — and in this RTL document the overflow shoved the whole toolbox off the viewport's left edge (measured at x=−239). Every builder grid now clamps with `minmax(0, 1fr)`; the same fix rode into the ≤1100px layouts. (2) `#btn-responsive` — the v0.90 device preview — was **unreachable on desktop**: hidden with the mobile drawer buttons via their shared `.page-props-btn` class, on exactly the screen where "how does this look on a phone" gets asked. It now owns its display; the pill styles moved to base so it renders styled at any width (`#btn-page-props` stays mobile-only — the desktop rail already shows page settings when nothing is selected). New `smoke-builder-tour` (34 checks) pins all of it: every step selector must exist in the builder template (a renamed id fails loudly, not silently), the copy must teach drag/drop-by-intent/click-to-edit/draft≠publish, the flag guards, AND the two CSS bugs. Verified live end-to-end: five aligned spotlights, on-screen cards, clean finish, no auto-start on return, relaunch + skip clean, responsive overlay opens on desktop, zero console errors. Both suites green. | The goal's own words ("walks you through") + Claude |
| v1.65-alpha | **The bridge stops pretending it can type into ChatGPT.** Ben, testing the extension: "it won't inject to gemini or chatgpt… I remember now why we made this tokenizer — the token is for OUR product." Two truths untangled: **(1) the token architecture was already right** — the agent token is minted in `/admin/agent`, lives ONLY in the extension's background worker, and authenticates the extension to *your own CMS* (`/agent/v1/*`); ChatGPT/Gemini never see a credential, only text (the roleplay pack in, the `.pzn` reply out). Nothing to back off from — the design he remembered is the design that exists. **(2) The injection half was genuinely broken on two of four hosts:** ChatGPT and Gemini ignore (or freeze on) synthetic composer writes — the v0.56 native-setter fix and the mutation throttle bought time, but the sites won. So those providers gain `copyFirst`: the ①/② buttons now **copy the pack to the clipboard** and say "הדביקו (Ctrl+V) ושלחו" instead of attempting a DOM write that spooks the page — the reliable paste flow becomes the *stated* flow, not the fallback. Claude/Grok keep direct inject. The valuable half of the bridge — watching the reply, extracting the `.pzn`, publishing with the worker-held token — is injection-free and unchanged on all four. Button labels, statuses (gender-agreed: משחק הועתק / בנייה הועתקה) and the diagnostics line all follow the host's real delivery mode, so the panel never claims a capability the site denies. `smoke-extension` +4 checks: chatgpt+gemini are copy-first, claude+grok are not, `inject()` branches to the clipboard BEFORE touching the composer, and copy-first still surfaces reply-reader health. Suite green. | Ben ("the token is for OUR product") + Claude |
| v1.64-alpha | **The showcase comes home, and an anchor becomes an identity.** (Logged retroactively — shipped in `18c8245` without a log row.) Ben: "check the reaction of the pagebuilder… where is the beautiful homepage we built to showcase to the user?" The builder was proven CLEAN by a live drag matrix (zero lost blocks, state===DOM at every step); what looked like "the builder removed containers" was the home page having lost its showcase content in the DB incident. The showcase homepage was rebuilt, and human block ids (`hero1`, `tools`, `faq`) now render as real DOM anchors — `anchorId()` in the renderer emits a HUMAN id as the DOM id (as the `.pzn` compiler always did) while machine ids stay out of the HTML. `exportAll` pruned three stale `public/*.html` ghosts. Verified live; both suites green (1908 smoke, 110 pzn). | Ben + Claude |
| v1.63-alpha | **The smooth swap reaches every link, not just the sidebar.** (Logged retroactively — shipped in `cdb724b` without a log row.) Ben: "extend the smooth swap to in-content links too." v1.62 caught the sidebar; one document listener now evaluates EVERY anchor, with `swappableUrl()` the single judge of what swaps in place (topbar brand, hub cards, cross-links, analytics tabs) and what falls through to the browser (off-site, `/admin/api/*`, file downloads, the shell-less builder/wizard). | Ben + Claude |
| v1.62-alpha | **The admin stops jumping — content swaps, the shell stays.** Ben: "site jumps when pressing on admin menu… ajax is an old form of refreshing something and not annoying the other part of the site; we upgraded far above." Every sidebar click was a full document reload that tore down and rebuilt the fixed rail identically — the eye reads that as a jump/flash. `admin-nav.js` (progressive enhancement) intercepts a sidebar left-click, fetches the destination, and replaces **only `#admin-main`**; the sidebar and topbar never repaint. `adminNav()` now opens `<main id="admin-main">` and `layout()` closes it when the sentinel is present, so the wrapper stays balanced and the shell sits outside it — a bare page (auth) or a shell-less one (builder, wizard) gets no `<main>` and no nav layer. On swap it updates the title, the `--admin-accent`, the section title, the per-screen `.topbar-actions`, and moves the sidebar's `.active` — and **re-executes the swapped scripts** (scripts inserted via innerHTML never run), so `admin-theme.js` rebuilds its look cards and `admin-menus.js` its editor exactly as on a full load. Back/forward works via `popstate`. Everything is a fallback: a fetch error, a login-redirect, or any page without `#admin-main` hands straight off to a real navigation, so it can never strand you. **Two real bugs found by live-probing, not by the suite:** (1) re-executing page scripts would re-arm `admin-media-picker.js`'s document-level listener — the v1.50 "two pickers" bug via navigation — fixed with a `window.__tapuzMediaPickerInit` guard, verified to open exactly one overlay after three loads; (2) with the shell otherwise dead-steady, the RTL fixed rail still slid ~15px whenever the vertical scrollbar toggled between pages of different heights — frozen with `scrollbar-gutter: stable`, measured 14.8px → 0. New `smoke-admin-nav` (20 checks) pins the balance, the fallbacks and the picker guard. Both suites green; verified live end-to-end (no reload, sidebar element identical, scripts re-ran, back/forward, one overlay, zero shift). | Ben ("ajax… we upgraded far above") + Claude |
| v1.61-alpha | **Inbox and import get their section lines.** The two screens the v1.60 audit left as "cards but no heads". **`/import`** rebuilt its three vendor sections onto the shared components: the live WordPress importer is a `.lead-card` with an accent head and a green `.pill` reading "פעיל"; the four coming-soon vendors (Builder.io, HubSpot, Camilyo, Mobeart) sit under a new "מערכות נוספות" `.side-title` divider as dimmed `.is-soon` cards, each with a "בקרוב" pill — a real roadmap of what's landing, not four grey boxes. Its bare `<h3>` + hand-rolled pill spans and the success message's hardcoded `#166534` all became shared classes. **`/inbox`** kept its stat tiles and `.rec` lead records but had nothing dividing the pipeline summary from the leads themselves; a new `.section-bar` — a heading with a live count on one side and the status filter + CSV export on the other — draws that line, and the count updates with the status filter so "📥 הפניות · חדשים 3" reads at a glance. New shared `.section-bar` for titling a LIST (not a card) with its controls on the far edge. Both suites green (110 pzn units + ~100 smoke scripts); the audit now shows zero flat admin screens. | Ben ("do the inbox and import sections too") + Claude |
| v1.60-alpha | **The five screens the design system never reached.** An audit scored every admin screen for the things that make a page read as sections — cards, section headings, status pills — and five came back FLAT: menus, seo, media-library, categories, sitemap. Three of them still carried private `<style>` blocks with their own hardcoded greys, which is exactly why they were left behind: they were never wired to the tokens, so v1.54's design system passed them by. **`/seo`** was the clearest case — four unrelated settings (title pattern, description, og:image, base URL) stacked inside one undifferentiated card. Now three cards, each with a `.card-head` that says what it is for: "איך הדף נקרא בגוגל" · "איך הדף נראה בשיתוף" · "כתובת האתר — הבסיס לכל השאר". **`/menus`** panels dropped their private white-box rule for the shared `.card` surface; chips, location selects and child rows follow the accent instead of a fixed `#e2e8f0`, and the selects gained focus rings. **`/sitemap`** section titles became accent-coloured `.card-head`s and its four private badge classes joined the shared `.pill` family. **`/categories`** got a real head with a live count, an accent-underlined column strip, and focus rings on its inputs. **`/media-library`** gained a head carrying the breadcrumb plus a live count — a folder holding forty files and one holding none used to look identical until you scrolled. New shared `.side-title` for cutting a single card into groups without stacking more cards. One smoke assertion updated rather than deleted: `smoke-sitemap-route` matched the old `sm-pub` / `sm-draft` class names, and now matches the LABEL a reader sees — it still tests which status shows, without pinning the stylesheet that paints it. Both suites green. | Ben ("keep going on the coloring and sectioning") + Claude |
| v1.59-alpha | **The copilot stops being blind — and stops short of acting alone.** Ben, on learning the connected model could only emit text: *"how could it be if we didn't built the copilot basics even"*. He was right. It could write a NEW page from nothing (the owner clicked a button) but could not see a single page of the site it was helping with, so "make the hero blue" was impossible and every request rebuilt from scratch. It now has four tools — `list_pages`, `read_page`, `create_page`, `edit_page` — declared once as JSON Schema and wrapped for either provider envelope (Anthropic `input_schema`, OpenAI `function.parameters`). **The rule that keeps it honest:** READ tools run on their own — a model that must beg permission to *look* will guess instead — while WRITE tools never do. The loop stops, the server keeps the conversation state under a single-use id, and the browser receives only that id plus one human sentence ("לערוך את הדף about (נשמר כטיוטה)") behind an approve/refuse gate. A refusal is fed back to the model as a tool result, so it offers something else instead of repeating itself. **What is deliberately absent is as much of the design as what is present:** no publish, no delete, no theme or settings writes. Every write lands in a DRAFT that the existing revision history already makes reversible, and going live stays a human act. This DOES reverse the guarantee proven one commit earlier ("the reply is a string; nothing executes it") — Ben chose that knowingly, which makes the gate the entire safety story, so `smoke-copilot-tools` (33 checks) exists to attack it: nothing is written while a proposal is pending, a refusal writes nothing, a pending id is single-use, an invented id is rejected, a runaway tool loop is cut off at six hops, an invented tool name is answered as a tool error rather than executed, and six named dangerous tools are asserted NOT to exist. Verified end-to-end against a scripted model: it listed pages, read real `.pzn` source, proposed an edit, the page did not move while waiting, and after approval the DRAFT changed while the live page did not. | Ben ("Real tool-calling — let it act") + Claude |
| v1.58-alpha | **The connected copilot is told who it actually is, and the extension gives back a secret it was never entitled to keep.** Ben: "the llm that connects the api key will get the explaination of the whole scenario of being the personal helper of mine in the cms building benTML." It was getting the **wrong briefing entirely**: `/admin/api/ai/chat` sent `buildRoleplayPack`, which is written for a stranger's chat tab — it opens with *"paste this pack into a NEW chat"*, insists *"there are no API keys"*, and casts the model as a contestant in a construction game facing a *"player"*. Every one of those lines is false for a copilot that arrived through the owner's own key and is already inside the CMS. New `buildCopilotBriefing()`: same dictionary, same real media manifest (they must never diverge), opposite framing — who it works for, that it is standing inside the CMS connected by the owner's private key, **the name of the site it is working on**, what becomes of its output (a complete `.pzn` lights up a "צור דף" button that turns the reply into a real draft — so a half-finished document isn't "almost", it's a button that never lights up), and how to behave (ask when unsure, Hebrew/RTL by default, real copy not lorem ipsum, never invent an image path). En route, a shared helper leaked the game's vocabulary into the briefing — `mediaInventoryMarkdown` told the copilot to *"ask the player"* — so it now takes the whole prepositional phrase rather than a noun, because Hebrew glues its preposition to the word and "בעל/ת האתר" is already definite: the naive `מה` + noun produced the double-definite "מהבעל/ת האתר". **Cleanup:** v0.73 stored the user's LLM key in the extension's service worker; v0.85 moved key-chat into the CMS and deleted the code that used it — but the KEY ITSELF stayed in `chrome.storage` on every browser that ever set one, with nothing left to read or clear it. The three `byok_*` entries are gone from `KEYS`, `getConfig` and `setConfig`, and the worker now **sweeps the orphaned entries once on startup**: dead code that held a secret has to take the secret with it. **QA:** `smoke-roleplay` +16 checks pinning the two personas apart (the briefing must name its owner, its site and the one-click outcome; it must NOT contain משחק / שחקן / מהלך מנצח / "אין מפתחות API"; the BYOT pack must keep its own voice untouched; and the route must send the briefing, not the pack). Verified end-to-end against a stand-in local server: 26,429 chars of the new briefing arrive, with no auth header — correct for a keyless local model. | Ben + Claude |
| v1.57-alpha | **A model on your own machine becomes a first-class provider.** Ben asked what the LLM layer actually is — "the connection is for the lmstudio to work on it as a headpoint or I give it to public llms also? this part of the build seems to grow out of my sight". Answer, after tracing it: it was **public-only, hardcoded to two hosts**. `ALLOWED_API_HOSTS = {api.anthropic.com, api.openai.com}` plus an https requirement — so LM Studio failed *both* tests (http, and not in the set). That allowlist is the v0.73 BYOK review's fix (a provider descriptor must never be able to redirect a user's key), so it could not simply be dropped. The rule is now two rules: **public → https and only the hosts we ship; loopback → any port, http fine** — a key sent to 127.0.0.1 never leaves the machine, so plaintext costs nothing and there is no third party to leak to. Endpoint policy moved into `providers.js` as the single implementation (`isLoopbackHost` / `endpointAllowed` / `resolveLocalEndpoint`), so the server and the `/agent/v1/providers` table can't disagree. A `local` provider joins the table — OpenAI chat shape (what LM Studio, Ollama, vLLM and llama.cpp all serve), `keyOptional`, `openModel` (a local runtime serves whatever it has loaded, so the model name is free text, and no empty `Authorization:` header is sent). The address is the user's own, re-validated per call and **refused at save time** if it is not loopback. The copilot's settings card grows a base-URL row that appears only for the local provider, and a connection failure now names the address it tried and says to start LM Studio. **The safety rests on checking the hostname AFTER URL parsing** — the parser normalises `127.1`, `2130706433` and `[0:0:0:0:0:0:0:1]` to real loopback literals while `localhost.evil.com`, `127.0.0.1.evil.com` and `http://user@evil.com` keep their true host — so new `smoke-local-llm` (43 checks) pins exactly that, including 14 refusals (suffix tricks, fragment, query, userinfo, `0.0.0.0`, LAN ranges, `::ffff:` mapping, `file:`, `javascript:`). Also recorded for the record: **the extension has no direct-call path to share** — `extension/llm.js` was deliberately deleted in v0.85 ("the key comes home to the CMS"), leaving the extension as the keyless BYOT tier. Server-side is therefore the only call site consistent with that decision, and the one that works for loopback anyway. Both suites green. | Ben ("I am willing to make it happen, we always looking to improve and diverse") + Claude |
| v1.56-alpha | **The site is born knowing how to introduce itself.** Ben: "the base page should present a colorful joyful presentation of the utilities available in the CMS… welcome messages all over the containers… make it colorful, full of joy… right after making the admin user the first page is full of examples." A new install used to land on a hero and one paragraph — correct, and completely silent about what the CMS can do. Now `runSetup` seeds the home page from a new **showcase** template: **24 blocks, 17 distinct modules**, every one a working instance rather than a screenshot — hero, moving text (marquee), features, stats, a `section` container with children *inside* it, a two-column split, a 6-image gallery, carousel, card grid, ticker, timestamped newspop, FAQ, testimonial, CTA and a closing banner. Each section carries its own "this is what you're looking at" line, so the page teaches the toolbox while being built with it, and the copy says outright that everything is deletable. **The second menu is now real:** menus were always named lists with a location pointing at one, but with only ever a single list in the table the design was invisible — nothing to switch between. Setup seeds an `explore` menu beside `main`, and the showcase's nav section explains that the header takes the main one until you reassign the location at עיצוב ← תפריטים. **Art:** eight hand-written SVG tiles (3.7 KB total, `public/demo/`) — abstract so they need no translation and nobody mistakes them for real content; `seedDemoAssets()` copies them into the *site's* public/ at setup, because the package copy would leave a TAPUZ_ROOT install or a static export copied elsewhere greeting its first visitor with broken images. **Ordering decided deliberately:** `basic` stays first in TEMPLATES because that entry is both the new-page picker's pre-checked option and the unknown-key fallback — a 24-block tour must not land on every page added later. runSetup asks for the showcase by name, and `body.homeTemplate: 'basic'` opts back into the quiet two-block seed. **QA:** `smoke-templates` grown from 22 to 38 checks — the showcase must carry 12+ distinct modules and each named one, every container must ship with children (a container that arrives empty teaches nothing), nested children must be real registry blocks with unique ids, and **every referenced image must exist on disk**. The walker checks `Array.isArray(data.columns)` rather than the key, because `columns` is a *count* on features/stats/gallery and a *list* only on the columns container — the first draft crashed on exactly that. Verified live: 24 blocks render, 6 gallery images, 13 images all serving, nav with 4 links, 5217px of page. Both suites green (110 pzn units + ~100 smoke scripts). | Ben ("think its our next move now") + Claude |
| v1.55-alpha | **Second pass: the design system reaches the rest of the admin, and the last hand-rolled card is gone.** v1.54 built the vocabulary and converted the screens that had exact-match patterns; this finishes the job on the ones that didn't. Inline styles **651 → 253** (a 61% cut overall; 497 → 253 in this pass), and **hand-rolled `background:#fff;border:1px solid #e2e8f0` cards: 3 → 0** — every card surface in the admin is now either `.card` or a tokenised class, so a token change re-tints all of them. New components for what the remaining screens actually needed: `.rec` (a disclosure record — the inbox lead rows, with `.unread` state and `.kv` field pairs), `.file-grid`/`.file-tile`/`.icon-btn` (the media bank and storage browser), `.stg-card`/`.stg-sec`/`.disk-path` (storage, hoisted out of its private `<style>` block), `.meter` (the analytics device split), `.tbl` adoption with `.tbl-empty`, `.banner` (accent-driven hero strip — the storage banner's hardcoded teal→blue gradient now follows its family), `.auth-card`/`.auth-stage` (the login screen — first thing anyone sees — now a floating card on a warm mesh with the gradient brand), `.check-row`, `.inline-field`, `.split-2`, `.seg`, `.code-box`, `.btn.sm/.xs/.block`, `.notice.slim` (one-line status strips). Hand-converted the four worst offenders — analytics 66→15, inbox 49→16, copilot 33→25, auth-screens 16→**0** — and swept the rest mechanically. Killed the last three local style constants (`const card` in analytics, `cardCss`/`inputCss` in site-chrome); the site-chrome one was also injected into client-side JS, so the dynamically-built inputs now take `.input` via `classList.add` (the old `className =` assignment would have clobbered it). Two corrections found by auditing the sweep's own output rather than trusting it: a checkbox label had been mapped to `.sub-head` (a heading — wrong, it got `.check-row`), and `width:100%` had to be scoped off the shared card-input rule. What remains inline is now genuine one-offs — a single `margin-top`, `display:none`, a dynamic `--c` — not re-typed components. One smoke assertion updated, not deleted: `smoke-analytics-route` matched `/7 <span/` on the range stat; it now reads the number tag-agnostically, so it still tests the range behaviour without pinning the design system's tag choice. Both suites green (110 pzn units + ~100 smoke scripts); 19 screens re-checked, 342 components live, builder desk pixel-identical (canvas 309,728 / toolbox 73,220). | Ben ("do the second pass on the remaining screens") + Claude |
| v1.54-alpha | **The admin gets a design system, and colour finally reaches it.** Ben: "color none existance in the admin, no separate nothing… we need to color it up modern, making the whole system look like a 2026 site." Measured before touching anything: **87% of the painted area was white or greyscale**, and the "colour" that existed was 7% washes that read as white. The cause was structural, not cosmetic — the admin screens were built from **651 inline `style=""` strings**, the same card, label, input and pill re-typed per route with its own hardcoded hex (`analytics.js` and `integrations.js` carried 66 each; two routes had gone as far as declaring local `const card = 'background:#fff;…'`). There was nothing to theme, so nothing could carry colour and no surface separated from any other. Built the missing layer in `admin.css`: `.card` / `.card-head` / `.notice` (4 tones) / `.pill` (5 tones) / `.row-item` / `.stat` / `.hub-card` / `.tbl` / `.field-label` / `.input` / `.lead` / `.empty-state` + layout helpers — every one inheriting `--accent`, so a screen's family hue from `ADMIN_NAV_GROUPS` now reaches its whole surface. Depth is carried by the section hue rather than grey (`--elev-1/2` are accent-mixed shadows), the workspace ground is a real tinted mesh instead of white, and buttons got a lit edge instead of a flat fill. Then swept the routes: **138 inline styles across 16 files replaced with components**, plus the dashboard's private 40-line `<style>` block hoisted into the shared system (`.stat` and `.hub-card` gained a `--c`/`--g` override so a tile can still carry its own meaning-colour). **Seven distinct accents** are now live across the 19 admin screens — pages blue, dashboard orange, design purple, growth green, media teal, AI magenta, system slate — so screens read as different places. Scoping checked both ways: input *looks* are shared inside `.card` but `width:100%` stays opt-in via `.input` (a blanket rule stretched inline checkboxes), and the builder's dark desk defines its own full `background` shorthand so it is untouched — verified pixel-identical geometry. Both suites green (110 pzn units + ~100 smoke scripts). | Ben (found it looking at the thing: "we refining") + Claude |
| v1.53-alpha | **The canvas accepts a drop where you aim it, and the menu stands up.** Ben, testing as a human: "the pagebuilder doesn't put the containers in place when you place them on live page builder — this is a critical issue." Confirmed by probing the live canvas on a grid of points and recording which ones called `preventDefault` on `dragover`: **32% of the canvas rejected every drop, and the rejected 32% was the inside of every block.** A `dragover` that doesn't `preventDefault` means the browser refuses the drop outright — so the only surfaces that ever worked were the 10px hairline slots between blocks, the 22px split strips at the edges, and an empty container's "גרור לכאן" placeholder. Release a container over an existing module — where anyone naturally aims — and nothing happened at all. Two independent causes: (1) nothing bound drop handling to a block's body; (2) `bindListSurface` bailed on `e.target.closest('.canvas-block, …')`, which for a NESTED list *always* matched the container's own block element — so a container's padding was dead from the moment it held one child, and the placeholder that would have caught the drop had already been replaced by that child. Fix: `bindBlockBody` makes every block body live and resolves the pointer into an intent — top/bottom band of a container → before/after it, middle of a container → INSIDE at the gap nearest the cursor (the pane under the cursor for columns), anything else → nearest half; it runs on the bubble phase and yields to any more specific target via `defaultPrevented`, so the innermost target still wins and the split strips still split. `claimedWithin` scopes the list-surface guard to the list's own subtree, and `nearestIndexIn` (shared by both paths) means a drop on padding lands where it was aimed instead of always appending. Three new indicators (`.drop-before`/`.drop-after`/`.drop-into`) show the landing spot before you let go. **Menu:** 19 tools across 7 families were packed into a 1320×102 horizontal strip that wrapped into two rows and read as one blur — now a vertical rail on the inline-start edge, one family per row, all 19 above the fold at 900px height, with an icon-only rail ≤1180px and a swipeable top strip ≤720px. The builder desk and the pre-login screens never render the rail, so they are untouched. **QA:** new `smoke-drop-targets` (22 checks) — verified to FAIL when either cause is reintroduced, not just to pass today. Both suites green (110 pzn units + ~100 smoke scripts); all 19 admin screens re-checked for the rail, the active marker and horizontal overflow. | Ben (found it testing as a human: "container placement should be flexible as much as possible") + Claude |
| v1.52-alpha | **The static export learns to FORGET — delete/rename/unpublish finally reach the published site, and 36 ghost pages stop serving.** Found by human-actor QA, not by the suites: create → publish → delete a page in the admin, and its URL kept answering 200 forever. Root cause: `deletePage`/`updatePage` synced the DB, revisions, `.pzn` store and the homepage crown through every transition — but never the exported HTML in `public/`, and `exportAll` (which runs on EVERY publish) never pruned, so the one layer a visitor actually sees was the one layer that never forgot. The repo itself carried the proof: 35 tracked orphan exports, including paired rename-duplicates (`arkyv`/`arkyvn`, `mamry`/`mamrym`), some ghost-serving pages deleted long ago (`privacy-policy.html`, `about.html`). Fix at the right layers: `export.removePageHtml()` (shared filename hardening with the writer so the two can never disagree; hands `/` to the next crowned home when the removed page owned it), called from `deletePage` and from `updatePage` on rename + unpublish — a renamed LIVE page moves its export immediately, no second publish click; and `exportAll` now reconciles: prunes top-level `*.html` not in the DB's published set (keyed to the DB list, not the pass's writes, so a failed render keeps its last good file). En route the QA rename call exposed a second real bug: `updatePage` merged `{...existing, ...updates}` and the save route spells keys with `undefined` values, so a partial update (slug-only — what the agent bridge sends) clobbered `title` into a NOT NULL crash; partial updates now merge, never clobber. **QA:** `smoke-publish-live` grown +5 checks (rename: old address 404s, new address serves published content without re-publishing; delete: no orphaned export). Both suites green (110 pzn units + ~99 smoke scripts). The 36 on-disk ghosts removed in this commit. Also repaired this file's own drift: header stamped v1.46 and the log stopped at v1.47 while the repo shipped v1.51 — the four missing rows below are reconstructed from their commits. | Ben (direction: "build all branches into one, test as a human, bug-free CMS worth while") + Claude |
| v1.51-alpha | **A raw-HTML block could break the page builder out of its own `<script>`.** The v1.49 html module's content is embedded in the builder page's inline script as JSON; a block containing `</script>` terminated the whole builder. Escaped the embedding in `admin-ui`/`pages-builder` (plus `menus`/`site-chrome` sharing the pattern), pinned by `smoke-pages-builder-route` +25 lines. Reconstructed from commit 27c9d00. | Ben + Claude |
| v1.50-alpha | **The media bank learns what a field can hold, and two pickers stop opening twice.** Media picker filters by field capability (image vs any file) via `media.js` + `routes/media.js`, double-open race in two builder pickers fixed in `admin-builder.js`; `smoke-media-route` +28 lines. Reconstructed from commit dcf3b80. | Ben + Claude |
| v1.49-alpha | **The raw-HTML escape hatch graduates into a real tool.** The provisional-HTML stash became a first-class `html` module: block-registry entry, pzn schema + spec + syntax dictionary, builder UI, compile/decompile/keywords support — with the sanitizer battery grown (`smoke-pzn-html` +4 adversarial vectors). Reconstructed from commit 3468a90. | Ben + Claude |
| v1.48-alpha | **The drift guards were line-ending blind — green on CI, red on Windows.** `smoke-pzn-spec` and `smoke-route-map` compared generated artifacts byte-for-byte, so a CRLF checkout failed guards that passed on CI's LF; both now normalize line endings before comparing. Reconstructed from commit 579464f. | Ben + Claude |
| v1.47-alpha | **Route extraction FINISHED — the thirtieth and final cut, `src/routes/copilot.js`, unlocked by the bug we resolved one version earlier.** The AI copilot cluster was the last named blocker in ARCHITECTURE.md, and it was blocked for a concrete reason: two of its routes were the shadowed `/admin/api/syntax-dictionary` pair from the v1.19 bug. v1.46 resolved that; this version collected the payoff. Taken as the CONSIDERED GROUP the doc asked for — four sibling admin screens off one shared concern (how a user AI reaches this CMS) plus the APIs their client scripts call: `/admin/agent`, `/admin/ai`, `/admin/inject`, `/admin/chat`, `inject-pack`, `syntax-dictionary[.md]`, `ai/settings` (GET+POST), `ai/chat`. **Both AI tiers deliberately share one module** — BYOT/keyless (copy the pack into the chat you already pay for) and BYOK/key-in-the-CMS are one product decision seen from two sides, and the old split hid that; the tier realignment now has one file to reason about. Executed as a `git mv` of v1.35 `ai-paste.js` (which already held two of the four pages) so the large Hebrew HTML moved byte-for-byte instead of being retyped — no transcription risk. `server.js` **690 → 508 lines, a 90% reduction** from the session-start ~5,252, and with its last admin renderer gone it no longer imports `admin-ui` or `admin-guard` at all: **server.js renders no admin HTML of its own.** What is left inline is the middleware chain plus five strays that belong beside it — pulling those would be motion, not progress. **QA:** `smoke-copilot-route` grown 4 → 14 checks (all four screens render with their client scripts; all four plus the AI settings API unreachable unauthenticated; `inject-pack` answers in both JSON and roleplay-markdown; the v1.46 dictionary pin still holds; and `ai/settings` never echoes the stored `apiKey` back to the browser). Route count held at 146 across 33 files — nothing lost or duplicated. Full suite + 110 pzn unit tests green. | Ben (direction: CMS/CRM north star — "modular," "enjoy editing this code as professionals") + Claude |
| v1.46-alpha | **The one security surface never checked all session — the DEPENDENCY tree — audited, clean, and now CI-enforced.** This session audited the app's OWN escape/sanitize code (v1.38–40, one real XSS fixed) but never the third-party dependency tree — a real attack surface for any CMS (supply-chain CVEs). Ran `npm audit`: **0 vulnerabilities** (prod and full), consistent with the deliberately lean dependency set (better-sqlite3, body-parser, express, nodemailer, sharp). Clean — but NOT enforced: the CI `security.yml` workflow scanned secrets (gitleaks) and ran the test suites, yet never audited dependencies, so a future vulnerable dep would ship silently. Added a `deps` job that fails on a HIGH/CRITICAL advisory in a PRODUCTION dependency (`npm audit --omit=dev --audit-level=high` — what actually reaches a deployed CMS), plus a non-blocking full report for dev/low-sev visibility. Verified the gate passes today (0 vulns → exit 0) and the YAML is well-formed (jobs: secrets / smoke / deps; no illegal tabs). Same "lock in the good state" discipline as the drift guards, now applied to supply-chain security: the clean tree can't silently rot. This completes the security posture — app code AND dependencies both audited AND continuously enforced. CI-config + docs change; app suites unaffected. | Ben (direction: CMS/CRM north star — "professional," real security for a real CMS) + Claude |
| v1.45-alpha | **The repo's front door — `README.md` — was 97 versions stale and didn't point to the manifest it now has. Fixed.** The README is the entry point a professional cloning the repo meets first ("the main manifest of tapuz own build code"), and it stamped **v0.47-alpha** while the project was at v1.44 — a factual error at the top of the front page, same drift class as the COMPETITIVE.md stamp (v1.42). Also: after 29 extractions + the v1.44 route map, the README still didn't reference `docs/ROUTE-MAP.md` or `docs/ARCHITECTURE.md`, so a contributor couldn't find the navigational manifest from the door. Three factual, non-voice fixes (Ben's content/framing untouched): (1) version stamp → current; (2) the "בדיקות" section now leads with `npm run test:smoke` (the ~99-script CI gate) + `npm run test:pzn`, not just a handful of individual scripts; (3) a new "לעורכי הקוד (המניפסט)" section pointing to ROUTE-MAP.md ("which file owns which route") and ARCHITECTURE.md (the modularization story). Verified every README link resolves (no broken paths). This closes the loop on the "professional codebase / main manifest" pillar: the modular structure exists (extractions), is mapped (ROUTE-MAP), and is now DISCOVERABLE from the front door. Docs-only; suites unaffected. | Ben (direction: CMS/CRM north star — "the main manifest of tapuz own build code," "enjoy editing this code as professionals") + Claude |
| v1.44-alpha | **The navigational manifest the modular codebase now needs — `docs/ROUTE-MAP.md`, generated + drift-guarded.** 29 route-group extractions made `server.js` lean, but they spread the HTTP surface across ~30 files, and server.js's `app.use(require('./routes/X'))` mounts DON'T show the paths (those live inside each module) — so "which file owns `/admin/inbox`?" became a grep hunt. That's real friction against "everyone to enjoy editing this code as professionals" and "the main manifest of tapuz own build code." Added `scripts/gen-route-map.js` (`npm run gen:route-map`) — it parses `router.METHOD('…')`/`app.METHOD('…')` across `src/routes/*.js` + `src/server.js` and writes `docs/ROUTE-MAP.md`: 148 routes across 33 files, grouped by owning module AND as an alphabetical route→file index. Generated FROM source (single source of truth via an exported `buildRouteMap()`), drift-guarded by new `smoke-route-map.js` (6 checks: exists, non-trivial surface, IN SYNC with a fresh build, and three known routes resolve to their modules) — same "generated artifact must match the code" discipline as `smoke-pzn-spec`, now in the CI chain. A nice by-product: the map makes the v1.19 shadowed route self-evident (two identical `/admin/api/syntax-dictionary` rows in server.js). This is the modularization's missing companion — extraction without a map is a maze; now editing the modular surface is navigation, not archaeology. Full suite (99 scripts) + pzn unit suite green. | Ben (direction: CMS/CRM north star — "everyone to enjoy editing this code as professionals," "the main manifest") + Claude |
| v1.43-alpha | **The one open flagged bug (v1.19 shadowed syntax-dictionary route) traced to its exact blast radius — turning a vague "product decision" into a 1-line choice.** Rather than let the last unresolved item sit as an ambiguous "which dictionary is canonical?", traced its real consumer (`public/admin-inject.js`) to pin the actual impact. Finding: it's NARROW. The inject page's tool list + module count come from `/admin/api/inject-pack` (which uses the newer `pzn/syntax-dictionary` directly — unaffected by the shadow, already correct). The shadow touches ONLY the `btn-dict` "copy dictionary" button, which today copies the older block-registry markdown (~15.4KB, 39 modules) instead of the newer pzn markdown (~19.9KB). BOTH are valid, current, drift-guarded dictionaries describing the vocabulary from different angles — so which one a human should paste into a chat is a genuine DESIGN call, NOT a correctness bug. The two safe resolutions differ only in that one button's clipboard payload: (a) delete the dead duplicate (keeps today's older-markdown behavior), or (b) point the surviving route at the newer dictionary (richer markdown). Recorded the full analysis in `docs/ARCHITECTURE.md` and replaced the original vaguely-scoped task-chip with a precise, one-click-actionable one. This is the highest-leverage prep I can do for the decision that's genuinely Ben's: it's now a trivial, de-risked, one-line change whenever he picks (a) or (b). Docs + task-management change; suites unaffected. | Ben (direction: CMS/CRM north star — the flagged decision made trivial to act on) + Claude |
| v1.42-alpha | **The competitive parity map audited against the code, corrected, and the missing "our RPM" differentiator added.** `docs/COMPETITIVE.md` is the strategic scoreboard vs builder.io/WordPress/Drupal/Joomla — but it was stamped "(v1.05)" while its content already ran to v1.12, so it read as stale. Verified every "shipped" claim in it against the actual code (email notify → `src/notify.js`, roles → `routes/team.js` + `admin-guard.js`, search → `pages.listSearchable`/`renderSearchWidget`, multilingual → `routes/translations.js`, pricing → `pzn/pricing-html.js`, CSV → `src/csv.js`, templates → `src/templates.js`) — ALL accurate, no false claims, so the map is honest about what's built. Two real fixes: the stale version stamp (→ v1.42), and — more substantive — the "Where we're ahead" table had NO row for the portable-standard capability, which IS the North Star's "pzn file, our RPM file, we set standard" pillar and a genuine competitive differentiator: a whole site / theme / page is a portable versioned file (site export-import v1.06, theme v0.99), and the `.pzn` format is a machine-readable PUBLISHED standard anyone can implement (`docs/pzn-schema.json` + `pzn-spec.md`, served at `/pzn-schema.json`, drift-guarded v1.36–37). WordPress's `.wxr` is lossy/plugin-shaped with no published page-format standard; the others have no portability. That row was missing from the map despite being shipped and verifiable — added it in the established table style. A factual, code-grounded correction (not a strategic rewrite — Ben's voice/framing untouched): the parity map now accurately reflects both what's shipped and Tapuz's signature "our RPM" edge. Docs-only change; suites unaffected. | Ben (direction: CMS/CRM north star — "pzn file, our RPM file, we set standard," competitive positioning) + Claude |
| v1.41-alpha | **Three real, passing smoke tests were silently NOT in CI — wired in, so the code they guard is actually protected.** A CI-chain integrity sweep (does every referenced script exist? are there tests that exist but never run?) found the chain clean on references but discovered THREE orphan smoke tests excluded from `test:smoke`: `smoke-import-file` (the WXR importer ENGINE — Gutenberg mapping, HTML fallback, draft-skipping; deeper than the route test), `smoke-decompile-picture` (the decompiler's full-page capture), and `smoke-sitemap` (menus + sitemap structure + redirect E2E). All three pass today and cover distinct real functionality — but being outside the gate meant a regression in `src/importers`, the decompiler, or `src/sitemap`/`src/menus` wouldn't be caught by the very tests written to catch it (a false sense of coverage). `smoke-sitemap` was excluded for a concrete reason — it writes real pages (`createPage`/`deletePage`) with NO throwaway `TAPUZ_ROOT`, so it polluted the live DB; hardened it to `mkdtemp` an isolated root before the src requires (paths.js resolves at load time) + clean up after, matching every other DB-touching smoke test. The other two are pure in-memory, added directly. `test:smoke` now runs 98 scripts with ZERO orphan smoke tests remaining — every smoke test in the repo is now an active CI guard. Full suite (98 scripts) + pzn unit suite green, the three newly-wired tests passing in-chain. A small but real "professional codebase" fix: a test that never runs is worse than no test — it looks like coverage without being it. | Ben (direction: CMS/CRM north star — "everyone to enjoy editing this code as professionals," "good engineering") + Claude |
| v1.40-alpha | **`scrubCss` (inline-style CSS scrubber) audited + its legacy-binding neutralizations pinned — completing the escape/sanitize security-surface review.** The provisional-HTML sanitizer (`src/html-sanitize.js`) preserves legit inline `style="…"`, scrubbed by `scrubCss`. Audited it against CSS-injection vectors: `url(javascript:)`, `expression()`, `-moz-binding` (Firefox XBL), `behavior:` (IE HTC), `@import`, `url(data:text/html…)` — all neutralized (dangerous properties renamed to inert `x-…`, `@import` removed, `url()` schemes → `url(#)`), even the IE `*behavior` star-hack. No bug. Coverage was partial, though — `smoke-pzn-html`'s adversarial battery pinned `expression`/`@import` but not the binding vectors, which are DISTINCT attack classes, so three cases were added (`behavior`, `-moz-binding`, `url(javascript:)` in a style) to lock each neutralization in. This closes the security-surface audit opened in v1.38: across the FOUR escape/sanitize primitives — `safeHref` (had a real XSS bypass → fixed, v1.38), `escapeCssUrl` (sound → guarded, v1.39), `sanitizeHtmlFragment` (sound + already covered), and `scrubCss` (sound → coverage completed here) — every one is now both verified robust AND regression-guarded in CI. `smoke-pzn-html` PASS with the 3 new vectors; full suites green. | Ben (direction: CMS/CRM north star — "professional," "we set standard," real security) + Claude |
| v1.39-alpha | **The OTHER half of spec rule 9 — CSS `url()` breakout — audited and locked in.** After fixing the `safeHref` XSS bypass (v1.38), the same rule-9 sentence also promises background-image URLs are escaped for the CSS `url()` context, not just HTML. Audited `escapeCssUrl` against breakout vectors — close-paren `);`, quote breakout, single-quote, `</style><script>`, and backslash-escape smuggling — and unlike `safeHref`, it held: every url()/CSS/HTML-dangerous character (`( ) ' " \ < >` + line terminators) is already CSS-hex-escaped, applied inside quoted `url('…')` in the renderer, so a background image can neither inject a CSS declaration nor break out of a `<style>` block. No bug — but, exactly like `safeHref` before this session, this security-critical function had ZERO regression coverage. Added a 4-group `escapeCssUrl` suite to `test/pzn/escape.test.js` (breakout resistance across all vectors, line-terminator stripping, plain-URL preservation, nullish safety) — 17 assertions total in the file now, all green and in CI, so a future refactor can't silently weaken the CSS escaper. Test-only change (the function was already correct); the value is the durable guarantee. This completes the audit of BOTH halves of the spec's normative security rule: one had a real bug (fixed), one was sound (now guarded). Full pzn + smoke suites green. | Ben (direction: CMS/CRM north star — "professional," "we set standard," real security) + Claude |
| v1.38-alpha | **A real XSS bypass in the `.pzn` URL guard — closed. `safeHref` was letting `java\tscript:` and `\x01javascript:` through.** The v1.36/v1.37 "does the code match the standard?" audits led here: `docs/pzn-spec.md` rule 9 promises clickable-link props (`href`/`url`) MUST reject the `javascript:`/`data:`/`vbscript:` schemes. Checking the implementation (`src/pzn/language/escape.js` `safeHref`) against real bypass vectors found the guarantee was PARTIAL — it only `.trim()`-ed leading whitespace, so three obfuscations sailed through: a leading non-whitespace control char (`\x01javascript:`), and tab/newline/CR smuggled INSIDE the scheme (`java\tscript:`, `java\nscript:`) — all of which browsers strip before resolving the URL, then execute. Since `.pzn` URL props come from author/AI-generated or imported content (and BYO-AI can be adversarial), a crafted `href` could smuggle script onto a published page. Fixed by normalizing the way a browser does — strip every ASCII control char (0x00–0x1F, 0x7F) AND trim BEFORE the scheme test, returning the cleaned value — so a smuggled scheme is both rejected and stripped, while legitimate URLs (https, relative, `mailto:`, `tel:`, `%09`-encoded query chars) pass untouched. `safeHref` guards links across 9 renderers + `renderer.js`, so the fix applies everywhere links render. **QA:** `test/pzn/escape.test.js` gains a 5-group `safeHref` suite (bare schemes, leading-whitespace/control obfuscation, in-scheme control smuggling, legit-URL preservation, nullish → harmless `#`) — 13 assertions, all green, and this file runs in CI (`.github/workflows/security.yml` → `npm run test:pzn` + `test:smoke`), so the guarantee can't silently regress. Full smoke suite (101 scripts) + the pzn unit suite both verified green — the hardened normalization breaks no renderer. | Ben (direction: CMS/CRM north star — "professional," "we set standard," real security for a real CMS) + Claude |
| v1.37-alpha | **The second stale standard artifact — `docs/SYNTAX-DICTIONARY.md` — regenerated, and the v1.36 drift-guard extended to cover it.** v1.36's fix raised the obvious question: if the `.pzn` schema silently rotted, what ELSE generated-from-code but committed had drifted? An audit of the four `gen:` scripts found the human syntax dictionary (`docs/SYNTAX-DICTIONARY.md`, generated from block-registry via `npm run gen:dictionary`) was ALSO ~9% stale — 13,983 chars committed vs 15,382 fresh. That's the reference agents and humans read to learn the vocabulary, silently behind the real registry. Regenerated (39 modules, now in sync) and `smoke-pzn-spec` extended (+2 checks, now 13) to assert it stays in sync with block-registry — so this artifact can't out-run its published dictionary either. The audit also checked `public/chat-snippet.txt` (a 1-char trailing-newline diff — cosmetic, not a real drift, deliberately left alone rather than churned) and `public/chat-snippet.json`; the two schema artifacts + this dictionary were the only meaningful drifts, both now fixed and guarded. This closes the "published standard silently lies to implementers" gap across all of Tapuziel's generated-standard artifacts. Full suite (101 scripts) verified green with the corrected exit-code check. | Ben (direction: CMS/CRM north star — "pzn file, our RPM file, we set standard") + Claude |
| v1.36-alpha | **The `.pzn` standard was silently STALE — regenerated + a drift-guard added so "our RPM" can never quietly lie again.** A pivot off pure modularization to the North Star's "pzn file, our RPM file, we set standard" pillar, which needed no product decision — just correctness. The published machine-readable standard (`docs/pzn-schema.json`) is generated from the live module registry, but nothing enforced that it STAYED generated: it was found stamped `tapuziel@0.66.0-alpha` and **missing 7 modules** the registry had gained since (slide, carousel, audio, trow, table, plan, pricing). Anyone implementing a `.pzn` reader/writer from the published "RPM" would have wrongly rejected valid pages using those modules — the standard was under-describing its own vocabulary by 12%. Fixed by regenerating (`npm run gen:spec` → `docs/pzn-schema.json` + `docs/pzn-spec.md` now cover all 56 modules, stamped at the current version). **QA:** new `smoke-pzn-spec` (11 checks) — the guardrail that makes the fix durable: the catalog carries a valid JSON-Schema envelope + spec version, EVERY registered module appears in it fully typed (no vocabulary gap), containers declare their `accept[]` children, the universal id/class props are correctly excluded, and — the one that matters — the committed `docs/pzn-schema.json` is asserted IN SYNC with a fresh `buildCatalog()` (comparing the substantive `modules`, ignoring the version-stamped provenance line), so adding a module without regenerating the standard now FAILS the build. The human `pzn-spec.md` is checked to advertise the right module count too. This is the invariant that keeps ".pzn is our RPM, we set standard" honest — a published standard that drifts from the code isn't a standard. Full suite (101 scripts) verified green with the corrected exit-code check. | Ben (direction: CMS/CRM north star — "pzn file, our RPM file, we set standard") + Claude |
| v1.35-alpha | **Twenty-ninth route-group extraction — `src/routes/ai-paste.js` — the BYO-AI paste-flow pages, a deliberately-scoped cut of the wedged copilot cluster.** `GET /admin/ai` (the keyless "teach your own AI subscription the .pzn syntax, paste its reply back" flow) and `GET /admin/inject` (the copy-the-pack screen) — the North Star's "your AI subscription, not our tokens" ace — move out as two cohesive onboarding pages (only `layout`/`adminNav`/`accentFor`, no state, no secrets). This is scoped ON PURPOSE: the full BYOK/AI cluster is wedged around two things that must stay in `server.js` — the `require('./routes/import')` MOUNT (sitting between `/admin/agent` and `/admin/ai`) and the two shadowed `/admin/api/syntax-dictionary` routes (the v1.19 bug still awaiting a decision) — so rather than force a fragmented extraction around both, only the clean CONTIGUOUS chunk between them was taken; the rest of the cluster (`/admin/agent`, `/admin/chat`, the ai/settings + ai/chat + inject-pack APIs) stays put. `server.js` dropped 120 lines (824 → 704), **under 750 for the first time — an 87% reduction** from the session-start ~5,252. **QA:** new `smoke-ai-paste-route` (4 checks) — genuinely new route coverage (no prior test hit these screens): both render behind the admin gate with their client scripts + mount points, and neither is reachable unauthenticated. A wrong marker id in the first draft (`copy-primer`, actually in a different page) was caught and fixed against the real rendered ids. Full suite (100 scripts) verified green with the corrected exit-code check, run both before and after. | Ben (direction: CMS/CRM north star — "your AI subscription, not our tokens," "modular") + Claude |
| v1.34-alpha | **Twenty-eighth route-group extraction — `src/routes/bentml-api.js` — the BenTML language API (".pzn is our RPM"), taking `server.js` to an 84% reduction.** The doc/primer endpoints (primer, chat-snippet, agent-pack) and the language OPERATIONS the advanced code tab runs — compile / preview / decompile / apply (BenTML source ⟷ builder blocks) — move out as one cohesive router, directly on-theme with the North Star's ".pzn / BenTML, our RPM" pillar. **Deliberately left inline:** the two `/admin/api/syntax-dictionary` routes (plus the small engine.js + modules routes around them), because they're entangled in the v1.19 shadowed-route bug still awaiting a product decision — extracting only the contiguous primer→apply run keeps that decision fully deferred rather than forcing it. A `__dirname` footgun handled on purpose: primer/chat-snippet read real files via `path.join(__dirname, '..', 'docs'|'public', …)`, so moving from `src/` to `src/routes/` needed one more `'..'` — verified by probing both endpoints and confirming they return the real 6.6KB cheatsheet + snippet, not the tiny fallback a wrong path yields. `looksLikePzn`/`pznSourceToBlocks`, used only by the compile route that moved, were then dead in `server.js` and trimmed. `server.js` dropped 165 lines (989 → 824) — an **84% reduction** from the session-start ~5,252. **QA:** verified by the pre-existing `smoke-bentml`/`smoke-bentml-engine`/`smoke-decompile` suites (which already cover this surface) plus a direct HTTP probe of the file-reading endpoints for the `__dirname` fix; no redundant new test written. Full suite (99 scripts) verified green with the corrected exit-code check, run both before and after. The remaining inline `server.js` is now mostly the middleware chain (app assembly, legitimately stays) plus the blocked BYOK/AI cluster. | Ben (direction: CMS/CRM north star — "pzn file, our RPM file," "modular," "professional") + Claude |
| v1.33-alpha | **Twenty-seventh route-group extraction — `src/routes/agent-bridge.js` — the public `/agent/v1` API, the session's most intricate surgery, taking `server.js` UNDER 1,000 lines.** The whole "agents are users too" surface (15 bearer-token read/write endpoints an AI or the extension uses to read the vocabulary and author pages) moves out with the three things it shares that were scattered across `server.js`: the CORS/preflight middleware, the per-IP `agentLimiter`, and the `requireAgent(scope)` guard. Genuinely non-contiguous — helpers at the top of the file, routes 200 lines below, and the PUBLIC `/pzn-schema.json` sitting in the MIDDLE of the route run (it's unauthenticated and not agent-scoped, so it stayed in `server.js`, relocated just above the mount). Two footguns handled on purpose: `require('../package.json')` → `require('../../package.json')` (the file moved a directory deeper), and the `/agent` JSON body-parser stayed in `server.js` mounted BEFORE the router (load-bearing — the POST handlers need a parsed body). `agentTokens`, used only by the moved guard, was then dead in `server.js` and trimmed. `server.js` dropped 267 lines (1,256 → 989) — an **81% reduction** from the session-start ~5,252, under 1,000 for the first time. **QA:** verified by the PRE-EXISTING `smoke-agent-bridge` — already the comprehensive HTTP-level suite for this surface (real bearer auth, scope enforcement, every endpoint, path-traversal rejection, CORS, the oversized-body 413 with no stack leak) — all green; no new test was written because the surface was already thoroughly covered end-to-end, and fabricating a redundant one would be theater. The full suite (99 scripts) passed too, confirming that moving the CORS/preflight middleware into the mounted router changed nothing observable. | Ben (direction: CMS/CRM north star — "agents are users too," "modular," "professional") + Claude |
| v1.32-alpha | **Twenty-sixth route-group extraction — `src/routes/admin-home.js` — `GET /admin`, the pages-list landing, and the payoff of the v1.30 `needsSetup` promotion.** The admin home (pages list + the post-build "your site is live" screen + the v0.78 homepage-status banner: who owns `/`, or the warning when nobody does) moves out; it redirects a pristine install to the setup wizard via the shared `needsSetup()`. With this route and the wizard (v1.30) both extracted, `needsSetup` had NO remaining caller in `server.js` — so its import was trimmed there entirely. The v1.30 promotion to `src/setup.js` has now fully paid off: `server.js` neither defines nor imports `needsSetup`, and the single source of truth serves the two route modules that need it. `server.js` dropped 88 lines (1,343 → 1,256), **under 1,300 for the first time — a 76% reduction** from the session-start ~5,252. **QA:** new `smoke-admin-home-route` (6 checks) across every state the page reasons about — the pristine-install redirect to setup (exercising the shared guard through real HTTP), the normal pages list with edit links, both status banners (`?built=1`, `?homeset=1`), and that an unauthenticated request never renders the list. Full suite (99 scripts) verified green with the corrected exit-code check, run both before and after. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.31-alpha | **Twenty-fifth route-group extraction — `src/routes/pages-builder.js` — THE site builder, the largest single cut of the session (~477 lines).** The whole builder surface moves out as one router: `/admin/new` (template picker) + create, delete, server-side `/admin/preview`, the visual builder page `/admin/edit`, and the save/publish/build lifecycle — the heart of the "easy to understand site builder" the North Star centers on. Every dependency is a clean module import (page store, BenTML engine, block registry, renderer, admin-ui shell), nothing reaching back into `server.js`. `server.js` dropped 473 lines (1,816 → 1,343) — a **74% reduction** from the session-start ~5,252. Because the builder PAGE moved, four pre-existing tests that source-assert against `server.js` for builder markup (`smoke-layers`/`smoke-responsive`/`smoke-symbols`/`smoke-templates`) correctly went red and were repointed at the new module (the routine v1.20/v1.29 stale-assert fix). **QA:** new `smoke-pages-builder-route` (12 checks) drives the FULL lifecycle over real HTTP — new → create (→ editor redirect) → builder renders → save draft → preview shows the draft with SAMEORIGIN framing → publish → the page is served as static HTML with real content → build regenerates → delete. Two test-fixture bugs taught real contracts before the test shipped: heading blocks render `data.text` (not `html`), and `title` is a NOT NULL column the builder always sends (omitting it 400s at the DB layer — pre-existing behavior, confirmed not a regression by `smoke-publish-live` still passing). Full suite (98 scripts) verified green with the corrected exit-code check, run both before and after. | Ben (direction: CMS/CRM north star — "easy to understand site builder," "modular," "professional") + Claude |
| v1.30-alpha | **Twenty-fourth route-group extraction — `src/routes/setup-wizard.js` — the onboarding wizard, with a `needsSetup` de-duplication done first.** The first-run wizard (`GET /admin/setup`, the 4-step name → colors → pages → menu flow) + its POST that runs `src/setup.js` and builds the site skeleton move out. But first, a real duplication got fixed: `needsSetup()` was a server.js-local helper AND (since v1.24) an inline copy in `dashboard.js` — heading toward a third copy in the wizard. Instead it was **promoted to `src/setup.js`** (single source of truth, next to `runSetup`, the state it guards); `server.js` and `dashboard.js` now import it, and dashboard's now-dead `loadConfig` import was trimmed. That 3-file consolidation passed the FULL suite as its own green checkpoint before the extraction proceeded. Then the wizard moved, and `runSetup` + `LOOKS` — dead in `server.js` once it left — were trimmed. `server.js` dropped 259 lines (2,075 → 1,816), **under 1,900 for the first time — a 65% reduction** from the session-start ~5,252. **QA:** new `smoke-setup-wizard-route` (8 checks) — genuinely new HTTP coverage (the pre-existing `smoke-wizard` tests the `runSetup` ENGINE at the module level, never the routes): the wizard renders on a pristine install with the shared LOOKS fed as `WIZ_LOOKS`, `/admin` bounces to it while pending, the POST builds real pages (home + about), and the guard's whole point is proven — after setup both routes flip (GET → redirect to `/admin`, POST → 409, no double-run). The test had to model the real onboarding flow (create-admin → login → wizard, since the gate makes `/admin/setup` reachable only WITH a session) while deliberately NOT running setup, to keep the install pristine until the POST. Full suite (97 scripts) verified green with the corrected exit-code check. | Ben (direction: CMS/CRM north star — "modular," "everyone to enjoy editing this code as professionals") + Claude |
| v1.29-alpha | **Twenty-third route-group extraction — `src/routes/auth-screens.js` — the login/logout/create-account surface, and an HTTP-test that caught a real 500-in-production bug.** The whole auth surface (login page + POST, logout, first-admin create-account page + POST), its shell helpers (authCard/authErr/authInput), and the escalating brute-force `loginGuard` move out together — non-contiguous (guard near the top, routes far below), two splices plus a rebuild. Mounted after the admin gate, which path-exempts `/admin/login` and `/admin/create-account` (logout stays gated); the now-dead `LoginGuard` top-import was trimmed. `server.js` dropped 163 lines (→ 2,075, under 2,100 for the first time) — a **60% reduction** from the session-start ~5,252. **The value of HTTP-level tests, demonstrated:** new `smoke-auth-screens-route` (9 checks) drove the full flow and the lockout check went RED with a 500 — because `authErr()` calls `escapeAdmin(msg)` but `escapeAdmin` was missing from the module's imports. Every happy path dodged it (`authErr('')` short-circuits before `escapeAdmin`), so syntax-check and the login-page render both passed — but EVERY error-message render (wrong-password `?err=1`, create-account errors, the lockout page) would have thrown a 500 in production. A source-assert or module-level test never touches that branch; only exercising the locked path over real HTTP surfaced it. Fixed by importing `escapeAdmin`, re-verified the lockout returns 429 after 6 failures. Also repaired the pre-existing `smoke-palette` (its source-assert counting the three `authCard(...bare)` calls in `server.js` correctly went red when they moved — repointed at the new module, the v1.20 stale-assert fix pattern). Full suite (96 scripts) verified green with the corrected exit-code check, run both before and after. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.28-alpha | **Twenty-second route-group extraction — `src/routes/form-capture.js` — the public form endpoint, a NON-contiguous cut.** The FORM module's default action (`POST /api/form` → forms inbox, v0.81) + its RTL thank-you page (`GET /form-sent`), PLUS the per-IP `formLimiter` they depend on — which lived 90 lines up near the top of `server.js`, so this was a two-splice extraction (routes → mount, limiter def → gone) plus a rebuild, not a single slice. Ordering is load-bearing: the mount sits AFTER the global urlencoded body-parser (so the POST body is parsed) and BEFORE the static mounts, exactly where the inline routes were. `server.js` dropped 63 lines (2,238 → 2,175) — a **59% reduction** from the session-start ~5,252. **QA:** new `smoke-form-capture-route` (9 checks) — genuinely new HTTP-level coverage (prior form tests hit `forms.saveSubmission` at the module level, never the endpoint): a real submission persists with `_`-key stripping + explicit `_page` attribution, the honeypot returns ok:true while storing NOTHING, missing `_page` falls back to the Referer path, an HTML post redirects to `/form-sent` while an `Accept: json` one gets `{ok,id}`, and the moved `formLimiter` still trips 429 after its cap. **A test-writing bug taught a real fact about the middleware chain:** the first draft sent JSON request bodies and failed — because `/api/form` is mounted BEFORE the JSON body-parser, so it only ever parses URLENCODED bodies (a real form post); `Accept: application/json` selects the RESPONSE shape, not the request encoding. Fixed to urlencoded and documented inline. That failure is the value of HTTP-level tests: a module-level test would never have surfaced the body-parser ordering. Full suite (95 scripts) verified green with the corrected exit-code check, run both before and after. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.27-alpha | **Twenty-first route-group extraction — `src/routes/seo-files.js` — the first PUBLIC-route cut, where registration ORDER is load-bearing.** `/sitemap.xml` + `/robots.txt` (derived live from published pages, v0.71) and the `siteBaseUrl()` helper they share move out — but unlike every admin-page move, these must be registered BEFORE the `express.static` mounts or a static file of the same name would shadow them. So the router mounts at the exact same pre-static line the inline routes held: only the code moved, never the registration point — the same discipline the v1.11 auth-gate cut set. `server.js` dropped 19 lines (2,257 → 2,238), a 57% reduction from the session-start ~5,252. **QA:** new `smoke-seo-files-route` (7 checks) — genuinely new HTTP-level coverage (the pre-existing `smoke-seo` exercises the `src/seo.js` BUILDERS directly, never over the wire): both files served 200 without auth at the right content-types, the sitemap carrying a real published page at the configured absolute baseUrl, robots pointing crawlers at the sitemap and disallowing `/admin`, and — the check that proves the ordering held through the move — planting a real `public/robots.txt` on disk and confirming the LIVE ROUTE still wins over it (the static file does not shadow the dynamic one). Full suite (94 scripts) verified green with the corrected exit-code check, run both before and after to isolate any regression to this change alone. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.26-alpha | **A consolidation, not a new cut — `GET /admin/api/sitemap` rejoins its own page in `src/routes/sitemap.js`.** When the sitemap PAGE was extracted back in v1.03, its JSON twin (`/admin/api/sitemap`) was left behind inline in `server.js` — so one feature lived in two files for 23 versions. This reunites them: the ~7-line JSON route moves into `routes/sitemap.js` beside the page it mirrors, both driven by the same `buildSitemap()`. The point worth recording: the modularization goal isn't merely "shrink `server.js`" but "each feature in one coherent home" — some cuts create a module, some finish one. `server.js` dropped 9 lines (2,266 → 2,257). **QA:** rather than a new test file, the existing `smoke-sitemap-route` was EXTENDED (+3 checks, now 10) — the relocated JSON twin returns the real menus+orphans structure and is behind auth, exercised through the same mounted router as its page; no new `package.json` entry, the same suite line now covers both halves of the feature. Full suite (93 scripts) verified green with the corrected exit-code check, run both before and after. | Ben (direction: CMS/CRM north star — "modular," "everyone to enjoy editing this code as professionals") + Claude |
| v1.25-alpha | **Twentieth route-group extraction — `src/routes/homepage.js` — homepage crowning, one concern behind two doors.** Setting which published page is served at `/` (v0.78): the `setHomepage()` helper (used nowhere else, so it moved with the routes) plus its two entry points — `POST /admin/homepage` (form → redirect, from the pages screen) and `POST /admin/api/homepage` (JSON, the builder's publish-flow toast). The rule that makes it a distinct concern rather than a stray pair: only a PUBLISHED page may be crowned, and the site rebuilds on the spot so `/` is live immediately (the "publish MEANS live" contract from v0.69). `server.js` dropped 33 lines (2,299 → 2,266) — a **57% reduction** from the session-start ~5,252. **QA:** new `smoke-homepage-route` (6 checks) — BOTH doors exercised (JSON crowning returns the new homepage; the form door redirects on success), the on-the-spot rebuild PROVEN by fetching `/` and finding the crowned page's live content, and the "published only" guard enforced on both doors (a draft-only page → JSON 400 and a 400 error-page on the form door, never a silent crowning), plus an unknown-page 400. The twentieth extraction of the session; twenty small, reversible, individually-tested cuts, each green before and after. Full suite (93 scripts) verified green with the corrected exit-code check. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.24-alpha | **Nineteenth route-group extraction — `src/routes/dashboard.js` — the admin landing hub + its build shortcut.** The dashboard (stat cards: pages/published/drafts/pending/media/unread-inbox; the tool-family grid derived from `ADMIN_NAV_GROUPS` so it can never advertise a tool that doesn't exist; recent pages; quick actions) and the `/admin/build-redirect` shortcut its "🚀 build the site" button posts to move out together — they belong together. One documented judgment call: `needsSetup()` is a 3-line server.js-local guard used by the dashboard AND three other still-inline routes (setup screens, admin index); rather than spin up a shared module for a trivial three-liner or reach back into `server.js`, its one-liner is inlined in the new module with a comment (the pragmatic call — it earns its own module only when a consumer outside `server.js` also needs it). `server.js` dropped 123 lines (2,422 → 2,299) — a **56% reduction** from the session-start ~5,252. **QA:** new `smoke-dashboard-route` (9 checks) — the hub renders real seeded counts (a published page + a draft + an unread lead), the tool grid, and recent pages with edit links; the unread-inbox card links to the inbox; and — the check that matters — `POST /admin/build-redirect` genuinely REBUILDS the site (the published page appears as static HTML on disk afterward, verified by fetching `/pub.html`) then redirects back to the dashboard. Also confirmed the existing `smoke-admin-gate` (which hits `/admin/dashboard` over HTTP as a generic gated route) still passes unchanged — the route is mounted, just from a different file. Full suite (92 scripts) verified green with the corrected exit-code check, run both before and after to isolate any regression to this change alone. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.23-alpha | **Eighteenth route-group extraction — `src/routes/settings.js` — site settings + the whole-site package (".pzn is our RPM").** The general site-settings page (name / description / homepage / baseUrl / language) + its save API move out together with the portable whole-site export/import — the North Star's "`.pzn` is our RPM" pillar one level up from theme packages (v0.99): a single versioned file bundling every page (draft + published), the theme, and the menus, for backup or transfer between Tapuz installs. Every route is `requireAdmin` (import creates/overwrites pages + site config — the strongest reason to keep the group intact and gated). Extracted programmatically (same slice-and-rewrite approach as v1.22's inbox) to move the ~140-line settings-page HTML with zero transcription drift. `server.js` dropped 139 lines (2,561 → 2,422) — a **54% reduction** from the session-start ~5,252. **QA:** new `smoke-settings-route` (11 checks, all passing first try) — the `requireAdmin` gate 403s an editor on both the page and every API (settings save AND whole-site export), a real settings round-trip persisting on a fresh render, the homepage-must-exist guard (400 on a missing page, config left intact), and a genuine export → import CYCLE proving the collision policy end-to-end: re-importing the same package skips existing pages by default (no clobber, `pagesCreated` empty), `overwrite:true` updates them (`pagesUpdated`), and a wrong-format package is rejected 400. Full suite (91 scripts) verified green with the corrected exit-code check, run both before and after to isolate any regression to this change alone. | Ben (direction: CMS/CRM north star — "pzn file, our RPM file," "modular," "professional") + Claude |
| v1.22-alpha | **Seventeenth route-group extraction — `src/routes/inbox.js` — the forms-inbox / lead-pipeline CRM surface, the biggest single cut of the session.** The whole "advanced CRM pipeline" the North Star names moves out as one router: the inbox page (leads as an expandable list + the pipeline summary tiles — open value / won value / due-today — and the `?status=` stage filter), its Excel-ready CSV export, and every lead mutation — read/delete plus the CRM fields status/notes/value/follow-up (v1.00 + v1.12). ~246 lines of hand-written HTML+JS, so to move it faithfully the block was extracted PROGRAMMATICALLY (slice the exact line range, rewrite `app.get/post`→`router.get/post` and `require('./forms')`→`require('../forms')`) rather than retyped — zero transcription drift. `server.js` dropped 246 lines (2,807 → 2,561), the biggest single-cut of the session and under 2,600 for the first time — a **51% reduction** from the session-start ~5,252 (past the halfway mark). **QA:** new `smoke-inbox-route` (13 checks) — two real seeded leads, then the page render with the summary tiles, the CSV export carrying real lead data, and EVERY mutation (status/value/follow-up/notes/read/delete) round-tripped through the mounted router with persistence CONFIRMED on a fresh re-render (a saved ₪5,000 deal value appearing in the open-pipeline tile), plus the `?status=` stage filter and the reject paths (non-numeric value and malformed date both → clean `{ok:false}`, not a crash). The pre-existing `smoke-forms-inbox` tests the `forms` MODULE directly (store contract), not the routes, so it needed no change — this is genuinely new HTTP-level coverage for the CRM surface. Full suite (90 scripts) verified green with the corrected exit-code check, run both before and after to isolate any regression to this change alone. | Ben (direction: CMS/CRM north star — "vast and big CMS/CRM mechanics," "advanced CRM pipeline") + Claude |
| v1.21-alpha | **Sixteenth route-group extraction — `src/routes/content-api.js` — the read/history content API, grouped by data concern.** The page-navigator list (`/admin/api/pages` with `?q=` search + `?status=` filter), the article-cube list (`/admin/api/articles`), and the revision history + restore (`/admin/api/revisions/:fullPath`, `/admin/api/revisions/restore`) move out as one router — grouped because all four are page-DATA reads (plus one restore) sharing `src/pages.js`, NOT because they share a URL prefix. The page-MUTATING routes (create/save/publish/delete) stay in `server.js` for their own heavier, higher-risk cut. A genuine dead-import cleanup came with it: `listArticles`/`listRevisions`/`restoreRevision` were each used in exactly ONE spot (the routes that just moved), so they were trimmed from `server.js`'s top-level `require('./pages')` destructure, while `listPages`/`getPageByFullPath`/etc. stay (used across many other routes). `server.js` dropped another 30 lines (2,837 → 2,807), down 47% from the session-start ~5,252. **QA:** new `smoke-content-api-route` (7 checks) — seeded with REAL pages, a REAL published+tagged article, and REAL saved revisions (two saves on one page → real backup history), then exercising the list + `?q=` search + `?status=published` filter, the article cube, and a genuine revision restore round-trip plus the foreign-revision-id 400. A test-seed bug (set the article tag via `meta.tags`, but `listArticles` reads the TOP-LEVEL `tags` page field that `updatePage({tags})` sets) was caught and fixed before the suite shipped — the endpoint code itself was unchanged by the move, so the red was purely the fixture. Full suite (89 scripts) verified green with the corrected exit-code check, run both before and after to isolate any regression to this change alone. Also cleaned a stray duplicated section in `docs/ARCHITECTURE.md` left by the v1.20 edit. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.20-alpha | **Fifteenth route-group extraction — `src/routes/symbols.js` — the builder's 💠 saved-block library API, and a pre-existing wiring test that correctly went red.** The three `/admin/api/symbols*` routes (list / save / delete the reusable-block library, v0.91) move out as one self-contained CRUD router; the builder's client panel (the `symbols-fold`/`symbols-list` toolbox UI on the `/admin/edit` page + `public/admin-builder.js`) is untouched, so only the API moved. `server.js` dropped another 25 lines (2,862 → 2,837), down 46% from the session-start ~5,252. **QA:** new `smoke-symbols-route` (9 checks — a real list/save/delete round-trip through the mounted router, the nameless-and-typeless 400 guards, the detached-block-snapshot round-trip, and the unknown-id clean `{ok:false}` rather than a crash). The extraction ALSO turned the pre-existing `smoke-symbols.js` red on its source-assert (`app.get('/admin/api/symbols'…` must appear in `server.js`) the moment the routes left that file — which is exactly the assertion doing its job: it caught that the wiring moved. Fixed the same way `smoke-palette.js` was in v1.05: assert the routes on the new router module and that `server.js` mounts it, rather than expecting the handler bodies to still be inline. Full suite (88 scripts) verified green with the corrected exit-code check — the mid-run `smoke-symbols` failure was diagnosed and fixed, not worked around. `docs/ARCHITECTURE.md` updated: fifteen extractions done; the BYOK/AI copilot cluster remains the last group and still waits on the v1.19 shadowed-route bug being resolved first. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.19-alpha | **Fourteenth route-group extraction — `src/routes/agent-tokens.js` — the credential-tier admin API, plus a real latent-bug find while scoping the last cluster.** The three `/admin/api/agent-tokens*` routes (mint / list / revoke the bearer tokens that gate the public `/agent` surface — all `requireAdmin`, since a minted token is site access at the same tier as an SMTP or AI key) move out as one router; the token VERIFICATION path (`agentTokens.verifyAgentToken`, used by the `/agent` bearer middleware) stays in `server.js` — same surface/auth-split principle as v1.18. `server.js` dropped another 17 lines (2,879 → 2,862), down 46% from the session-start ~5,252. **QA:** new `smoke-agent-tokens-route` (13 checks, security-focused, all passing first try): the `requireAdmin` gate genuinely 403s an editor for both list and mint, the secret is returned exactly ONCE at mint and never appears in the list (verified by substring search, not just field shape), a minted token really authenticates a live `/agent/v1/ping` while a tampered one 401s, and — the property that matters most — revoking a token actually KILLS its `/agent` access on a follow-up request, not just drops the list row. **Latent bug flagged (not silently fixed — it's a product call):** while mapping the last big cluster (the BYOK/AI copilot area), a genuine shadowed-route bug surfaced — `/admin/api/syntax-dictionary` and its `.md` variant are each registered TWICE in `server.js`, once against the older `src/syntax-dictionary.js` (~line 1749) and again against the newer, richer `src/pzn/syntax-dictionary.js` (~line 2697). Express matches the first, so the newer handler is dead code and the `/admin/inject` consumer currently receives the OLDER dictionary (≈15.4KB md vs ≈19.9KB). Resolving it means choosing which dictionary is canonical, so it was spun off as a dedicated follow-up rather than decided mid-extraction. Full suite (87 scripts) verified green with the corrected exit-code check, run both before and after to isolate any regression to this change alone. `docs/ARCHITECTURE.md` updated: the BYOK/AI copilot cluster is the last remaining group and should move only after the shadowed-route bug is resolved, since two of its routes are entangled in it. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.18-alpha | **Thirteenth route-group extraction — `src/routes/mission.js` — the copilot-mission admin API, split from its own agent-facing pull side by SURFACE.** The four `/admin/api/mission/*` routes (list providers, produce a teach pack, create a mission, activate one) — the admin half of the BYO-AI copilot flow, where the admin mints the roleplay teach-pack + build-quest an external AI then runs — move out as one router. Deliberately staying in `server.js`: the copilot's own PULL side (`/agent/v1/mission`, `/agent/v1/mission/:id/step`), because it's the same feature on a genuinely different surface — bearer-token auth on the public `/agent` namespace vs. admin-session auth here — and splitting by surface/auth is the exact same principle that kept the public `/_tapuz/collect` beacon out of the analytics route in v1.16. `server.js` dropped another 67 lines (2,946 → 2,879) — down 45% from the session-start ~5,252. **QA:** new `smoke-mission-route` (11 checks) — genuinely new HTTP-level coverage for the admin mission API, which had none before (only the agent PULL side was covered, via `smoke-agent-bridge`): the provider list carrying real destination URLs for the named AIs (Claude/ChatGPT/Grok/Gemini) while the catch-all `generic`/"Any AI" entry intentionally carries an empty one, the teach pack reporting a real module count, a full create → activate round-trip proving the mission actually persisted through `mission-store` rather than being echoed back, and both the blank-description 400 and unknown-id 404 error paths. A test-assertion bug (asserted every provider carries a non-empty URL; `generic` deliberately has none, since there's no single site to send the user to) was caught and fixed before the suite shipped. Full suite (86 scripts) verified green with the corrected exit-code check, run both before and after to isolate any regression to this change alone. `docs/ARCHITECTURE.md` updated: thirteen extractions done; the one remaining named cluster (the BYOK/AI copilot area — four sibling admin pages off one shared provider/key concern) should move as a considered group, not one page at a time. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.17-alpha | **Twelfth route-group extraction — `src/routes/import.js` — the import wizard, self-contained and small.** `/admin/import` (pick a source system, upload its export file) plus `POST /admin/api/import` (runs the file through `src/importers.js` and lands real `.pzn` pages) move out as one router — WordPress/Elementor is the one live adapter today; HubSpot/Camilyo/Mobeart/Builder.io render as "coming soon" placeholders, matching `docs/COMPETITIVE.md`'s own stated import roadmap. `server.js` dropped another 99 lines (3,041 → 2,946) — down 44% from the session-start ~5,252. **QA:** new `smoke-import-route` (8 checks) — a REAL WordPress WXR export (Gutenberg-block markup, a draft page, a non-content nav-menu item) run through the mounted router end-to-end, not a mocked call into the importer: Gutenberg heading/paragraph blocks land as real `.pzn` source, the draft and nav item are both correctly skipped (exactly 2 of 4 WXR items become pages), and re-importing the identical file a second time suffixes the new slugs rather than clobbering the first import's pages — verified by confirming the original page's content is untouched after the second run. A fixture bug (the WXR sample's slug collided with the `home` page the test's own setup fixture already creates, so two assertions were silently reading the WRONG page) was caught and fixed before the suite shipped. Full suite (85 scripts) verified green with the corrected exit-code check, run both before and after to isolate any regression to this change alone. `docs/ARCHITECTURE.md` updated: twelve extractions done; what remains (the BYOK/AI copilot cluster, the mission/BYOT admin API) still needs its own scoping pass. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.16-alpha | **Eleventh route-group extraction — `src/routes/analytics.js` — the reporting dashboard, split cleanly from its own public-facing data-collection endpoint.** The analytics dashboard page (inline-SVG per-day chart, top pages/referrers, device breakdown, form-conversion-by-page) and its CSV export move out together, matching the established page+API template — but `POST /_tapuz/collect`, the unauthenticated public beacon that actually WRITES pageviews, deliberately stays in `server.js`: same topic, different concern (write path vs. read/report path) and a materially different risk profile (public, rate-limited, DNT/GPC-respecting, bot-filtered) that shouldn't be dragged along for proximity alone. `gaData` (the GA Data API read-back status), referenced nowhere else in `server.js`, moved fully into the new route file — a genuine dead-import removal, not just a relocation; `analytics` itself stays a top-level `server.js` require since the collector endpoint still needs `isBot`/`recordPageview`. `server.js` dropped another 201 lines (3,242 → 3,041), the first time this session under 3,100 — down from a session-start ~5,252, a 42% reduction. **QA:** new `smoke-analytics-route` (10 checks) — seeded with REAL pageviews through the actual public collector endpoint rather than fixture rows written straight to the database, proving the collector → storage → dashboard pipeline still connects correctly end-to-end through the extraction: the seeded views/referrer surface in the rendered dashboard, an admin-path beacon is confirmed to never get recorded (the defense-in-depth guard surviving the move), the days-range selector and its invalid-value fallback both still work, and the CSV export carries the real seeded numbers. A test-assertion bug (assumed unquoted CSV cells; `csvTable` actually quotes every field) was caught and fixed before the suite shipped. Full suite (84 scripts) verified green with the corrected exit-code check, run both before and after to isolate any regression to this change alone. `docs/ARCHITECTURE.md` updated: eleven extractions done; what remains (the BYOK/AI copilot cluster, the import wizard, the mission/BYOT admin API) still needs its own scoping pass. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.15-alpha | **Tenth route-group extraction — `src/routes/theme.js` — found by re-scanning `server.js` after every previously named candidate had shipped.** With the pzn/builder API surface split closed in v1.14, `docs/ARCHITECTURE.md`'s "what's next" list was empty for the first time this session — rather than stop, the file itself was re-scanned for the next self-contained, low-risk piece: the theme settings API (colors/fonts/layout/logo overrides, export/import) plus the `/admin/theme` page, one cohesive page-and-its-API pair matching the `site-chrome.js`/`seo.js` template exactly. `server.js` dropped another 163 lines (3,405 → 3,242), the first time this session it's read under 3,300. A real dead-import cleanup rode along: `getThemeSettings`/`saveThemeSettings`/`loadOverrides`/`overridesToCss` were destructured at the top of `server.js` since early on, but once the route bodies moved out, only `LOOKS` (the setup wizard's own use, unrelated to this page) was still referenced anywhere — trimmed to just that, not carried forward as dead weight. **QA:** new `smoke-theme-route` (11 checks) — genuinely new HTTP-level coverage: the page rendering with its real script tags and looks-gallery mount point intact, a color-override round-trip through the mounted router that also proves an *unset* override field falls back to its default rather than being silently wiped (the merge-onto-defaults contract `saveOverrides` promises), and the export/import pair — a wrong-format package rejected with 400, a real one applied and immediately live on a follow-up GET. Full suite (83 scripts) verified green with the corrected exit-code check, run both before and after the extraction to isolate any regression to this change alone. `docs/ARCHITECTURE.md` updated: the mechanical "named candidate" list is exhausted; what remains (analytics reporting, the BYOK/AI copilot cluster, the import wizard, the mission/BYOT admin API) needs its own scoping pass rather than working off a checklist. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.14-alpha | **The page-builder/pzn API surface finishes splitting — `src/routes/pzn-pages.js`, the second and last sub-concern cut.** v1.13 pulled out every STATELESS pzn endpoint; this closes the gap `docs/ARCHITECTURE.md` named as the sole remaining candidate: the four routes that actually write a page file to disk — `source` (GET/POST), `decompile`, `create-from-source`, `ops` — the highest-risk cluster of the whole surface (real disk writes, slug-collision handling, async image ingestion, draft/publish divergence). `server.js`: 3,647 → 3,405 lines, dropping below 3,500 for the first time this session. The shared `looksLikePzn`/`pznSourceToBlocks` pipeline (pulled into `src/pzn-source.js` in v1.13) stays a real shared module — still required by the unrelated "advanced code tab" route that remains in `server.js`, so this cut, like the last, is a genuine extraction rather than a relocation. **QA:** new `smoke-pzn-pages-route` (24 checks) — genuinely NEW HTTP-level coverage for a cluster only ever exercised indirectly before. Checks include the actual draft/published divergence contract (an unpublished save must not touch the live copy; `publish:true` must converge them), the `loose:true` prose-extraction path, that a strict-save failure returns a usable repair suggestion WITHOUT mutating the existing draft, decompile correctly routing a BenTML-looking paste through the forgiving pipeline instead of the HTML shredder (the v0.69 fix), the v0.72 empty-placeholder-document guard surviving the move intact, and create-from-source's slug-collision 409. One test-fixture bug caught before the suite shipped: `ops`'s `insert` overrides use the module schema's `text` key for content, not an invented `html` key — the first draft guessed wrong and failed honestly. Full suite (81 scripts) verified green with the corrected exit-code check. `docs/ARCHITECTURE.md` updated: the page-builder/pzn API surface split is now complete — no more named modularity candidates remain in `server.js`. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.13-alpha | **The page-builder/pzn API surface starts splitting — the largest, most interconnected block finally gets its first cut.** `docs/ARCHITECTURE.md` had named this "probably split by sub-concern rather than moved wholesale"; this is that first sub-concern. `src/pzn-source.js`: `looksLikePzn` + `pznSourceToBlocks` (the ONE forgiving import pipeline every paste door shares) pulled into a real shared module — required both by the routes that moved out AND by the decompile route that stayed in `server.js`, so a genuine extraction, not just a relocation. `src/routes/pzn-tools.js`: every STATELESS pzn endpoint — repair (dry-run), graduate, to-blocks, toolbox, primer, preview — chosen specifically because none of them ever write a page file to disk; the mutating ones (`source`, `decompile`, `create-from-source`, `ops`) deliberately stay in `server.js` for a separate, higher-risk cut later. `server.js`: 3,702 → 3,647 lines. **QA:** new `smoke-pzn-tools-route` (11 checks) — genuinely NEW coverage; no prior smoke test exercised any of these six endpoints over real HTTP before (only their underlying logic, indirectly, via other tests) — including that `preview` compiles real HTML but creates zero pages (checked via a before/after page-count diff, not just a 200 status), that `to-blocks`/`repair` actually auto-correct a known BenTML alias rather than just accepting well-formed input, and that invalid input produces real validation errors through the mounted router, not a generic 500. A test-script assumption about the `toolbox` endpoint's response shape (assumed a flat array; it's actually `{categories: [...]}`) was caught and fixed before the suite shipped. Full suite (79 scripts) verified green with the corrected exit-code check. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.12-alpha | **Deal value + follow-up dates — the rest of "advanced CRM pipeline."** v1.00 shipped status + private notes; this closes the gap the hunt list kept naming. `src/db.js`: `value` (REAL, nullable) + `follow_up_at` (TEXT date, nullable) on `form_submissions`, same migration pattern as every prior column. `src/forms.js`: `setValue`/`setFollowUp` — both nullable-and-clearable as first-class actions (0 is a real small deal, not "unset"; clearing is `''`, not a separate endpoint), `listDueFollowUps()` (today-or-earlier, excludes already-closed won/lost leads, sorted soonest-first), `pipelineSummary()` (lead count + summed value per status, every `STATUSES` key always present even at zero). `/admin/inbox` gains a three-tile summary header (open pipeline value, won value, due-today/overdue count) and per-lead value + follow-up-date inputs that auto-save like status already did, plus an overdue/due-today badge on the lead row itself. Two new endpoints, `POST /admin/inbox/value` and `/admin/inbox/follow-up`, mirroring the existing `/admin/inbox/status` shape. CSV export gains both columns without disturbing existing column order (a checked invariant since v1.00). **QA:** new `smoke-crm-forecast` (25 checks: value set/clear/zero-is-valid/negative-rejected/non-numeric-rejected, date set/clear/malformed-rejected, the due-list's today/overdue/future/closed-exclusion logic, and `pipelineSummary`'s per-bucket isolation — verified via a genuinely fresh child process rather than an in-process cache-reset trick, after the first draft's `require.cache` deletion silently left `src/paths.js`'s cached `TAPUZ_ROOT` binding pointing at the wrong throwaway root and polluted the counts) and `smoke-crm-forecast-route` (7 checks, the two new endpoints plus the rendered summary tiles through a real server). Full suite (76 scripts) verified green with the corrected exit-code check. | Ben (direction: CMS/CRM north star — "CRM properties... leads from the published site") + Claude |
| v1.11-alpha | **The auth/CSRF gate itself extracted — `src/admin-gate.js`, the highest-stakes move this session.** Every prior route-group extraction moved a handful of specific paths; this one touches what runs on EVERY `/admin/*` request — rate limit → CSRF → session → role resolution, "the SINGLE place that gates the admin" per its own long-standing comment. The safety rule that kept it low-risk anyway: only the LOGIC moved, never the registration point — `server.js` still calls `app.use(require('./admin-gate').adminGate)` at the exact same line in the exact same position (after the static mounts, before every admin route); the delicate ordering relative to body-parsers and `express.static` was never touched, only where the ~55 lines of gate code physically live. `adminLimiter` (the per-IP flood-cap instance) moved into the new module too, since nothing else used it. **QA:** new `smoke-admin-gate` (16 checks, every one a security property rather than a feature): public routes fully untouched by the gate, the HTML-vs-JSON response-shape branch on an unauthenticated request (302 redirect vs. 401 JSON), CSRF rejection for a forged cross-origin `Origin` header AND for a request with no `Origin`/`Referer` at all (fail closed, not open), confirmation that GET requests are correctly CSRF-exempt while POST isn't, a real login issuing a real session, the gate's role resolution correctly feeding the downstream `requireAdmin` gate for both an admin (200) and an editor (403) hitting the same admin-only route, and the rate limiter actually tripping after 305 requests — which also proves it runs BEFORE the `/admin/login` session exemption, not after (the login screen itself is rate-limited too). A test-script bug — defaulting every request's `Accept` header to `application/json`, which silently broke the one check verifying the HTML-navigation redirect path — was caught and fixed before the suite shipped. Full suite (73 scripts) verified green with the corrected exit-code check. `docs/ARCHITECTURE.md` updated: the only remaining candidate is the page-builder/pzn API surface — the largest, most interconnected block, likely split by sub-concern rather than moved wholesale. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.10-alpha | **Ninth route-group extraction — `src/routes/media.js` — the last named same-shaped candidate.** Folder CRUD, upload, the legacy root-scope `/admin/assets` list, and the library browser page — three non-contiguous blocks in the original file (media CRUD near the top, upload/assets nearby, the big library UI page far below), unified into one route module. A fully dead, already-commented-out `/admin/upload-legacy` block ("unused after v0.31" per its own comment) was dropped rather than relocated — moving confirmed dead code forward serves nobody. `server.js` dropped another 223 lines (3,974 → 3,751), the ninth extraction in a row on the same template. **QA:** new `smoke-media-route` (11 checks) exercises a REAL upload — a real magic-byte-valid 1×1 PNG through `src/media.js`'s `saveBase64`, not a placeholder string — plus folder create/move/delete and the root-vs-subfolder scoping distinction between `/admin/media` and the legacy `/admin/assets`. The first draft of this test assumed `/admin/assets` was a recursive all-folders listing; it isn't (root-scope only, same as `/admin/media` with no folder param) — caught by the test failing honestly and fixed before it shipped. Full suite (72 scripts) verified green with the corrected exit-code check. **Every named same-shaped standalone-admin-page candidate is now done** — nine extractions, one template. `docs/ARCHITECTURE.md` updated: what's left (the auth/CSRF gate, the pzn/builder API surface) is genuine design risk, not mechanical work. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.09-alpha | **Seventh and eighth route-group extractions — `src/routes/storage.js` + `src/routes/categories.js` — server.js crosses below 4,000 lines.** Two more small, independent admin pages (the "content is real files on disk" inventory screen; the category taxonomy editor) moved out together: `server.js` drops from 4,136 to 3,974 lines — the first time this session it's read under 4k. Both extractions were mechanical at this point (eight in a row on the same template) — the only judgment call was keeping them as two separate route files rather than one, since they're two distinct admin-nav entries with no shared state, matching the one-file-per-admin-page convention every prior extraction has followed. **QA:** new `smoke-storage-route` (6 checks) and `smoke-categories-route` (5 checks), both booting a real server and proving real data round-trips through the mounted routers — not just that the pages return 200. Full suite (69 scripts) verified green with the corrected exit-code check. `docs/ARCHITECTURE.md` updated: media library is the last named same-shaped candidate; after that, what remains is the auth/CSRF gate and the pzn/builder API surface — design risk, not mechanical work. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.08-alpha | **Multilingual pairing — gap #5 on the `docs/COMPETITIVE.md` hunt list, "a fraction of WPML's surface" by design.** No per-string translation engine, no URL-prefix routing/locale framework — just linking two EXISTING pages as translations of each other. `src/pages.js`: `linkTranslations(pathA, langA, pathB, langB)` stores a shared opaque `meta.translationGroup` + per-page `meta.lang` (existing meta fields are preserved, never wiped — `updatePage`'s `meta` write replaces the whole object, so every setter here spreads the old meta first); linking a third page into an existing pair reuses the group instead of forking it. `getTranslations(full_path)` finds siblings; `unlinkTranslation` drops a page out. `src/seo.js` gains `buildHreflangTags` — one `<link rel="alternate" hreflang>` per sibling (self included, Google's own guidance), deliberately empty for a lone page or a missing `baseUrl` (a half-set of hreflang tags is worse than none — implies siblings that don't resolve). `src/renderer.js` gains `renderLangSwitcher` — a small zero-JS floating pill of language links, wired into the same `{{site_extras}}` slot as WhatsApp/search, appearing ONLY on pages that actually have linked siblings (linking two pages IS the opt-in — no separate site-wide toggle to forget, matching the "not just read the label" north star). New `/admin/translations` (own admin page, `src/routes/translations.js`, built directly as a route module rather than inline-then-extracted — the pattern is proven six times over now): lists existing translation groups, a two-page-picker link form, per-page unlink. **A real UI bug caught by the route-level smoke test:** after unlinking one page from a pair, the survivor still carried its old `translationGroup` value (harmless data-wise — `getTranslations` already correctly returns `[]` for it) but the admin listing naively grouped by raw group-id presence, so a solo leftover page kept showing as a phantom "group of one." Fixed by filtering the listing to groups with 2+ members — the data model was fine, only the display logic needed the fix. **QA:** two new suites — `smoke-translations` (27 checks: symmetric linking, group-reuse on a third page, meta-preservation, self-link/missing-page rejection, unlink, the hreflang pure-function contract incl. the half-set and no-baseUrl guards, the switcher's 0/1-entry no-op, and a full `renderPage` integration check distinguishing the actual `<div class="tapuz-lang-switch">` element from the theme stylesheet's own mention of that class name — the false positive the first draft of this test fell into) and `smoke-translations-route` (6 checks, the admin page + API through a real mounted server, including the group-of-one display-bug regression). Full suite (65 scripts) verified green with the corrected exit-code check. | Ben (direction: CMS/CRM north star + `docs/COMPETITIVE.md` gap #5) + Claude |
| v1.07-alpha | **Sixth route-group extraction — `src/routes/menus.js`.** Menu entities (name → nestable items) + the header/footer location map, moved out of `server.js` (4,170 → 4,136 lines). The editor's own client logic already lived in a pre-existing external `public/admin-menus.js` (same pattern as `admin-theme.js`), so this move was routes + page shell only — and a genuine dead-code cleanup: `menusLib`/`loadMenus`/`saveMenus`/`saveMenu` had zero remaining uses in `server.js` once the routes left, so those top-level requires were deleted rather than left as unused imports. **QA:** new `smoke-menus-route` (9 checks) through a real server: create a menu with a nested child item, assign it to the footer location, delete it, and confirm the sibling `main` menu (seeded by the setup wizard) survives untouched — the full lifecycle through the mounted router, not just a syntax check. Full suite (61 scripts) verified green with the corrected non-piped exit-code check. Six extractions in (team, integrations+notify, seo, sitemap, site-chrome, menus) — `docs/ARCHITECTURE.md` names media/storage/categories as the remaining same-shaped candidates before the harder auth/CSRF gate and the pzn/builder API surface. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.06-alpha | **Site packages — ".pzn is our RPM" one level up from theme packages (v0.99): a whole site, not just a theme.** New `src/site-package.js`: `exportSitePackage(name)` bundles every page's canonical `.pzn` source (draft AND published, when they differ) + theme overrides + menus + a hand-picked non-secret config subset (title/description/language/logo/seo/homepage — never `admin.path`, never any of the gitignored key files) into one self-describing, versioned package, same `{format, version, ...}` shape as theme packages. `importSitePackage(pkg, {overwrite})`: same format/version validation, and — the safety invariant that matters — **a colliding page is skipped and reported by default, never silently clobbered**; `overwrite:true` opts in explicitly. New `GET /admin/api/site-package/export` / `POST /admin/api/site-package/import`, both `requireAdmin` (import creates/overwrites pages and site config — real power, unlike theme editing). UI on `/admin/settings`, same download-link + paste-and-apply pattern as the theme page. **A real ordering bug caught by the smoke test before it shipped:** `savePageSource(path, source, {publish:true})` writes the SAME source to both the draft AND published files (it's the "save+publish" primitive) — importing published-then-draft naively would have collapsed a page's unpublished draft edits into its published state, silently losing exactly the "I have changes not live yet" scenario the draft/publish split exists to protect. Fixed by publishing first (establishes the live snapshot) then writing the draft separately (diverges it back) — the two can end up genuinely different again, as they should. **QA:** new `smoke-site-package` (26 checks) builds a real 3-page site including one page with unpublished draft changes sitting on top of a published version, exports it, wipes the site, imports into empty — and specifically asserts the restored draft has the unpublished edit WHILE the restored published copy is still the old live text (the exact bug the ordering fix addresses), plus the full collision-safety contract (skip by default / update opt-in) and four rejection cases for malformed packages. Full suite (60 scripts) verified green with the corrected non-piped exit-code check. | Ben (direction: CMS/CRM north star — ".pzn, our RPM file") + Claude |
| v1.05-alpha | **The pricing-table module (module-hunt gap #7) — and two real regressions caught along the way by finally running the smoke suite correctly.** New `pricing`/`plan` module, full cross-layer recipe: `src/pzn/pricing-html.js` (shared renderer), `src/pzn/modules/registry.js` (the canonical `.pzn` system), the bridge (`src/pzn/bridge/tapuz-json.js`), `renderer.js`, `src/block-registry.js` (schema-driven side panel — no custom UI code needed), `src/blocks.js`, `public/admin-builder.js` (canvas preview), theme CSS (source, `themes/default/css/main.css`) — **and, matching every other module this dialect covers, the LEGACY BentML keyword compiler too** (`src/bentml/keywords.js` + `compile.js` + `decompile.js`), not just the canonical system. **A real bug caught mid-build:** the legacy dialect lowercases every param name (`src/bentml/parse.js:549`) and has no camelCase param anywhere in its vocabulary — `ctaLabel`/`ctaUrl` would have silently become unreachable `cp.ctalabel`/`cp.ctaurl` in the compiler; renamed to the dialect's own single-lowercase-word convention (`cta`/`url`, matching how every other module names its link param), mapped to the canonical `ctaLabel`/`ctaUrl` in the compile/decompile bridge. **Two pre-existing regressions found and fixed, from turns earlier this session that had been reporting green due to a verification blind spot** (`npm run test:smoke \| tail -N` reports `tail`'s exit code, not `npm`'s — a failing script mid-chain was invisible; every "full suite green" claim since was checking the wrong exit code): (1) `themes/default/css/main.css`'s v0.98 search-widget CSS used literal `background: #fff;` on `.tapuz-search-float`/`.tapuz-search-box` instead of `var(--color-surface, #fff)`, breaking `smoke-theme.js`'s "no bare white panels" check silently since v0.98 — fixed. (2) `scripts/smoke-palette.js` asserted on `src/server.js`'s raw source text for `layout()`/`adminNav()`/`ADMIN_NAV_GROUPS`/`paletteBootJson()` — code the v0.96 `admin-ui.js` extraction had moved out of that file — silently broken since v0.96; updated to read `src/admin-ui.js` for those checks. **QA:** new `smoke-pricing` (32 checks spanning both dialects: `.pzn` round-trip, compile/render parity, security — escaping + `javascript:` neutralization, empty/edge cases, AND the full legacy-dialect compile→decompile→recompile round-trip); full suite (58 scripts) verified green with a corrected, non-piped exit-code check this time — confirmed by actually reaching the last script in the chain, not by trusting a wrapper's exit status. | Ben (direction: CMS/CRM north star — module hunt gap #7) + Claude |
| v1.04-alpha | **Fifth route-group extraction — `src/routes/site-chrome.js`, the largest single-page one yet.** Header (tagline/logo/sticky/CTA) + footer (text/credit/link-columns/social-links) — the site-wide chrome every page renders — moved out of `server.js`. Biggest single cut of the modularity series: 4,424 → 4,170 lines. **QA:** new `smoke-site-chrome-route` (10 checks) goes past a round-trip check into the actual complexity this page has — the filter/dedupe logic on save: a fully-empty footer column is dropped, a blank link *inside* a real column is dropped while the real link survives, a social row with no URL is dropped, `sticky:false` round-trips instead of silently defaulting true — plus a partial-update check (saving only `header` must never wipe a previously-saved `footer`, the classic shallow-merge bug this endpoint's prev-preserving pattern is built to avoid). Full suite (56 scripts) green. Five extractions in (team, integrations+notify, seo, sitemap, site-chrome) — `docs/ARCHITECTURE.md` names media/storage/categories/menus as the remaining same-shaped candidates before the harder auth/CSRF gate and the pzn/builder API surface. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.03-alpha | **Fourth route-group extraction — `src/routes/sitemap.js`, the simplest yet.** One read-only GET, no API endpoint, structure entirely derived from `src/sitemap.js`'s `buildSitemap()` — nothing to round-trip, nothing to validate. `server.js` drops another ~70 lines (4,490 → 4,424). **QA:** new `smoke-sitemap-route` (7 checks) earns its keep past a trivial 200-check: creates a published page never added to any menu and a draft page likewise unlinked, then asserts the mounted router's rendered HTML actually gets the structure right — the menu section links the home page correctly, the orphan section surfaces the unlinked published page with a "פורסם" badge and the unlinked draft with a "טיוטה" badge (not the other way around), and an unauthenticated request never leaks the page. Full suite (54 scripts) green. Four route-group extractions in (v0.97 team, v1.01 integrations+notify, v1.02 seo, v1.03 sitemap) — `docs/ARCHITECTURE.md` names menus/site-chrome/media/storage/categories as the remaining same-shaped candidates. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.02-alpha | **Third route-group extraction — `src/routes/seo.js`, the smallest yet.** Site-wide SEO defaults (title pattern, default description/og:image, base URL) moved out of `server.js` (~90 lines; another small cut: 4,577 → 4,490). Proves the `routes/team.js`/`routes/integrations.js` template scales down as cleanly as it scales up — three extractions in, this is now a template to repeat, not a design problem to resolve each time. **QA:** new `smoke-seo-routes` (9 checks) boots a real server and proves the mounted router works end-to-end, including the one real piece of logic in this page — baseUrl validation: a schemeless domain (`example.co.il`) gets `https://` auto-prefixed, garbage input (`"not a url at all!!"`) is rejected with 400 and leaves the previously-saved value untouched (not silently corrupted), and an explicit empty string clears the field without being treated as an invalid URL. Full suite (52 scripts) green. `docs/ARCHITECTURE.md` updated: sitemap/menus/site-chrome/media/storage/categories are the remaining same-shaped candidates; the auth/CSRF gate and the pzn/builder API surface stay deliberately last (higher risk, more interconnected). | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.01-alpha | **Second route-group extraction — `src/routes/integrations.js`, following the v0.97 template exactly.** Every `/admin/integrations*` route (WhatsApp float, site search toggle, GA4/first-party analytics/the disabled GA Data API skeleton) AND `/admin/api/notify/*` (shown on the same admin page — natural to move together) moved out of `server.js` into a mounted Express `Router`. `server.js` drops another ~160 lines (4,736 → 4,577); two blocks collapse to `app.use(require('./routes/integrations'))`. **QA:** new `smoke-integrations-routes` (10 checks, boots a REAL server) proves the extraction works as *wired*: an editor genuinely gets 403 from both the page and the notify API through real HTTP, an admin's WhatsApp/search/GA4 settings genuinely round-trip through the mounted router, and notify settings save + never echo the password back over the wire — the same "prove it through HTTP, not just unit tests" standard `smoke-team-routes` set. Full suite (50 scripts) green. `docs/ARCHITECTURE.md` updated: the auth/CSRF gate and the remaining standalone admin pages (media, storage, categories, SEO, sitemap, menus, site-chrome) are next — each now a template to repeat, not a design problem to resolve. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v1.00-alpha | **The lead pipeline — a form submission finally graduates into a worked lead.** v0.81 built the inbox and v0.94 added email notification, but a lead just sat there — no status, no way to note "I called them." This is the missing CRM half of "CMS/CRM." **`src/db.js`**: `status TEXT DEFAULT 'new'` + `notes TEXT DEFAULT ''` on `form_submissions`, added via the existing `hasColumn`/`ALTER TABLE` migration pattern (same one `draft_blocks` used) — a fresh install and an upgrading install both end up with every row reading `status='new'`, zero migration surprises. **`src/forms.js`**: five stages (`new → contacted → qualified → won/lost`) — deliberately small, an SMB flow, not a deal-stage kanban nobody asked for. `setStatus(id, status)` rejects unrecognized values without a partial write; `setNotes(id, notes)` caps at 4000 chars (matches the existing field-value cap). `toCsv` gains `status`/`notes` columns after the existing fixed columns (backward-compatible — the CSV `smoke-forms-inbox` regression check on column order still passes unmodified). **`/admin/inbox`**: every lead shows a colored status pill in its summary row, an inline status `<select>` (auto-saves on change) and a private notes textarea (save button) inside the expanded view, plus a status filter dropdown (`?status=` query) at the top. Two new endpoints, `POST /admin/inbox/status` and `/admin/inbox/notes`, mirroring the existing `/admin/inbox/read` request shape exactly. **QA:** new `smoke-crm-pipeline` (21 checks: new-submission defaults, the full valid-transition sequence, an unrecognized-status write rejected without corrupting the existing value, notes save/clear/truncate/null-safety, the CSV export carrying real pipeline data with existing columns unmoved, AND a legacy-row check — a submission inserted with the raw pre-v1.00 shape still reads `status='new'` off the column default, proving the migration invariant, not just the fresh-install default) joined `test:smoke`; full suite (48 scripts) green, including a targeted re-run of `smoke-forms-inbox` to confirm zero CSV-column regression. | Ben (direction: CMS/CRM north star — "CRM properties... leads from the published site") + Claude |
| v0.99-alpha | **Theme packages — the first concrete step of "`.pzn` is our RPM" for themes, not just pages.** The North Star names a portable container for "sites, themes, plugins" as its Red Hat move; nothing shared a theme between installs yet. **Deliberately its own small format, not raw `.pzn`/BenTML** (page-markup shaped, wrong tool for a settings object) — unifying site/theme/plugin packages under one container is a real design decision for its own future version, not something to redecide unilaterally mid-session; this ships theme sharing something concrete today instead of waiting on that decision. `src/theme.js` gains `exportThemePackage(name)` (a self-describing `{format:'tapuz-theme', version, name, exportedAt, overrides}` object — pure, no I/O) and `importThemePackage(pkg)` (validates format tag + version ceiling + overrides shape before ever touching disk; a malformed or foreign JSON throws a clear Hebrew error and the live theme is untouched — `saveOverrides`'s existing `mergeDeep` onto `DEFAULT_OVERRIDES`'s known shape means even a validated package can't inject arbitrary config keys). New `GET /admin/api/theme/export` (downloads the package as a dated `.json` file) and `POST /admin/api/theme/import`; deliberately NOT admin-gated — theme editing itself is already open to editors, so restricting only the package form would add no real protection. New "📦 ייצוא / ייבוא ערכת נושא" section on `/admin/theme` (download link + paste-and-apply, `public/admin-theme.js`). **QA:** new `smoke-theme-package` (18 checks: package shape, name/timestamp correctness, a full customize→export→reset→import round-trip proving the exact overridden values come back — not just that import "succeeds", and six rejection cases incl. null/string/no-format/wrong-format/future-version/no-overrides, each asserting the live theme was left untouched) joined `test:smoke`; full suite (46 scripts) green. | Ben (direction: CMS/CRM north star — "`.pzn`, our RPM file") + Claude |
| v0.98-alpha | **Published-site search — gap #4 on the `docs/COMPETITIVE.md` hunt list, and the one deliberate exception to "zero-JS published pages."** A static export has no way to search itself without SOME client-side code; the design keeps everything else's zero-runtime discipline and isolates the JS to this one feature. **The index**: `pages.js` gains `listSearchable()` — every published page as `{title, url, excerpt}`, excerpt = `meta.description`/`meta.teaser` if the author set one, else the same auto-teaser-extraction `listArticles` already uses (`firstTeaser`, reused not duplicated). **The write path**: `export.js`'s `exportAll()` writes `search-index.json` into the export root — but ONLY when `config.integrations.search.enabled` (checked once per export); toggled off, any stale index from before is deleted rather than left answering searches nobody can reach from the UI. No new server route needed — `express.static` already serves anything under the export root, so a live-served site and a fully static Netlify/S3 export both work identically. **The widget**: `renderer.js` gains `renderSearchWidget()`, wired into the same `siteExtras` slot as the WhatsApp float and gated the identical way (`config.integrations.search`, returns `''` when off) — a floating button opens an overlay, fetches the index once, filters title+excerpt client-side (plain substring, not fuzzy — good enough at small-SMB page counts), Escape/backdrop-click to close. Styled in `themes/default/css/main.css` (the SOURCE, not the build output) with the same `inset-inline-*` logical-CSS convention as the WhatsApp float. **Admin UI**: a new "🔎 חיפוש באתר" section on `/admin/integrations`, one checkbox, reusing the existing `POST /admin/api/integrations` endpoint (extended, not a new endpoint). **QA:** new `smoke-search` (16 checks: the index contract incl. draft-exclusion and the description-over-auto-teaser priority, widget gating incl. malformed/absent config never throwing, and — the part that actually proves the feature works — `exportAll()` really writing the file when enabled, really omitting it when disabled, and really cleaning up a stale one on toggle-off) joined `test:smoke`; full suite (44 scripts) green. | Ben (direction: CMS/CRM north star + `docs/COMPETITIVE.md` gap #4) + Claude |
| v0.97-alpha | **The first route-group leaves `server.js` — `docs/ARCHITECTURE.md`'s plan continues.** Two more shared pieces extracted (`src/http-util.js`: `clientIp`/`wantsJson`/`isStateChanging`, the rate-limit/CSRF/content-negotiation helpers; `src/admin-guard.js`: `requireAdmin`, the v0.95 role gate) — both zero/near-zero dependency, both needed by any route module before routes themselves could move. Then the actual milestone: **`src/routes/team.js`** — every `/admin/team*` route (the v0.95 feature) as a mounted Express `Router`; `server.js` shrinks that whole block to one line, `app.use(require('./routes/team'))`. This is the template for every future route-group extraction the plan calls for. **QA:** new `smoke-team-routes` (10 checks) boots a REAL server and proves the extraction works as *wired*, not just as functions — an editor genuinely gets 403 from `/admin/team` and its API through real HTTP, an admin genuinely gets 200 and can run the full invite/promote/remove lifecycle, self-protection holds via the API even bypassing the UI, and an editor keeps access to an ungated route (the pages API) — joined `test:smoke` alongside the existing `smoke-team` (the `auth.js` data-layer contract). Full suite (42 scripts) run three times across this extraction (after the helper split, after the route move, after registering the new smoke) — green every time. `docs/ARCHITECTURE.md` updated with what moved and the next two candidates (notify/integrations route groups, using the exact same template). | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v0.96-alpha | **The monolith starts breaking up — `docs/ARCHITECTURE.md` + the first extraction out of `src/server.js`.** The North Star's own words ("modular," "everyone to enjoy editing this code as professionals") were false of the codebase itself: `server.js` had grown to ~5,000 lines, every route/template/helper inline. Rather than a risky big-bang rewrite of a live site's server, a documented, repeatable pattern: extract one self-contained piece, verify the FULL smoke suite before and after, give the extraction its own smoke test — small, reversible, always green. **First extraction: `src/admin-ui.js`** — the admin UI shell (`layout()`, `adminNav()`, `ADMIN_NAV_GROUPS`/`ADMIN_ACCENTS`/`accentFor`, `paletteBootJson()`, `escapeAdmin()`), pure rendering with a single dependency (`./auth`, for the logout form's `getAdminBase()`). `server.js` now imports these instead of defining them inline — roughly 190 lines of shell code left the file for a two-line import, the file's first real seam. **QA:** new `smoke-admin-ui` (18 checks: escape correctness, nav-groups/accent derivation incl. the v0.95 team item surviving the move, `adminNav`/`layout` rendering, palette memoization) joined `test:smoke`; full suite (now 40+ scripts) run twice through the extraction — green both times, zero regressions. `docs/ARCHITECTURE.md` documents the pattern and a prioritized list of what's next (route groups with few cross-cutting deps first, the auth/CSRF gate later, the page-builder API surface last) — not a commitment to finish in one pass, each future extraction lands as its own version like every other feature here. | Ben (direction: CMS/CRM north star — "modular," "professional," "we set standard") + Claude |
| v0.95-alpha | **User roles — more than one admin account (gap #2 on the `docs/COMPETITIVE.md` hunt list, WP/Drupal parity for the agency tier).** Every account in `auth.json` now carries a `role`: `admin` (everything) or `editor` (content/media/pages/menus/forms/analytics — the day-to-day work, no settings or credentials). `auth.js` gains the team lifecycle: `addTeamMember`, `setUserRole`, `removeUser`, `listUsers`, `findUserById`, `roleOf` — and the invariant that matters most, **a site can never lock itself out of its own admin surface**: demoting or removing the last remaining admin throws, checked server-side on every call, not just in the UI. `verifyLogin`/`findUserById` now surface the role; a role-less account already on disk (every pre-v0.95 install, incl. `seed:admin`) reads as `admin` via `roleOf()` — nobody's access silently narrows on upgrade. **New `requireAdmin` middleware**, applied to the security-sensitive surface: `/admin/integrations` (+ its API — SMTP/GA/WhatsApp config), `/admin/api/ai/settings` (the AI provider key), `/admin/agent` + `/admin/api/agent-tokens*` (bearer tokens = full site access), `/admin/settings` (site config incl. the admin path), and the new team page itself; an editor hitting any of these gets a clean 403, never a silent partial page. **New `/admin/team`** (admin-only, added to the "מערכת" nav group): a table of every account with an inline role selector and remove button, plus an invite form (username/password/role) — the self-row can't change its own role or remove itself (both blocked in the UI and re-checked server-side). **QA:** new `smoke-team` (22 checks: legacy-account role defaulting, the full invite/promote/demote/remove lifecycle, the last-admin-standing guard from both the demote and remove directions, password hashes never leaking through `listUsers`) joined `test:smoke`; full suite green, zero regressions (no existing smoke touched the newly-gated routes, and every account those smokes DO create still resolves to `admin` under the new `roleOf()` default). Not done: hiding admin-only nav links from an editor's sidebar (correct 403 today, just not a polished dead-end) and a finer editor/author split for the agency tier — noted as follow-ups in the gap map. | Ben (direction: CMS/CRM north star + `docs/COMPETITIVE.md` gap #2) + Claude |
| v0.94-alpha | **Email notification on new lead — the forms inbox stops being silent (top of the `docs/COMPETITIVE.md` gap hunt list).** Ben's field observation: leads only surface when someone opens `/admin/inbox`; WordPress notifies via `wp_mail` out of the box, we didn't. **BYO-SMTP, no shared relay:** new `/admin/integrations` section ("📧 התראת אימייל על פנייה חדשה") — recipient, from, SMTP host/port/secure/user/password — mirrors the `ai.js` key-storage pattern: credentials live in gitignored `config/notify.json`, `getSettings()` NEVER echoes the password (`hasPass` boolean + tail-free), `saveSettings` patch semantics (`undefined`=keep, `''`=clear, value=replace). New `src/notify.js`: a pure `buildMessage()` (unit-testable, no I/O) plus `sendLeadNotification()` via `nodemailer`, wired into `POST /api/form` right after `saveSubmission` — **fire-and-forget**, wrapped so a misconfigured or unreachable SMTP server can never delay or break form capture (the submission is already durably saved before the notify call fires). A "שלח בדיקה" test-send button in the admin UI hits `POST /admin/api/notify/test`. **QA:** new `smoke-notify` (26 checks: settings CRUD incl. password-never-echoed + undefined/empty/value semantics, the `isConfigured` gate, the pure message builder incl. empty-fields degrade-gracefully, and the send path with an injected fake transport — both the success case and a thrown-transport-error case caught cleanly) joined `test:smoke`; full suite green. Not built: choosing/hosting SMTP for the user (bring-your-own, matches the no-vendor-lock-in North Star) — a future "we host it for you" tier is a separate, explicit product decision. | Ben (direction: CMS/CRM north star + `docs/COMPETITIVE.md` gap #1) + Claude |
| v0.69-alpha | **"No page to observe" — the publish loop closes (critical field report from Ben).** Ben pasted a ChatGPT page, the builder showed it, publish "worked" — and the URL answered nothing, Hebrew slug or English. Four real breaks found by walking his exact path: **(1) publish never exported** — `/admin/publish` updated the DB/.pzn but only the separate "build" step wrote `public/*.html`; a published page had NO live file. Publish now runs `exportAll()` — **publish MEANS live** — and returns `liveUrl`. **(2) The natural URL had no route** — `express.static` served only `/שם.html`; `/שם` (what a human types, what the admin implies) 404'd for EVERY page, Hebrew and English alike. `extensions:['html']` on both static mounts. **(3) The paste doors spoke the wrong language** — the advanced code tab compiled only the legacy keyword dialect and the `/admin/new` import fold fed everything to the HTML decompiler, so the `<bent-*>` reply our own dictionary teaches ChatGPT to write was REJECTED in one door and shredded to provisional blobs in the other. One shared forgiving pipeline (`pznSourceToBlocks`: extract → parse | repair → toTapuzPage) now backs all three doors; both endpoints auto-detect the dialect (`looksLikePzn`), so paste ANYTHING anywhere and the server figures it out. **(4) Prop drift silently dropped links** — a model writing `url=` for `href=` lost the button's destination even on a VALID doc (repair only ran on parse failure). New `adoptPropTwins` in `repair.js` (url/href, src/image, text/content…) runs on every paste — the tag-ALIASES move, extended to props. **UX:** the builder now keeps a permanent **"צפה בדף החי ↗"** button (appears on load for published pages, updates on publish, URL-encoded for Hebrew), and "פרסם + בנה" opens THE page, not the homepage. **QA:** new `smoke-publish-live` (10 checks, boots a REAL server on a throwaway root: extensionless URLs, login, bent-*-paste → draft with a HEBREW slug and zero shredding, publish → liveUrl → GET the Hebrew URL 200 with content, advanced-tab dialect acceptance, url=-drift adoption) joined `test:smoke`; suite green at 27 smokes + 98 pzn tests; browser-verified — the builder shows the live-door link for a published page, encoded `/נסיון` answers 200. (Also noted: dev login is still admin/admin — [[tapuz-auth-ops]] says change it.) | Ben (field report: "critical, no page to observe") + Claude |
| v0.68-alpha | **The brains merge — grok-lab decompiler branch 2 harvested into Tapuz (rebuilt, never copied).** Ben: "we made decompiler branch 2 [in the lab] using both brains — we should have the best decompiler around." Read `C:\Dev\grokTapuziel` commit 599c69c ("Decompile fidelity + OCR + universal module overrides") against our v0.67 and rebuilt every idea that beats us: **(1) CDN-safe srcset** — CDN URLs carry bare commas (`f_auto,q_auto,w_500/…`), so srcset is now read by width descriptors and picks the LARGEST candidate (our old split-on-comma truncated real walla/wcdn URLs — bug-grade); **(2) case-blind attrs** (`attrOf`) — React DOM dumps say `srcSet`/`data-Original`; plus `data-bg`/`data-image`/`data-url` variants and protocol-relative `//cdn` → `https:`; **(3) teaser links** — a link wrapping a picture/headline is a CARD, not a button: `anchorCard` reads the anchor's children so **the picture survives** (v0.67 kept only title+href), and adjacent single-item cards FUSE into one wall; **(4) flat learns to skip** — the flat walk never skipped `<script>`: walla's 356KB `window.loadDataState` dump became a "provisional" blob that **gamed the safety-guard scorer** (raw html content counted as coverage → the blob read WON the strategy race). Fixed both ends: flat skips script/style/svg/template noise + descends SPA custom elements (their content graduates instead of blobbing), and `blockTextLen` no longer counts provisional html — preserved ≠ READ; **(5) leftover diet** — `<q>`/`<cite>` → quote, texty bare `<button>` → button (textless = chrome, dropped), textless links fall back to `aria-label`/`title` for a name. **Walla rendered-DOM scoreboard (v0.67 → v0.68): 180 → 112 blocks, 3 → 0 leftovers, 78 → 33 buttons, 105 → 132 real images, ingest 13 → 28 found (24 saved webp); BOTH strategies now read walla with zero provisional leftovers.** Deliberately NOT ported: OCR (tesseract heb+eng text-from-photos — heavy dep, its own version if wanted), universal `override=` module params (builder-war feature, separate track), lab's table module (Tapuz table needs the 7-file recipe through renderer.js/block-registry.js/theme css — all owned by a concurrent session's uncommitted work; queued next). **QA:** 9 new smoke-decompile checks (CDN srcset, camelCase srcSet, protocol-relative, teaser card keeps picture, teaser fuse, script-skip regression, aria-label name, q/cite + bare-button, textless-button drop) + smoke-graduate updated (custom elements descend); full suite green (26 smokes, 98 pzn tests). **Field note (Ben):** the extension's inject freezes ChatGPT ("alerts the llm and it gets afraid") — the built-in CMS copy-dictionary + paste flow works well; lead with built-in tools for ChatGPT. | Ben (direction + lab build with grok) + Claude (rebuild) |
| v0.67-alpha | **The decompiler SEES the page — image sight, menu sight, and the pictures become OURS (webp, self-hosted).** Ben: "picture a page and compare to the decompiled version… we implement the images (probably at webp), we host it in our server, the decompiled file should look similar to the page itself." The comparison harness told the story brutally: walla's homepage (679KB, 74 images) decompiled to **ONE button**. Root cause #1 — **the page-eater** (`extractBodyHtml`): no `<main>` → first-`<article>` fallback grabbed one news item out of 111 and threw away the page; an `<article>` is now the page only when it IS the page (single + holds ≥50% of body text), a tiny `<main>` (SPA shell) falls back to body. Root cause #2 — **the decompiler was image-blind the way 2010 sites were built, and real sites are built like 2026**: the true URL hides in `srcset`/`data-src` (`imageSrcOf`), in inline `background-image`, and — the walla lesson — in **emotion/CSS-in-JS `<style>` blocks** (`classBgMap`: class → background URL, threaded through cards/hero/wrappers). Root cause #3 — **menus decompiled as garbage**: `ul>li>a` menus became text lists (hrefs LOST), wrapper-per-link menus became 85 stacked primary buttons, textless icon links became nameless "קישור" buttons. Now: link-runs + wrapper-blind `coalesceButtonRuns` → **nav**; ≥3 consecutive headline-length links → a **cards** wall (walla's headline stacks ARE article walls); a textless picture-link keeps its picture, never a nameless button. **URLs become absolute** against the fetch URL or (pasted/saved HTML) the page's own `<base>`/canonical/og:url. **The ingest pipeline** (`src/media-ingest.js` + `media.saveBuffer`): every remote image the blocks reference is downloaded (same SSRF guard as the decompiler, 8MB cap, 4-way polite concurrency), converted to **webp** via sharp (animated kept; SVG stays SVG, sanitized), saved through the media library's magic-byte gate into `/assets/imported/<slug>/`, and the blocks rewritten to local paths — wired into `POST /admin/api/pzn/decompile` (on by default, `assets:false` skips, report in the response). **The measured arc on walla's rendered DOM (1.5MB):** 1 button → 265 blocks → **180 blocks: 39 card walls with pictures, 5 navs, form, 15 embeds, 78 buttons; 105 images — 98 rewritten to `/assets/`, 29/30 sampled render from OUR server as webp**. Known truth documented: walla's *fetched* HTML carries ZERO content photos (XHR-hydrated) — the rendered DOM (extension scrape / saved page) is the real input for portals; server-rendered sites work straight from the URL. **QA:** 14 new smoke-decompile checks (the page-eater fix both directions, srcset/data-src sight, emotion bg → card picture, ul→nav with hrefs, wrapped-link coalesce, headline-stack→cards, icon-link picture survival, canonical base absolutization, imageRefs find+rewrite through nested trees); full suite green (26 smokes, 98 pzn tests). New dep: sharp. | Ben (direction: "the decompiled file should look similar to the page") + Claude |
| v0.66-alpha | **Decompiler V2 — the structure hunt + the safety guard (idea matured in the grok lab, rebuilt here).** Ben: "a set of them decompiling sites… a safety guard to always produce the highest quality"; "the page builder should swim with the idea of separating the page to few halves… the cut in percentage." Real pages are not text-photo-text — they are halves, heroes, card walls. **The hunt** (`src/pzn/hunt.js`): a second decompile strategy that reads the SHAPE — row/grid wrappers with 2–4 column children become a `columns` block with a **percentage cut** (bootstrap `col-md-8/4` → `67:33`, inline `width:70%` → `70:30`, else an equal split summing to 100); hero-classed sections that pass a shape test (slot-sized: 1–2 headings, ≤2 paragraphs, ≤3 links) collapse to ONE `hero` block — stripe's five-deep hero-in-hero markup can no longer eat a page; responsive twins (the same heading/nav rendered for desktop AND mobile) dedupe per container level; empty markup residue drops; `<picture>` → image; card clusters/forms/navs/videos map through the SAME shared parsers as v1 (extracted in `graduate.js`), so nothing regresses. **The safety guard** (`decompile.js`): a SET of strategies — flat v1 + hunt v2 — reads every page; a quality scorer (content coverage 50pts, structure bonus, leftover/duplicate/empty penalties) picks the winner, so a page the hunt reads badly still ships with the flat map. `toolGap` is the **union** across strategies (the vocabulary engine keeps every sighting). API returns `strategy` + per-strategy scores. **Lenient tokenizer** (`language/parse.js` `{lenient:true}`): yahoo-class attribute soup degrades to text instead of throwing; strict stays strict for `.pzn`. **The cut speaks percent end to end:** BenTML `ROW(ratio: "70%:30%")` normalizes to `70:30` (compile.js); the builder's drag handle now STORES integer percentages summing to 100 (`setColumnRatios`), so the drag, the % labels, and the `.pzn` all say the same numbers. **QA:** new `smoke-hunt` (24 checks: the percentage cut from classes/styles/equal-split, row-detection guards, hero collapse + the shape-test decline, twins dedupe, residue drop, cluster-inside-hunt, attribute-soup survival, the strategy race + forced-strategy + never-crash, `.pzn` round-trip of the cut, BenTML % normalize) in `test:smoke`; full suite green. **Browser E2E through the real admin:** decompiled a two-half page via `POST /admin/api/pzn/decompile` (hunt won 53.2 vs 45.2, `ratio="67:33"`), opened the draft in the builder — two panes with % labels + the resize handle, dragged the divider, labels moved 67/33→46/54, saved, and the draft `.pzn` on disk reads `ratio="46:54"`. Demo page kept: `/v2-halves-demo`. (Log gap: v0.61–v0.65 shipped — ticker, video, storage, categories, card clusters — rows pending backfill.) Next: mine tool-gap, AUDIO, BYOK media-write, carousel. | Ben (direction + lab prototype) + Claude |
| v0.60-alpha | **The nav module with color options (batch item 2/6).** Ben: "menu/nav… coloring option also for the nav and background." New `nav` container of `navitem` links (label + href) rendered as a **zero-JS flex bar** with a **background color + text color + alignment** (start/center/end). Full cross-layer recipe: pzn registry `nav`+`navitem`, shared **`src/pzn/nav-html.js`**, bridge (items ⇆ navitem children), renderer case, block-registry `nav` (color params + items list), keywords `NAV` (**graduated out of `RESERVED`**), `blocks.js`, `navitem` in the dictionary HIDDEN set, builder preview, CSS. Colors go through **`safeCssColor`** (the v0.44/v0.52 KEY LESSON — CSS context, not HTML: strip `;{}:"` breakout, keep hex/rgb/named); links through `safeHref`; text color via a `--bent-nav-color` CSS var. **Loop closed** (`graduate.js`): a `<nav>` now decompiles into a nav block (its `<a>` children → links), dropped from `suggestedTools`. **Bug caught by browser verification:** the nav emitted its OWN `style=""` AND the renderer's generic `extra` already carried one (from the block's `align`→`text-align`) — **two `style` attributes**, so the browser kept the first and silently dropped the nav colors. Fixed by passing the generic style *declarations* (via `styleDecls`) into the shared renderer to merge into **one** style attribute; regression-guarded in `smoke-nav`. **QA:** new `smoke-nav` (round-trip, compile/render parity, the CSS color-breakout guard, safeHref, align fallback, single-merged-style regression, decompile-loop) + updated `smoke-decompile` + `smoke-registry` green at 30 entries; full suite green (20 suites); **browser E2E** — published a colored nav, computed style confirmed `background: rgb(10,102,194)`, white links via the var, `justify-content:center`, zero console errors. Batch remaining (3–6): moving-news ticker, video, categories (make + presentation). | Ben (direction: the walla batch) + Claude |
| v0.59-alpha | **The card grid — walla's atomic unit, built (batch item 1/6).** The v0.58 walla learning session (decompiled its RENDERED DOM, 4520 elements) showed a content site is **a wall of media cards**: ~111 `<article>`, 119 `<figure>`+`<picture>`, 347 card-like clusters. Ben ordered the batch; this is #1. New `cards` container of repeatable `mediacard` items (image / tag-kicker / title / excerpt / href) rendered as a **zero-JS responsive auto-fill grid** (`repeat(auto-fill, minmax(15rem,1fr))` — 3 cols desktop → 1 on mobile, verified). Full cross-layer recipe: pzn registry `cards`+`mediacard`, shared **`src/pzn/card-html.js`** (one renderer for compile AND `renderer.js`, like form-html), bridge (items ⇆ mediacard children), renderer case, block-registry `cards` (items list param), keywords `CARDS`, `blocks.js`, `mediacard` in the dictionary HIDDEN set, builder canvas preview, CSS. **Security:** image + href through `safeHref` (no `javascript:`); a card with no href renders `<article>` (not a dead `<a>`); everything escaped. **QA:** new `smoke-cards` (round-trip, compile/render parity via the shared module, safeHref guard, no-href→`<article>`, title-escape) + `smoke-registry` green at 29 entries; full aggregate green (19 suites); browser E2E — published a "מבזקים" page, the live grid is `display:grid`, 3 responsive columns → 1 on mobile, first card a real `/n1` link, tags/titles correct, zero console errors. **Deferred:** the decompiler auto-recognizing card *clusters* (figure+h3+a repeated → cards) — a fuzzy cluster-detection pass, its own follow-up (unlike `<form>` which is one clear element). Batch remaining (2–6): menu/nav **with nav+background color options**, moving-news ticker, video, categories (make + presentation). | Ben (direction: the walla-learned batch) + Claude |
| v0.58-alpha | **The form module — the vocabulary engine turns its first full cycle.** v0.57's decompiler reported `form` as the #1 missing tool (`config/tool-gap.json` → `form:1`); v0.58 builds it and **closes the loop** so the next decompile maps forms instead of dumping them to `bent-html`. Ben: "make the modules so the designer will have all the things he needs." Full cross-layer add (the [[adding-a-module-recipe]] 8 files): pzn registry `form` container + `field` child, the shared **`src/pzn/form-html.js`** (one renderer for both the compile path AND `renderer.js` so they can't diverge), bridge (fields ⇆ field children, items pattern), `renderer.js` case, block-registry `form` entry (fields list param), `keywords.js` FORM (**graduated out of RESERVED**), `blocks.js`, the builder canvas preview, CSS (published + preview), and `field` added to the dictionary HIDDEN set. Fields: text/email/tel/textarea/select/checkbox with label/name/placeholder/required/options; zero-JS `<form>` posting to the owner's own `action` URL (their backend / Formspree / a future Tapuz leads endpoint). **Security:** action through `safeHref` (no `javascript:`), field `name` reduced to a safe `[\w-]` token (it's both an attribute and the submit key), everything escaped. **Loop closed** (`graduate.js`): a `<form>` now parses its label/input/textarea/select children into fields → a form block, skipping submit/hidden controls; `form` dropped from `suggestedTools`. **Bonus bug fix** surfaced while wiring: mid-walk raw markup could flush as a *text* block (tags as visible garbage) and nested leftovers hoisted to top level — now `flushRaw(raw, sink)` as provisional bent-html into the correct sink. **QA:** new `smoke-form` (round-trip, compile/render parity via the shared module, the `javascript:` guard, name sanitization, decompile-a-form-maps-clean) + updated `smoke-decompile` + `smoke-registry` green at 28 entries; full aggregate green (18 suites); **browser E2E** — pasted a real "צור קשר" page with a form into `/admin/new`, decompile returned **`mapped:3, leftover:0, toolGap:[]`** (pre-v0.58 this was `toolGap:['form']` + a provisional blob), the builder showed an editable form block (labels + required stars + submit, no provisional warning), and publish produced a live accessible `<form action="/lead" method="post">` with zero `javascript:` and zero console errors. Next: learning room (label decompiled patterns from walla/yahoo → new modules), then table/nav (the next gaps), BYOK media-write, carousel. | Ben (direction: "give the designer all the tools") + Claude |
| v0.57-alpha | **The HTML→BenTML decompiler — the vocabulary engine (idea harvested from the grok lab, rebuilt here).** Ben built a decompiler in grokTapuziel and said "we should work on this." The product thesis (his Red Hat line): **every real homepage is a map of missing tools** — decompile it and you get modules we already speak + provisional `bent-html` leftovers (nothing lost) + **`toolGap`**: the patterns we saw but have no module for. The toolGap is the vocabulary backlog; we build those modules, the next decompile maps cleaner, and every agent's dictionary gets richer. The decompiler is not an importer feature — it is how the palette fills. **Shipped:** `src/pzn/decompile.js` — whole-page decompile (extract `<main>`/`<body>`, title/dir/lang with a Hebrew-chars rtl heuristic, `deriveSlug`) → blocks → `fromTapuzPage` → serialize → validate; `decompileUrl()` fetches any live site — **rebuilt with the SSRF guard the lab version lacked** (http/https only, hostname denylist, DNS-resolve with every address checked against private/reserved IPv4+IPv6 ranges incl. cloud-metadata 169.254.169.254 and v4-mapped v6, manual redirects re-validated per hop, 10s timeout, 2MB body cap, content-type check). `graduate.js` upgraded into the engine: `suggestedTools` reporting (form/table/video/audio/nav + grid/flex→columns heuristic + whatsapp-link hint), iframe/youtube-link→`embed` (auto-embeds), **plus a real pre-existing bug fixed** — raw markup flushed mid-walk used to become a *text* block (tags as visible garbage) and nested leftovers were hoisted to the top level; both now flush as provisional `bent-html` into the correct sink. `POST /admin/api/pzn/decompile` {url|html, create} creates a no-clobber draft; toolGap counts aggregate into gitignored `config/tool-gap.json` (the per-install backlog). UI: **"🔁 יש לכם כבר דף? ייבאו אותו"** fold on `/admin/new` — URL or pasted HTML → draft → straight into the builder, with the mapped/leftover/toolGap report. **QA:** new `smoke-decompile` (28 checks: mappings, whole-page pipeline, rtl heuristics, script-only pages never hard-fail, the SSRF battery incl. localhost/metadata/file:/[::1], self-decompile of our own renderer output) in `test:smoke`; full aggregate green (17 suites); browser E2E — pasted a restaurant page with a form on `/admin/new`, it created the draft, opened the builder with 5 block cards (heading/text/image/button + provisional form), the `.pzn` on disk is valid with the form escaped-preserved, and `tool-gap.json` recorded `form:1`; zero console errors. Next: mine tool-gap.json → build the top-asked modules (form is already #1), BYOK media-write tier, carousel. | Ben (direction + lab idea) + Claude |
| v0.56-alpha | **Refine the ports + the free media win (no-key "media in reach of agents").** Ben's architecture call: the roleplay injection (BYOT) builds pages from text but can't *touch* media — *touching* (upload/generate) media needs BYOK (a key, third-party use), its own future tier. But **referencing existing media doesn't need a key** — so this version makes agents media-aware for free. **Media win:** `src/media.js` `listAllMedia()` (flat manifest of every uploaded image) + `GET /agent/v1/media`; the roleplay pack now injects an **"Available media — use ONLY these paths (do NOT invent)"** section (`agent-roleplay.js`, threaded through `/agent/v1/roleplay` + `/admin/api/{inject-pack,mission/*}`), so agents reference real `/assets/…` images instead of inventing `/uploads/x.jpg`. Pure-function `buildRoleplayPack({media})` stays DB-free; the server fetches + passes (capped at 40). **Injection hardening:** fixed the #1 silent failure — React-controlled `<textarea>`/`<input>` composers ignore `el.value=…` (onChange never fires), so `setComposerText` now goes through the native prototype value setter; plus a live **selector-health diagnostic** in the panel (`שדה ✓ · תשובה ✗ · שליחה ✓`) so per-provider tuning in a loaded Chrome is guided, not blind. **Consolidation:** the four AI on-ramps collapsed to one coherent front door — the copilot `/admin/chat` is now the single **AI ✨** nav item (was 4: ai/chat/inject/agent → 2: chat/agent), with the paste flow (`/admin/ai`), the dictionary/game (`/admin/inject`), and the extension bridge (`/admin/agent`) as sub-tools reached from the hub's "עוד דרכים לבנות" card + back-links. **QA:** `smoke-roleplay` extended (media section injects/omits, warns, mediaCount) + `smoke-extension` (native-setter present, selector diagnostics); full aggregate green; browser-verified — media manifest threads into the pack end to end (real `/assets/` path), nav shows the single AI hub highlighting across all three pages, `/admin/ai` intact, zero console errors. Next: the BYOK media-write tier (upload/generate under roleplay rules), then carousel. | Ben (direction: BYOK for media / refine first) + Claude |
| v0.55-alpha | **The roleplay injection + copilot mission bridge — "make any chat roleplay BenTML at a press of a button" (BYOT).** Ben: "the injection which will be in the extension… we're going to make any chat roleplay BenTML using the dictionary in a press of a button, then the user tells the primed agent the game's rules." Ported the roleplay/"game" layer from the `grokTapuziel` language lab and grounded it on Tapuz's live registry. **The pack** (`src/pzn/agent-roleplay.js` + `agent-mission.js` + `syntax-dictionary.js`): one document that primes any LLM to be the **Site Builder agent** — role + the `bent-*` **tool inventory** (built live from `getCommandCatalog`, so it can't drift) + a worked example + the **completion contract** (reply with ONE fenced `html` doc ending in `</html>`, optional `PZN_READY`) + the full dictionary. Two on-ramps: **(1) the extension** — the floating BYOT panel now **injects** the pack straight into the chat composer ("① הזרק משחק + מילון"), watches the streamed reply, and **auto-publishes the moment a COMPLETE `.pzn` lands** (closed fence + `</html>` + ≥1 module — half-streams are refused); **(2) `/admin/chat`** — a copilot where you describe a page → it mints a **mission** (`src/mission-store.js`, gitignored `config/missions.json`) → the extension pulls it (bearer token) and injects teach→build→auto-publish. New endpoints: `GET /agent/v1/roleplay|dictionary`, `GET/POST /agent/v1/mission[/:id/step]`, plus `/admin/inject` (copy the game/card/dictionary) and the `/admin/api/mission/*` set. The **token stays only in the extension's background worker** (never in the content script/page) — the standing security invariant, re-asserted by the smoke. This is the "hand the dictionary once, then chat to build" flow Ben asked for since v0.51, now one-click. **QA:** new `smoke-roleplay` (20 checks — pack has role+contract+live tools, tabs/accordion in the inventory, child leaves hidden, per-provider coaching) + `smoke-mission` (the create→pull→advance lifecycle, isolated to a throwaway `TAPUZ_ROOT`) + upgraded `smoke-extension` (composer/send/streaming selectors, the `analyzeReply` completion gate, the new background handlers, token-only-in-background re-verified); full aggregate green; browser-verified end to end — `/admin/inject` renders the live 30-tool inventory + previews the pack, and `/admin/chat` turned a page description into a ready mission with the inject actions. The DOM inject/scrape selectors can't run headless (same caveat as the scrape bridge) — tuned per-provider in a loaded Chrome. Next: carousel/slider, then motion/scroll effects. | Ben (direction) + Claude |
| v0.54-alpha | **Interactive containers — the palette starts filling out (tabs + accordion).** Ben: "we should not be limited to byok… give people the power to inject BenTML into their agent and imagine their site — we will have the effects and containers ready to be built." First batch of the effects/containers library: **tabs** (pure-CSS via the radio hack — no JS) and **accordion** (native `<details>`). Both wired the FULL way — the BenTML language (`<bent-tabs>`/`<bent-tab>`, `<bent-accordion>`/`<bent-fold>` in the pzn registry), the bridge (block⇄BenTML, items ⇆ children like faq), `renderer.js` (published-site HTML), the Tapuz block-registry + `keywords.js` (TABS/ACCORDION graduated out of RESERVED) + `blocks.js`, the builder (toolbox modules + canvas previews + the list-item editor), CSS, and — crucially — the **auto-generated BenTML dictionary**, so any agent (any model, no key) now knows tabs+accordion and can imagine sites with them. Zero-JS by design (radio hack + `<details>`), so they stay fast and portable. **QA:** new `smoke-containers` (11 checks incl. the pure-CSS radio→label→panel adjacency the tabs depend on) + `smoke-registry` still green at 27 modules; full aggregate green; browser-verified on a LIVE published page — clicking the 2nd tab switched panels with no JS, the accordion toggled, the builder shows previews + both in the toolbox, zero console errors. Next batch: carousel/slider (`SLIDER` still reserved), timeline, steps. | Ben (direction) + Claude |
| v0.53-alpha | **Import BenTML from any AI — the in-builder bridge (BYOT).** Ben: "page builder properties with the ability to import BenTML from any agent… we bridge between llms to the system with BYOT style… hand the dictionary to the llms, one time, then chat for editing." New **🤖 ייבא מ‑AI** button in the builder opens a modal: **(1)** one-click "📋 העתק מילון BenTML ל‑AI" (the primer — the one-time handoff), **(2)** a paste box for the AI's reply (prose/fences fine — extracted), **(3)** replace-page or append. New non-saving endpoint `POST /admin/api/pzn/to-blocks` extracts → parses → (on failure) **repairs** → `toTapuzPage` → returns blocks the builder applies in place; imperfect agent output still lands (repaired), and the fixes are surfaced. BYOT: the AI runs on the customer's own subscription — Tapuziel only carries the reply, no keys. This brings the paste-flow's power inside the builder without leaving it (the standalone `/admin/ai` page and the Chrome extension remain the other two on-ramps). **QA:** new `smoke-import` (8 checks: clean import, prose-wrapped extraction, imperfect-→-repaired-into-blocks, graceful junk failure); full aggregate green; browser-verified end to end — pasted a prose-wrapped reply containing `<bent-paragraph>`, it extracted + repaired (paragraph→text) + rebuilt the page as hero+text+heading, modal closed, zero console errors. | Ben (direction) + Claude |
| v0.52-alpha | **Splash background on scroll — the page-level parallax photo.** Ben: "I want splash photo when scrolling… background in the page properties and moving photos like onepage does." New **רקע הדף · splash** section in page properties: a full-page background image (+ media picker), a dark-overlay slider, a **parallax toggle** ("תמונה נעה בגלילה כמו onepage"), and a fallback background colour. The renderer (`pageBackgroundStyle` in `renderer.js`) injects a page-scoped `<style>` + body class: when parallax is on it's a **fixed full-viewport layer the content scrolls over** (a fixed `::before` layer, smoother on mobile than `background-attachment:fixed`, with a touch fallback that pins it); off = a scrolling `absolute` layer. The image URL is CSS-hex-escaped (no `url()` breakout) and the colour is stripped of CSS-breakout chars. The builder shows a **live preview** of the splash on the canvas. **Bug caught by end-to-end verification:** static export (`export.js`) strips ALL inline `<style>` tags (theme CSS is externalized to `/css/main.css`) — which silently ate the page-bg style, shipping a body class with no background. Fixed by extracting `externalizeStyles()` that PRESERVES the `tapuz-page-bg` style across the strip. **QA:** new `smoke-page-bg` (15 checks incl. the CSS-escape security cases AND the export-preservation regression); full aggregate green; browser-verified end to end — set a parallax splash in the builder, published, and the live page renders a `position:fixed` background layer with the overlay. Stored in page `meta` (persists via the DB; `.pzn`-head portability is a follow-up). | Ben (direction) + Claude |
| v0.51-alpha | **Slug auto-rename + graduation UI + a calmer builder for customers.** Four asks from Ben in one pass. **Slug auto-follows the page name:** editing the title rewrites the address (spaces → dashes, Hebrew kept) while it's still title-derived; a hand-edited slug detaches (clear it to re-enable auto). The address is now a field in page properties and renaming a live page moves its `.pzn` files + updates the URL bar in place — and a taken address never blocks the content save (it just keeps the old address and flags it). **Graduation UI:** provisional `bent-html` blocks now render as a clear "raw HTML — provisional" card (content shown ESCAPED, never injected into the admin DOM) with a **"✨ convert to modules"** action; a new best-effort converter (`src/pzn/graduate.js`, `/admin/api/pzn/graduate`) maps h1-6→heading, p→text, img→image, ul/ol→list, a→button, blockquote→quote, hr→divider, descends through wrapper divs, and keeps anything unmappable as a smaller `bent-html` block so nothing is lost — the graduated blocks always form valid BenTML. **The code view is now ADVANCED:** the live BenTML dock under the canvas and the code tab are hidden behind a remembered "⚙ מתקדם" toggle, so a customer's default is a clean visual builder + chat (the raw code "looked odd" up front). **Terminology:** the language is **BenTML** (fixed in the primer, which now names it for the LLM); customer-facing copy is encouraging ("תיהנו מהזרימה"), "foolproof" stays internal-only. **QA:** new `smoke-graduate` (12 checks incl. graduated-blocks-are-valid-BenTML) added to the suite; full aggregate green; browser-verified end to end — auto-slug rename moves the page + URL, the advanced toggle hides/shows the dock, and clicking convert turned a provisional HTML block into heading+text+list+button in place, zero console errors. | Ben (direction) + Claude |
| v0.50-alpha | **The foolproof live builder.** Ben: "we must have auto slug for people who ain't familiar with sites… editing in the live build should be allowed, if I edit text on the live builder I should have the option to edit it… the side properties is the advanced part, we give full control but we are doing the live site fool proof." **Auto-slug:** `/admin/new` no longer forces a manual slug — it auto-derives one from the title as you type (Hebrew kept, editable, stops the moment you touch it), and the server fills+dedupes if left blank. A newcomer never has to know what a slug is. **Single-click inline editing:** editing on the canvas was hidden behind *double*-click and covered ~6 block types; now a SINGLE click on any text edits it in place, with a persistent faint-underline affordance so it's discoverable (no one has to guess). The commit path was generalized to `block.data[key]` — one code path for hero/heading/text/quote/testimonial AND the generic registry text fields, so **marquee and banner became editable on the canvas too**. Edit-start no longer re-renders the whole canvas when the block is already selected (no flicker, no lost caret), and only marks dirty when the text actually changed. **The side panel stays the advanced surface:** clicking text to edit ALSO selects the block, so its full control panel is right there — foolproof on the canvas, full power in the drawer. **Saves verified:** save-draft, publish, and publish+build all confirmed working end-to-end (edit inline → save → the change lands in both the draft and published `.pzn`). **QA:** full suite green; browser-verified end to end — auto-slug fills from a Hebrew title and stops on manual edit; single click makes hero/banner text editable and commits to the model; the side panel populates (10 fields) while editing; save+publish persist the inline edits; zero console errors. | Ben (direction) + Claude |
| v0.49-alpha | **The forgiving pipeline + the escape hatch — "saving works now".** Ben's report: saving edited/AI-authored BenTML failed constantly, the strict validator blocked designs he wanted, models didn't understand the syntax, and the extension "asked for a key" instead of giving tools. Root cause found: one bad tag rejected the WHOLE document, and there was no way to express an off-vocabulary design. **The escape hatch — `bent-html`:** a new registered module whose payload is arbitrary HTML, sanitized at compile/serve time (Ben's call: "both, with a graduation path" — it renders, is marked `provisional`, and the builder flags it to convert into real modules). The standard stays a registry; this is its pressure valve (like MDX→JSX). Wired across every layer (registry compile, bridge both ways, `renderer.js` published path) and round-trip-lossless (raw HTML lives HTML-escaped in a `content` attribute — no parser surgery). **The sanitizer is the guarantee, not the CSP** (`src/html-sanitize.js`): the published `script-src` allows `'unsafe-inline'`, so an entity-aware denylist scrubber strips script/handler/scheme vectors — 15-case adversarial battery incl. nested `<scr<script>ipt>`, `java&#115;cript:`, tab-obfuscated schemes, SVG `onload`, `data:text/html`, `@import`, `expression()`. **The repair engine (`src/pzn/repair.js`) — Ben's call: "auto-correct, then you apply":** a strict-save failure becomes a corrected document + a human-readable change list. It ALIASES model drift to real modules server-side (`bent-paragraph`→text, `bent-img`→image, `bent-h1`→heading level 1, ~50 mappings — "the server owns the vocabulary"), QUARANTINES the truly-unknown into provisional `bent-html`, WRAPS raw HTML in the body, fills/snaps/clamps bad props, dedupes ids, and hoists illegal nesting — every repaired doc parses AND validates clean. **Admin UX:** the paste flow now offers a one-click "🔧 תקן והחלף" when a reply is broken (dry-run `/admin/api/pzn/repair`, no save); the fix flows back into the existing preview→apply pipeline. **Extension:** the popup now LEADS with "תן ל‑AI את ערכת הכלים" (equip your AI) and folds the token into a one-time setup; the button falls back to the forgiving path so imperfect replies land as a reviewable DRAFT (never auto-published) with the change count shown. **Primer** now tells the model it needn't be perfect (the server repairs) and can fall back to plain HTML. **QA:** new `smoke-pzn-html` (20 checks) + `smoke-pzn-repair` (every failure mode → clean output) added to the suite; full aggregate green; end-to-end browser-verified — pasted a broken doc (raw div + alias + level=99), saw the 3-fix offer, clicked apply, and confirmed the stored draft is valid (`bent-html` + `bent-text` + `level="6"`, no raw div). | Ben (direction: both forks) + Claude |
| v0.48-alpha | **The unification — one core, no branches (it's an OS now).** The language core (`src/pzn`) and the whole security phase stop being feature branches and become THE trunk: every feature branch (v0.39-pzn-language-core, v0.45/46/47 work, feat/security-chrome-analytics) is now merged into `main` and deleted, local **and** remote — the repo is a single trunk going forward (`origin/main` only). **Dev ergonomics — the "I couldn't stand up an environment to test" fix:** new `scripts/seed-dev-admin.js` + `npm run seed:admin` mints an instant `admin`/`admin` login so you skip the first-run wizard for local testing. It is explicitly **not a backdoor** — writes only to gitignored `config/auth.json`, the app ships zero default account, refuses to run under `NODE_ENV=production`, and self-verifies the seeded hash before exiting. Extension first-run friction removed: `localhost`/`127.0.0.1` granted in the manifest, popup requests host permission for non-local CMS origins, ping failure now hints "is the server running?", README is a 5-step runbook (no Apache — Tapuziel *is* the server). **Fix:** `/agent/v1/ping` reported a hardcoded `version: '0.45'` → now reads `package.json`, so the extension popup shows the true version. Published spec regenerated (`docs/pzn-spec.md` + `pzn-schema.json` → `tapuziel@0.48.0-alpha`). **QA:** full smoke suite green (exit 0); `admin`/`admin` login verified end-to-end against a live server — HTTP 302 + signed session cookie, and a real browser login into the full admin dashboard. | Ben (direction) + Claude |
| v0.47-alpha | **Security phase + the `.pzn` standard published + extension load fix** (Phase 3 begins; Phase 4 groundwork). **Extension fix:** the Chrome extension wouldn't load — manifest referenced a phantom `icon.png`. Generated real 16/48/128 tangerine PNGs (scripts/gen-extension-icons.js), wired into manifest icons + action.default_icon; smoke-extension now asserts every manifest-referenced file exists (the gap that let it through). **Security — upload validation:** src/upload-validate.js sniffs MAGIC BYTES (never the client mime/filename), image allowlist only, 5MB cap — closes the `data:text/html` → stored-XSS-hosting and type-spoof vectors. **Security — the `.pzn` standard:** docs/pzn-spec.md (implementable standard) + docs/pzn-schema.json (machine catalog, generated from the live registry via src/pzn/spec.js) + public `GET /pzn-schema.json` — the Red Hat "publish your RPM" move; anyone can implement a reader/writer. **Security — CSP + hardening:** CSP on published pages, 512kb agent body cap, gitleaks CI (.github/workflows/security.yml + .gitleaks.toml). **A 19-agent adversarial review found 6 real holes in the security work itself — all fixed** (the review's most important catch yet): (1) regex SVG sanitizer bypassable (nested `<scr<script>ipt>`, `/onload`, SMIL, encoded schemes) → the load-bearing fix is the SERVE LAYER (uploaded SVGs now served with `sandbox` CSP + `Content-Disposition: attachment` + nosniff via express.static setHeaders, so a bypass can't execute), plus a ReDoS-safe best-effort scrubber; (2) that sandbox header was DEAD CODE (shadowed by the static mount) — fixed via setHeaders so it applies at serve time; (3) CSP exclusion prefix gap (`/admin*` matched public pages) → boundary-aware; (4) unauth stack-trace disclosure on the body-cap → terminal error handler returns generic JSON; (5) ReDoS in the sanitizer; (6) SVG external-ref. Browser + live-HTTP verified; regressions locked in smoke-uploads + smoke-agent-bridge; full suite green. **Deferred (still before beta):** DB encryption at rest (dependency + key-management change — its own version) | Ben (direction) + Claude + adversarial review fleet |
| v0.46-alpha | **The Grokin bridge CLIENT — the Chrome extension, zero keys, Claude included** (Phase 2 piece 3 complete, the ace is end-to-end). New `extension/` (MV3): `background.js` (holds the token, message router, CMS fetch), `content-bridge.js` (floating "🍊 → תפוזיאל" button that scrapes the latest assistant reply on the LLM site), `providers.js` (per-provider scrape selectors — **Claude first-class**, plus ChatGPT/Grok/Gemini), `extract.js` (pull .pzn from the reply), `popup.html/js` (set CMS URL + token, ping, copy primer, pick target page), README. Flow: paste primer into your own Claude/ChatGPT → ask for a page → bot replies .pzn → click the button → live page. Server: new write-scoped `/agent/v1/create-from-source` (the extension's main path; hardened deriveSlug, 409 on collision). **Security model (first-party):** the bearer token lives ONLY in the background worker (chrome.storage, read only there), used ONLY in `Authorization: Bearer` to the configured CMS — NEVER sent to a content script or web page; no `externally_connectable` so LLM pages can't message the worker; getConfig returns `hasToken` boolean only. **QA:** scripts/smoke-extension.js (26 checks: extraction, provider table incl claude.ai, MV3 manifest validity, and the token-exposure invariants — content script touches no storage/auth/fetch, popup write-only, no raw token in any response); smoke-agent-bridge extended with create-from-source (build/live/409/scope); manual token-leak scan confirmed both raw-token uses are Authorization headers. Full suite green. Note: DOM-scrape selectors + the real-LLM path need the extension loaded in Chrome (can't run headless) — Ben tunes selectors per provider as sites redesign | Ben (vision) + Claude |
| v0.45-alpha | **The Grokin bridge server contract — BYO-agent API, zero keys** (Phase 2 piece 3, North Star pillar 6 groundwork). New `/agent/v1/*` bearer-token API so an external agent (the coming Chrome extension) drives the CMS cross-origin: ping/primer/toolbox/pages/source(GET+POST)/ops/build. `src/agent-tokens.js` — scoped (read/write) bearer tokens, own gitignored config/agent-tokens.json, scrypt-hashed (reuses auth.js), prefix-lookup + full-token verify, shown once at mint. CORS `Allow-Origin:*` WITHOUT credentials (safe: bearer never auto-sent → no CSRF → no cookie riding); deliberately outside the /admin same-origin guard. `src/pzn/intent.js` — the Grokin trick: model emits a forgiving intent `{sections[]}`, SERVER owns the .pzn (14 section types incl hero/marquee/parallax/features/cta, validated). Admin `/admin/agent` page + token CRUD (session-authed). **QA: 24-check HTTP smoke (reference client) + a 26-agent adversarial security review (5 lenses) that found 7 real holes behind the green smoke — ALL FIXED**: (1) path-traversal arbitrary .pzn write via backslash/`..` in a bot-authored slug → hardened safeName + sanitizeFullPath + deriveSlug + resolved-path guard, across pzn-store/pages/export; (2) rate-limit lockout keyed on attacker-supplied token prefix → re-keyed to source IP; (3) /build silently clobbering an existing page → 409 unless update:true; (4) unbounded intent nesting DoS → depth(6)+count(250) caps; (5) `javascript:`/`data:`/`vbscript:` in href/url → new safeHref/safeUrl neutralize on BOTH the intent AND the render+compile chokepoints (button/embed/cta/hero/@LINK/logo — closes it for the paste flow too). Browser-verified token UI (mint→shown once→authenticates a real ping). Full suite green | Ben (vision) + Claude + adversarial review fleet |
| v0.44-alpha | **Signature visuals — the patterns the vision was named for** (Phase 2 piece 2). Full-stack: HERO `overlay` (0–80 darkening) + `parallax` (fixed bg) now STORED end-to-end (keywords→compile→JSON→renderer→decompile→pzn→bridge) — **the "parsed but never stored" drift from src/block-registry.js:23 that started the whole merge is closed; smoke-registry KNOWN_UNCOVERED is now empty and any future gap fails the smoke**. New block types: `marquee` (moving text, robust two-full-width-track loop = seamless at any length) and `parallax` (section with fixed bg + overlay + content scrolling over). New `animate` prop (fade/rise, scroll-driven via animation-timeline where supported) on heading/text. Theme + preview CSS with reduced-motion + touch-device (hover:none/pointer:coarse) fallbacks. Registry 23→25 types, pzn modules 30→32. **QA: 22-agent adversarial review workflow (4 lenses × verify) found 15 real bugs the green suite missed — all fixed**: CSS `url('…')` injection in hero+parallax (HTML-entity escaping is wrong for CSS-string context → new `cssUrl`/`escapeCssUrl` CSS-hex escaper), marquee/parallax className emitted as bare attribute, hero overlay-without-image emitting `url('')`, parallax dropping inline styles, hero parallax truthy-vs-strict mismatch, animate enum unvalidated, PARALLAX image required-vs-optional round-trip E306, marquee short-text pop, overlay ignoring border-radius, fixed-bg missing touch fallback. Browser-verified live (2 tracks, 100% min-width, RTL anim, 16px inherited overlay radius, injection neutralized). Round-trip tests 25 types, full suite green. Follow-ups chipped: builder-canvas parallax editing, legacy keyword-lang nested-id/whitespace quirks | Ben (vision) + Claude + adversarial review fleet |
| v0.43-alpha | **The paste flow — BYO AI, zero keys** (Phase 2 piece 1, North Star pillar 6 goes live). New `/admin/ai` screen: copy the primer → paste the bot's reply → live preview → apply. `src/pzn/agent-primer.js` generates the paste-into-any-AI primer from the live registry (all 30 modules, reply contract, worked example — can never drift). `src/pzn-extract.js` pulls the .pzn document out of prose/code fences (```/~~~/bare). API: GET `/admin/api/pzn/primer`, POST `/admin/api/pzn/preview` (compile without saving), `loose` flag on POST source, POST `/admin/api/pzn/create-from-source` (bot invents a page: slug from bent-slug, 409 on collision). Publish from the paste flow runs exportAll — "צפה בדף החי" is live immediately. **QA**: smoke-pzn-paste.js (15 checks) in test:smoke; full REAL-BROWSER E2E on a throwaway root — login, paste a bot reply with prose around the fence, live preview rendered, new page created+published, live page served with hero/features/stats/cta. Browser QA caught two real bugs pre-ship: pages API shape mismatch in the picker, and publish-without-export 404ing the live link | Ben (vision) + Claude |
| v0.42-alpha | **The canonical editing path — .pzn source + AST ops** (Phase 1 step 4, Phase 1 complete). pages.js gains `getPageSource` (canonical .pzn, legacy fallback), `savePageSource` (validate → save → author formatting preserved byte-for-byte → title/tags/teaser/cardImage/direction sync to DB index → optional publish), `applyPageOps` (builder-standard ops: insert/update/move/replace/duplicate/remove/document via src/pzn/builder/ops, serialized back through savePageSource). New admin API (behind auth like all admin routes): GET+POST `/admin/api/pzn/source`, POST `/admin/api/pzn/ops`, GET `/admin/api/pzn/toolbox` (module schemas for agents). The visual canvas's JSON is now formally a wire projection of the file (v0.40 bridge over v0.41 store); agents/tools edit the canonical form directly. **QA**: scripts/smoke-pzn-ops.js (22 checks: formatting preservation, invalid source rejected without touching the file, every op kind, revision trail) joined test:smoke; live boot verified new routes answer 401 unauth exactly like existing admin API; full suite green (88 tests + all smokes + wizard + 1138 registry checks). **Phase 1 (unification) done — one language, one file, one model.** Next: Phase 2 the moat — signature visuals (parallax/animated text), unified agent pack, Grokin-V2 BYO-subscription translator, copilot chat | Ben (direction) + Claude |
| v0.41-alpha | **The storage flip — the .pzn file IS the page** (Phase 1 step 3, the one-data-model moment). New src/pzn-store.js: every save writes `pages/drafts/<path>.pzn`, every publish writes `pages/published/<path>.pzn` (SITE_ROOT-relative, TAPUZ_ROOT-aware). parsePageRow overlays blocks from the file when present — every reader (admin, renderer, export, article cubes) is file-first automatically; DB JSON stays as index + legacy fallback. Revisions now store canonical .pzn text (legacy JSON revisions still readable); restore flows through the file. Rename moves files, delete removes them. scripts/migrate-pzn-store.js backfills files idempotently (existing files win — they are the truth). **QA**: scripts/smoke-pzn-store.js on a throwaway root proves the flip — 18 checks incl. "edit the file on disk → CMS serves the edit while the DB still holds the old value"; full suite green (88 pzn tests, all smokes incl. round-trip, wizard E2E, 1138 registry checks); joined test:smoke. Scope note: content is file-canonical; status/theme/extended meta (redirect) stay DB-owned until the .pzn head covers them. Next: v0.42 = builder edits the AST via ops | Ben (direction) + Claude |
| v0.40-alpha | **Registry unification — every Tapuz block speaks .pzn, losslessly** (Phase 1 step 2). The pzn module registry grew from 16 to 30 modules: all 10 missing Tapuz types added (testimonial, features+feature, card, map, cta, stats+stat, logos+logo, faq+qa, contact-info, banner) with compile HTML matching src/renderer.js contracts (theme CSS keeps working), plus prop parity on existing modules (heading/text/button align, text size/lead/dropcap/maxwidth, image width, hero image+height, gallery columns, columns ratio/collapse/valign + Tapuz gap vocab, spacer size, divider bentstyle). Bridge is now two-way: `toTapuzPage`/`moduleToBlock` join `fromTapuzPage`, rule = only carry what exists (no invented defaults/ids). **QA in the way**: gate 1 = 27 synthetic round-trip tests, all 23 types + edge cases (test/pzn/tapuz-roundtrip.test.js); gate 2 = scripts/smoke-pzn-roundtrip.js proves JSON→.pzn→JSON lossless on the real DB read-only AND on a full wizard-generated site (5 pages, 24 blocks); gate 3 = 88 pzn tests + full smoke suite + 1138 registry checks green. smoke-pzn-roundtrip joined test:smoke. Next: v0.41 flips canonical storage to the page file | Ben (direction) + Claude |
| v0.39-alpha | **The merge begins — Grok language core lands in Tapuz** (Phase 1 step 1 of the unification plan). `C:\Dev\grokTapuziel`'s `.pzn`/benTML implementation ported wholesale into `src/pzn/` (language: tokenizer/AST/parse/compile/validate/serialize; modules registry with i18n labels + prop schemas; immutable builder ops; site build/publish; tapuz-json bridge; loader). All 61 node:test tests ported to `test/pzn/` and green inside Tapuz (`npm run test:pzn`); example fixtures at `examples/pzn/` + demo site at `examples/pzn-site/`. Full existing smoke suite still green (registry 1138 checks, wizard, render, drafts, articles, bentml, map, analytics). Nothing wired into the CMS yet by design — v0.40 unifies the 23-block registry with the module defs and round-trips real pages via the bridge; v0.41 flips canonical storage to the page file; v0.42 points the visual builder at the AST ops. Grounding decision: `.pzn` (constrained-HTML benTML) is the canonical standard — one data model, Red Hat play, `.pzn` is our RPM | Ben (vision) + Grok (implementation) + Claude (port & verification) |
| v0.38-alpha | BenTML v0.1 spec (`docs/bentml-v0.md`, multi-agent designed + blind-verified), registry-driven builder, admin auth, site chrome, analytics | Ben + Claude |
| v0.37-alpha | **Wizard v2**: 4 guided steps (name → coloring that *teaches the theme creator* with palettes + live mini-preview → default pages home/about/contact/articles with article-list + sample article → menu step with page picks + external links). Setup engine extracted to src/setup.js (runSetup — agents/CLI can bootstrap a site in one call). **TAPUZ_ROOT** env var via new src/paths.js — all data (db/config/public) can live in any directory (npm-publish blocker step 1). Fix: home exported only as index.html so menu links to /home.html 404'd — now writes both. Docs ecosystem aligned to version (README, next-steps, mvp-scope, implementation-plan, cli-agent-support, package.json 0.37.0-alpha). smoke-wizard E2E (32 checks, runs on throwaway root) | Ben (spec) + Claude |
| v0.36-alpha | **article-list module** (cubes: image + title + teaser, tag-driven, 1-4 columns, newest first), page properties in side panel (article toggle, manual teaser + card image with auto-extraction fallback), live article preview in builder canvas via new /admin/api/articles, canvas background click = deselect → page properties, tags/meta flow through save/publish, article-cubes theme CSS (responsive grid), smoke-articles E2E (15 checks), block-schemas.md documents article-list for agents | Ben (spec) + Claude |
| v0.32-alpha | Draft/Publish split + automatic revision backups, page navigator modal (search/jump), theme overrides system (colors, fonts, logo, menu placement) with live preview + API, menu entity in DB with editor, publish flow in builder, revisions modal with restore | Ben + Grok |
| v0.35-alpha | Renamed to **Tapuziel** (Tapuz + Shaltiel's ־יאל — free on npm), npm packaging (bin: tapuziel + tapuz alias, engines, repository, npm pack verified 81 files), full E2E: wizard → published site → static serve all verified. TODO before real npm publish: cwd-based paths refactor (db/config/public should live in the user's project dir, not inside node_modules) | Ben + Claude |
| v0.34-alpha | Admin section identity (subtle accent color per area: pages blue, menus purple, theme green, sitemap orange), first-run setup wizard for newcomers (3 questions → site + home page + menu + build), security phase spec'd in roadmap (deferred by design) | Ben + Claude |
| v0.33-alpha | Typed menu links (existing page picker, custom URL, tel: click-to-call, mailto:, anchor), /admin/sitemap tree derived from menus with published/draft/missing badges + orphan-page detection, redirect pages via meta.redirect, menu editor rewrite with live URL preview | Ben + Claude |
| v0.32-alpha | Draft/publish separation (save keeps draft, publish snapshots), auto revisions (30/page) with restore, pages navigator with search + unpublished badges, menu entity in DB with nesting, theme overrides (colors/fonts/layout/menu top-side) + theme & menus admin pages. Fixes: db.js circular-dependency (schema silently failing), revision restore loaded published instead of draft, removed leftover patch script | Grok + Ben, review & fixes Claude |
| v0.31-alpha | DB-wired media library (folders, Explorer UI, right-click delete/copy, create folder), gallery module with multi-select picker, toolbox buttons for list/video/gallery | Ben + Claude |
| v0.30-alpha | Undo/redo, autosave + unsaved guard, list & YouTube modules, Hebrew toasts, XSS fix, semantic HTML5 output, skip-link + aria, theme comfort pass | Claude + Ben |
| v0.2 | Smart module replace, clearer RTL UX | Ben |
| v0.1 | Initial CMS: Elementor import, RTL visual builder, static export | Ben |

## Idea Pool (unscheduled)

- Column width ratios with visual handles
- Keyboard: Ctrl+D duplicate, Alt+arrows move block
- Block templates: save a group as reusable "section"
- Inline text editing on canvas
- SEO panel per page (meta, og:image, slug)
- Auto sitemap.xml + RSS on build
- Scheduled publish
- Dark mode (prefers-color-scheme)
- Second theme (cyberpunk grokskin-style)
- Elementor import: map more widget types
- Markdown import (folder of .md → pages)
- Media: alt-text editing, image resize on upload, drag file into folder

### Ben's ideas
- **Palette from image**: in the theme maker, drop in images that *feel like the color you want* — extract a palette (+ gradients) from them
- **Gradient support** in theme coloring, not just flat colors
- **`.pzn` format**: single-file import/export container (site / theme / plugin), shares the import pipeline with WordPress/Elementor exports
- **Theme import type**: anyone can package a theme as an import and share it; selling allowed, no DRM, no piracy liability on us
- **Plugin install**: install plugins from a file (`.pzn`) — third-party module API is the foundation (see Community section)
- **Import skips wizard steps**: importing a site auto-answers wizard questions it can (homepage, pages, menu) — wizard only asks what's still missing
- **Generic homepage scheme**: from-scratch sites get a homepage pre-cut into page-builder sections (hero / content / CTA…) so there's never a blank page
- **Agent mini-API**: endpoint/spec that lets agents identify page-builder blocks and edit them safely — make Tapuziel the most AI-friendly CMS
- **Copilot chatbot in admin**: ChatGPT-style chat that operates the builder for real (halve page, make hero, color text, place the co-written article) — human skin over the agent mini-API
- **Select-text AI actions**: correct / expand / shrink / rewrite on selected text — secondary entry point alongside the chatbot
- **Side options panel**: module options appear in a side panel when a placed module is selected; schema-driven so every new module gets its panel for free
- **BYO AI subscription**: user's own AI subscription powers the in-CMS assistant (no token middleman); port the Grokin-V2 bot + Chrome extension functions (`C:\Dev\Grokin-V2`), universal across providers
- **Syntax primer + translator**: user pastes a primer into their own bot to teach it Tapuziel/`.pzn` syntax; the bot replies in our syntax and a translator in Tapuziel parses + executes via the mini-API — BYO subscription with zero API keys (the Grokin trick)

### Grok's ideas
- (add here)
