# Tapuziel — Roadmap & Idea Pool

**Current version: v0.60-alpha**

## How versions work

- Every working session that ships = **+0.01** (v0.32-alpha, v0.33-alpha, ...)
- Each bump gets a row in the Version Log: what shipped, whose idea
- Ideas from three brains: **Ben**, **Claude**, **Grok** — all land in this file, best ones win
- When builder + media + pages + theme builder feel complete → v1.0-beta
- Long game: a real open-source CMS competitor, community-driven once Ben sets the direction. Security hardening gets its own phase before beta.

## North Star (Ben, 2026-07-10)

**Become the Red Hat of CMSs.** Fully open source, community-driven, no DRM, no vendor lock-in — the business is the ecosystem and the mastery, not gatekeeping. Competition is absurd, so we plan big and hard; the edge is knowing what people actually want in their sites and how they want them made (Ben = CMS master), plus being **more AI-friendly than any other CMS**.

Pillars:
1. **Ideas flow from mind to website.** The wizard + page builder turn intent into a live site with minimal friction. We advise building in *our language* (page-builder blocks), but never trap anyone — overrides (classes, custom CSS) always possible in the editor, like every serious CMS.
2. **Import as a first-class citizen.** Elementor / WordPress export imports feed the wizard; an import can *skip* wizard steps it already answers (e.g. homepage creation — our generic homepage scheme with page-builder cuts/sections is only for from-scratch sites). Test-drive on an existing server or a fresh one.
3. **Theme & plugin ecosystem.** Theme *maker* (colors, gradients, palette-from-image) + theme *import type* so people build and share themes. Selling is allowed but not our advice; we take zero liability for piracy of sold themes and will never add DRM — it's open source, it's all gitted, +0.01 versioning is our method. Plugin install support.
4. **`.pzn` file format** — one portable container for imports/exports (sites, themes, plugins), same pipeline as the WordPress/Elementor import. This is a NEW packing/file system of ours and the first step of making a mark — same as Red Hat, whose mark was **RPM**, their own package format. `.pzn` is our RPM.
5. **Agents are users too.** Mini-API so agents recognize page-builder parts and know what they're editing (see docs/cli-agent-support.md). The human face of that same API is an in-admin **chatbot that looks and feels like ChatGPT** — see "Module language & AI copilot" below.
6. **Your AI subscription, not our tokens (the ace).** The *brain* of the copilot is the user's own subscription — no middleman charging tokens and reselling API. Already made with Grokin. How it works without API keys (the Grokin trick):
   - The LLM runs where the user's subscription lives — the **web browser environment**, where LLMs are confident to work — or we use conversation tricks: the bot acts from a chat by emitting instructions.
   - We set up **a chat with our syntax, `.pzn`-ready**: the user gets a primer (instructions to paste) that teaches *their* bot (ChatGPT / Claude / Grok…) Tapuziel syntax and `.pzn` syntax.
   - A **translator inside Tapuziel** parses what the bot says in our syntax and executes it through the agent mini-API. The conversation *is* the protocol.
   - API keys stay an option (already proven with ChatGPT + keys), but the goal is universal across providers. `C:\Dev\Grokin-V2` (WordPress bot + Chrome extension) is the sophisticated prior art to port in.

## The Plan (Ben's vision, phased)

### v0.31 — Media & Gallery (this version)
- Media library wired to the database (was: raw folder listing)
- Folder system, Windows-Explorer look: folders + files grid, breadcrumb, create folder, right-click → delete / open / copy URL
- Gallery module exposed in toolbox: picks multiple images from the library
- Toolbox buttons for list + video modules (were replace-only)

### v0.32 — Pages, Draft/Publish, Backups
- Page navigator inside the builder: see all pages, search, jump to edit
- Save ≠ Publish: draft blocks live separately from published blocks; "שמור" keeps a draft, "פרסם" copies draft → published; build renders published only
- Automatic backup revisions on every save (keep last N, restore from list)

### v0.33 — Menu entity + Theme Builder v1
- "menu" becomes a real entity (DB table): items, order, nesting
- Menu module lives in the header — editable in the THEME builder, not the page builder
- Theme builder: colors, logo, menu placement variations (top / side), fonts

### v0.34 — Site-map & Structure
- Visual site-map: page tree, drag to re-parent, status badges
- More separator/section modules (banner, CTA strip, section with background)

### v0.35 — Responsive everything
- Admin + output audited on all resolutions, window-resize safe
- Mobile/tablet preview toggle in builder

### v0.36 — Article system ✅ (shipped) + Wizard v2 (next)
- **article-list module** ✅: shows articles as "cubes" (cards) — image + title + teaser
  - Card image: set manually (page properties), OR auto-extracted from the article's first image block
  - Teaser: manual or auto from first text block; clicking a cube opens the article page
  - A page becomes an article via the new **page properties** panel (deselect → side panel): article checkbox + teaser + card image
- **Wizard v2** ✅ (shipped v0.37) — a real guided setup, not 3 questions:
  1. Site name
  2. Coloring — walks the user *through the theme creator* (teaches the tool while using it)
  3. Creates default pages: home, contact, about, articles (articles page uses article-list module)
  4. Menu step: pick from the pages just created ("the menu's menu") or href to external sites — typed links already support this
- Theme creator: evolve toward drag & drop

### Module language & AI copilot (Ben's spec, 2026-07-10)

**Modules are the type system of the page.** Each module is its own class — primitive like `int`: text, heading, image, button, youtube (paste a URL — already the pattern since v0.30), gallery, list… Containers (sections, columns) get *filled* with modules. Goal: as many modules/tools as possible — the vocabulary of our language.

- **Side options panel**: selecting a module already placed on the page opens its options in a panel at the side of the screen (not inline clutter). Every module declares its own options — one panel, schema-driven, so a new module gets its UI for free.
- **The language must be sound for agents**: an agent should be able to place a text block, take an article it co-wrote with the user and *make it real* on the page, or write into the page in real time. Structured blocks (docs/block-schemas.md) are already this — the mini-API exposes read/insert/update/move per module.
- **AI text operations**: correct, expand, shrink, rewrite. Could be offered as select-text → AI menu (keep as secondary option), **but the chosen direction is a chatbot**:
- **The copilot chatbot**: an in-admin chat that *resembles the LLM UIs people already trust*. The user who is "so confident with their ChatGPT" has a blast — the bot they "know so much about" is operational here: it understands the builder and takes real actions — halving the page into columns, making a hero, coloring text inside a text block, filling a section with the article you wrote together. Chat is the human skin over the same agent mini-API; every chatbot ability is an API ability first.

### The long game (Ben)
1. **CMS** — finish the builder, articles, themes (we are here)
2. **CRM properties** — contacts, forms that feed them, leads from the published site (Ben's mastery: CMS/CRM)
3. **Security phase** — see below; "bundle that no one can deny"
4. **Open source, community-driven** — led by whiteno1se
5. **Ecosystem** — `.pzn` import/export format, theme sharing, plugin installs, agent mini-API
6. **BYO AI subscription** — Grokin-V2 functions ported in, agents talk directly to the system (the Red Hat endgame)

### Later (pre-beta) — Security phase (Ben: features first, security before beta)
Deliberately deferred. When we get here, in order:
1. **Admin auth**: password (bcrypt/argon2) + session cookie; wizard sets it on first run
2. **Database encryption at rest**: encryption key (env var / key file outside repo) using SQLite cipher (better-sqlite3-multiple-ciphers) — "not let anyone near the database"
3. **Request hardening**: CSRF tokens on all admin POSTs, rate limiting, JSON body size limits (exists: 12mb — tighten)
4. **Upload validation**: magic-byte checks (not just mime), SVG sanitization, size caps
5. **Output**: CSP headers on admin + published site, X-Frame-Options
6. **Secrets hygiene**: gitleaks in CI; key never committed

### Community (after security)
- CONTRIBUTING.md, plugin/module API for third-party modules

## Version Log

| Version | Shipped | Source |
|---|---|---|
| v0.68-alpha | **The brains merge — grok-lab decompiler branch 2 harvested into Tapuz (rebuilt, never copied).** Ben: "we made decompiler branch 2 [in the lab] using both brains — we should have the best decompiler around." Read `C:\Dev\grokTapuziel` commit 599c69c ("Decompile fidelity + OCR + universal module overrides") against our v0.67 and rebuilt every idea that beats us: **(1) CDN-safe srcset** — CDN URLs carry bare commas (`f_auto,q_auto,w_500/…`), so srcset is now read by width descriptors and picks the LARGEST candidate (our old split-on-comma truncated real walla/wcdn URLs — bug-grade); **(2) case-blind attrs** (`attrOf`) — React DOM dumps say `srcSet`/`data-Original`; plus `data-bg`/`data-image`/`data-url` variants and protocol-relative `//cdn` → `https:`; **(3) teaser links** — a link wrapping a picture/headline is a CARD, not a button: `anchorCard` reads the anchor's children so **the picture survives** (v0.67 kept only title+href), and adjacent single-item cards FUSE into one wall; **(4) flat learns to skip** — the flat walk never skipped `<script>`: walla's 356KB `window.loadDataState` dump became a "provisional" blob that **gamed the safety-guard scorer** (raw html content counted as coverage → the blob read WON the strategy race). Fixed both ends: flat skips script/style/svg/template noise + descends SPA custom elements (their content graduates instead of blobbing), and `blockTextLen` no longer counts provisional html — preserved ≠ READ; **(5) leftover diet** — `<q>`/`<cite>` → quote, texty bare `<button>` → button (textless = chrome, dropped), textless links fall back to `aria-label`/`title` for a name. **Walla rendered-DOM scoreboard (v0.67 → v0.68): 180 → 112 blocks, 3 → 0 leftovers, 78 → 33 buttons, 105 → 132 real images, ingest 13 → 28 found (24 saved webp); BOTH strategies now read walla with zero provisional leftovers.** Deliberately NOT ported: OCR (tesseract heb+eng text-from-photos — heavy dep, its own version if wanted), universal `override=` module params (builder-war feature, separate track), lab's table module (Tapuz table needs the 7-file recipe through renderer.js/block-registry.js/theme css — all owned by a concurrent session's uncommitted work; queued next). **QA:** 9 new smoke-decompile checks (CDN srcset, camelCase srcSet, protocol-relative, teaser card keeps picture, teaser fuse, script-skip regression, aria-label name, q/cite + bare-button, textless-button drop) + smoke-graduate updated (custom elements descend); full suite green (26 smokes, 98 pzn tests). **Field note (Ben):** the extension's inject freezes ChatGPT ("alerts the llm and it gets afraid") — the built-in CMS copy-dictionary + paste flow works well; lead with built-in tools for ChatGPT. | Ben (direction + lab build with grok) + Claude (rebuild) |
| v0.67-alpha | **The decompiler SEES the page — image sight, menu sight, and the pictures become OURS (webp, self-hosted).** Ben: "picture a page and compare to the decompiled version… we implement the images (probably at webp), we host it in our server, the decompiled file should look similar to the page itself." The comparison harness told the story brutally: walla's homepage (679KB, 74 images) decompiled to **ONE button**. Root cause #1 — **the page-eater** (`extractBodyHtml`): no `<main>` → first-`<article>` fallback grabbed one news item out of 111 and threw away the page; an `<article>` is now the page only when it IS the page (single + holds ≥50% of body text), a tiny `<main>` (SPA shell) falls back to body. Root cause #2 — **the decompiler was image-blind the way 2010 sites were built, and real sites are built like 2026**: the true URL hides in `srcset`/`data-src` (`imageSrcOf`), in inline `background-image`, and — the walla lesson — in **emotion/CSS-in-JS `<style>` blocks** (`classBgMap`: class → background URL, threaded through cards/hero/wrappers). Root cause #3 — **menus decompiled as garbage**: `ul>li>a` menus became text lists (hrefs LOST), wrapper-per-link menus became 85 stacked primary buttons, textless icon links became nameless "קישור" buttons. Now: link-runs + wrapper-blind `coalesceButtonRuns` → **nav**; ≥3 consecutive headline-length links → a **cards** wall (walla's headline stacks ARE article walls); a textless picture-link keeps its picture, never a nameless button. **URLs become absolute** against the fetch URL or (pasted/saved HTML) the page's own `<base>`/canonical/og:url. **The ingest pipeline** (`src/media-ingest.js` + `media.saveBuffer`): every remote image the blocks reference is downloaded (same SSRF guard as the decompiler, 8MB cap, 4-way polite concurrency), converted to **webp** via sharp (animated kept; SVG stays SVG, sanitized), saved through the media library's magic-byte gate into `/assets/imported/<slug>/`, and the blocks rewritten to local paths — wired into `POST /admin/api/pzn/decompile` (on by default, `assets:false` skips, report in the response). **The measured arc on walla's rendered DOM (1.5MB):** 1 button → 265 blocks → **180 blocks: 39 card walls with pictures, 5 navs, form, 15 embeds, 78 buttons; 105 images — 98 rewritten to `/assets/`, 29/30 sampled render from OUR server as webp**. Known truth documented: walla's *fetched* HTML carries ZERO content photos (XHR-hydrated) — the rendered DOM (extension scrape / saved page) is the real input for portals; server-rendered sites work straight from the URL. **QA:** 14 new smoke-decompile checks (the page-eater fix both directions, srcset/data-src sight, emotion bg → card picture, ul→nav with hrefs, wrapped-link coalesce, headline-stack→cards, icon-link picture survival, canonical base absolutization, imageRefs find+rewrite through nested trees); full suite green (26 smokes, 98 pzn tests). New dep: sharp. | Ben (direction: "the decompiled file should look similar to the page") + Claude |
| v0.66-alpha | **Decompiler V2 — the structure hunt + the safety guard (idea matured in the grok lab, rebuilt here).** Ben: "a set of them decompiling sites… a safety guard to always produce the highest quality"; "the page builder should swim with the idea of separating the page to few halves… the cut in percentage." Real pages are not text-photo-text — they are halves, heroes, card walls. **The hunt** (`src/pzn/hunt.js`): a second decompile strategy that reads the SHAPE — row/grid wrappers with 2–4 column children become a `columns` block with a **percentage cut** (bootstrap `col-md-8/4` → `67:33`, inline `width:70%` → `70:30`, else an equal split summing to 100); hero-classed sections that pass a shape test (slot-sized: 1–2 headings, ≤2 paragraphs, ≤3 links) collapse to ONE `hero` block — stripe's five-deep hero-in-hero markup can no longer eat a page; responsive twins (the same heading/nav rendered for desktop AND mobile) dedupe per container level; empty markup residue drops; `<picture>` → image; card clusters/forms/navs/videos map through the SAME shared parsers as v1 (extracted in `graduate.js`), so nothing regresses. **The safety guard** (`decompile.js`): a SET of strategies — flat v1 + hunt v2 — reads every page; a quality scorer (content coverage 50pts, structure bonus, leftover/duplicate/empty penalties) picks the winner, so a page the hunt reads badly still ships with the flat map. `toolGap` is the **union** across strategies (the vocabulary engine keeps every sighting). API returns `strategy` + per-strategy scores. **Lenient tokenizer** (`language/parse.js` `{lenient:true}`): yahoo-class attribute soup degrades to text instead of throwing; strict stays strict for `.pzn`. **The cut speaks percent end to end:** BenTML `ROW(ratio: "70%:30%")` normalizes to `70:30` (compile.js); the builder's drag handle now STORES integer percentages summing to 100 (`setColumnRatios`), so the drag, the % labels, and the `.pzn` all say the same numbers. **QA:** new `smoke-hunt` (24 checks: the percentage cut from classes/styles/equal-split, row-detection guards, hero collapse + the shape-test decline, twins dedupe, residue drop, cluster-inside-hunt, attribute-soup survival, the strategy race + forced-strategy + never-crash, `.pzn` round-trip of the cut, BenTML % normalize) in `test:smoke`; full suite green. **Browser E2E through the real admin:** decompiled a two-half page via `POST /admin/api/pzn/decompile` (hunt won 53.2 vs 45.2, `ratio="67:33"`), opened the draft in the builder — two panes with % labels + the resize handle, dragged the divider, labels moved 67/33→46/54, saved, and the draft `.pzn` on disk reads `ratio="46:54"`. Demo page kept: `/v2-halves-demo`. (Log gap: v0.61–v0.65 shipped — ticker, video, storage, categories, card clusters — rows pending backfill.) Next: mine tool-gap, AUDIO, BYOK media-write, carousel. | Ben (direction + lab prototype) + Claude |
| v0.60-alpha | **The nav module with color options (batch item 2/6).** Ben: "menu/nav… coloring option also for the nav and background." New `nav` container of `navitem` links (label + href) rendered as a **zero-JS flex bar** with a **background color + text color + alignment** (start/center/end). Full cross-layer recipe: pzn registry `nav`+`navitem`, shared **`src/pzn/nav-html.js`**, bridge (items ⇆ navitem children), renderer case, block-registry `nav` (color params + items list), keywords `NAV` (**graduated out of `RESERVED`**), `blocks.js`, `navitem` in the dictionary HIDDEN set, builder preview, CSS. Colors go through **`safeCssColor`** (the v0.44/v0.52 KEY LESSON — CSS context, not HTML: strip `;{}:"` breakout, keep hex/rgb/named); links through `safeHref`; text color via a `--bent-nav-color` CSS var. **Loop closed** (`graduate.js`): a `<nav>` now decompiles into a nav block (its `<a>` children → links), dropped from `suggestedTools`. **Bug caught by browser verification:** the nav emitted its OWN `style=""` AND the renderer's generic `extra` already carried one (from the block's `align`→`text-align`) — **two `style` attributes**, so the browser kept the first and silently dropped the nav colors. Fixed by passing the generic style *declarations* (via `styleDecls`) into the shared renderer to merge into **one** style attribute; regression-guarded in `smoke-nav`. **QA:** new `smoke-nav` (round-trip, compile/render parity, the CSS color-breakout guard, safeHref, align fallback, single-merged-style regression, decompile-loop) + updated `smoke-decompile` + `smoke-registry` green at 30 entries; full suite green (20 suites); **browser E2E** — published a colored nav, computed style confirmed `background: rgb(10,102,194)`, white links via the var, `justify-content:center`, zero console errors. Batch remaining (3–6): moving-news ticker, video, categories (make + presentation). | Ben (direction: the walla batch) + Claude |
| v0.59-alpha | **The card grid — walla's atomic unit, built (batch item 1/6).** The v0.58 walla learning session (decompiled its RENDERED DOM, 4520 elements) showed a content site is **a wall of media cards**: ~111 `<article>`, 119 `<figure>`+`<picture>`, 347 card-like clusters. Ben ordered the batch; this is #1. New `cards` container of repeatable `mediacard` items (image / tag-kicker / title / excerpt / href) rendered as a **zero-JS responsive auto-fill grid** (`repeat(auto-fill, minmax(15rem,1fr))` — 3 cols desktop → 1 on mobile, verified). Full cross-layer recipe: pzn registry `cards`+`mediacard`, shared **`src/pzn/card-html.js`** (one renderer for compile AND `renderer.js`, like form-html), bridge (items ⇆ mediacard children), renderer case, block-registry `cards` (items list param), keywords `CARDS`, `blocks.js`, `mediacard` in the dictionary HIDDEN set, builder canvas preview, CSS. **Security:** image + href through `safeHref` (no `javascript:`); a card with no href renders `<article>` (not a dead `<a>`); everything escaped. **QA:** new `smoke-cards` (round-trip, compile/render parity via the shared module, safeHref guard, no-href→`<article>`, title-escape) + `smoke-registry` green at 29 entries; full aggregate green (19 suites); browser E2E — published a "מבזקים" page, the live grid is `display:grid`, 3 responsive columns → 1 on mobile, first card a real `/n1` link, tags/titles correct, zero console errors. **Deferred:** the decompiler auto-recognizing card *clusters* (figure+h3+a repeated → cards) — a fuzzy cluster-detection pass, its own follow-up (unlike `<form>` which is one clear element). Batch remaining (2–6): menu/nav **with nav+background color options**, moving-news ticker, video, categories (make + presentation). | Ben (direction: the walla-learned batch) + Claude |
| v0.58-alpha | **The form module — the vocabulary engine turns its first full cycle.** v0.57's decompiler reported `form` as the #1 missing tool (`config/tool-gap.json` → `form:1`); v0.58 builds it and **closes the loop** so the next decompile maps forms instead of dumping them to `bent-html`. Ben: "make the modules so the designer will have all the things he needs." Full cross-layer add (the [[adding-a-module-recipe]] 8 files): pzn registry `form` container + `field` child, the shared **`src/pzn/form-html.js`** (one renderer for both the compile path AND `renderer.js` so they can't diverge), bridge (fields ⇆ field children, items pattern), `renderer.js` case, block-registry `form` entry (fields list param), `keywords.js` FORM (**graduated out of RESERVED**), `blocks.js`, the builder canvas preview, CSS (published + preview), and `field` added to the dictionary HIDDEN set. Fields: text/email/tel/textarea/select/checkbox with label/name/placeholder/required/options; zero-JS `<form>` posting to the owner's own `action` URL (their backend / Formspree / a future Tapuz leads endpoint). **Security:** action through `safeHref` (no `javascript:`), field `name` reduced to a safe `[\w-]` token (it's both an attribute and the submit key), everything escaped. **Loop closed** (`graduate.js`): a `<form>` now parses its label/input/textarea/select children into fields → a form block, skipping submit/hidden controls; `form` dropped from `suggestedTools`. **Bonus bug fix** surfaced while wiring: mid-walk raw markup could flush as a *text* block (tags as visible garbage) and nested leftovers hoisted to top level — now `flushRaw(raw, sink)` as provisional bent-html into the correct sink. **QA:** new `smoke-form` (round-trip, compile/render parity via the shared module, the `javascript:` guard, name sanitization, decompile-a-form-maps-clean) + updated `smoke-decompile` + `smoke-registry` green at 28 entries; full aggregate green (18 suites); **browser E2E** — pasted a real "צור קשר" page with a form into `/admin/new`, decompile returned **`mapped:3, leftover:0, toolGap:[]`** (pre-v0.58 this was `toolGap:['form']` + a provisional blob), the builder showed an editable form block (labels + required stars + submit, no provisional warning), and publish produced a live accessible `<form action="/lead" method="post">` with zero `javascript:` and zero console errors. Next: learning room (label decompiled patterns from walla/yahoo → new modules), then table/nav (the next gaps), BYOK media-write, carousel. | Ben (direction: "give the designer all the tools") + Claude |
| v0.57-alpha | **The HTML→BenTML decompiler — the vocabulary engine (idea harvested from the grok lab, rebuilt here).** Ben built a decompiler in grokTapuziel and said "we should work on this." The product thesis (his Red Hat line): **every real homepage is a map of missing tools** — decompile it and you get modules we already speak + provisional `bent-html` leftovers (nothing lost) + **`toolGap`**: the patterns we saw but have no module for. The toolGap is the vocabulary backlog; we build those modules, the next decompile maps cleaner, and every agent's dictionary gets richer. The decompiler is not an importer feature — it is how the palette fills. **Shipped:** `src/pzn/decompile.js` — whole-page decompile (extract `<main>`/`<body>`, title/dir/lang with a Hebrew-chars rtl heuristic, `deriveSlug`) → blocks → `fromTapuzPage` → serialize → validate; `decompileUrl()` fetches any live site — **rebuilt with the SSRF guard the lab version lacked** (http/https only, hostname denylist, DNS-resolve with every address checked against private/reserved IPv4+IPv6 ranges incl. cloud-metadata 169.254.169.254 and v4-mapped v6, manual redirects re-validated per hop, 10s timeout, 2MB body cap, content-type check). `graduate.js` upgraded into the engine: `suggestedTools` reporting (form/table/video/audio/nav + grid/flex→columns heuristic + whatsapp-link hint), iframe/youtube-link→`embed` (auto-embeds), **plus a real pre-existing bug fixed** — raw markup flushed mid-walk used to become a *text* block (tags as visible garbage) and nested leftovers were hoisted to the top level; both now flush as provisional `bent-html` into the correct sink. `POST /admin/api/pzn/decompile` {url|html, create} creates a no-clobber draft; toolGap counts aggregate into gitignored `config/tool-gap.json` (the per-install backlog). UI: **"🔁 יש לכם כבר דף? ייבאו אותו"** fold on `/admin/new` — URL or pasted HTML → draft → straight into the builder, with the mapped/leftover/toolGap report. **QA:** new `smoke-decompile` (28 checks: mappings, whole-page pipeline, rtl heuristics, script-only pages never hard-fail, the SSRF battery incl. localhost/metadata/file:/[::1], self-decompile of our own renderer output) in `test:smoke`; full aggregate green (17 suites); browser E2E — pasted a restaurant page with a form on `/admin/new`, it created the draft, opened the builder with 5 block cards (heading/text/image/button + provisional form), the `.pzn` on disk is valid with the form escaped-preserved, and `tool-gap.json` recorded `form:1`; zero console errors. Next: mine tool-gap.json → build the top-asked modules (form is already #1), BYOK media-write tier, carousel. | Ben (direction + lab idea) + Claude |
| v0.56-alpha | **Refine the ports + the free media win (no-key "media in reach of agents").** Ben's architecture call: the roleplay injection (BYOT) builds pages from text but can't *touch* media — *touching* (upload/generate) media needs BYOK (a key, third-party use), its own future tier. But **referencing existing media doesn't need a key** — so this version makes agents media-aware for free. **Media win:** `src/media.js` `listAllMedia()` (flat manifest of every uploaded image) + `GET /agent/v1/media`; the roleplay pack now injects an **"Available media — use ONLY these paths (do NOT invent)"** section (`agent-roleplay.js`, threaded through `/agent/v1/roleplay` + `/admin/api/{inject-pack,mission/*}`), so agents reference real `/assets/…` images instead of inventing `/uploads/x.jpg`. Pure-function `buildRoleplayPack({media})` stays DB-free; the server fetches + passes (capped at 40). **Injection hardening:** fixed the #1 silent failure — React-controlled `<textarea>`/`<input>` composers ignore `el.value=…` (onChange never fires), so `setComposerText` now goes through the native prototype value setter; plus a live **selector-health diagnostic** in the panel (`שדה ✓ · תשובה ✗ · שליחה ✓`) so per-provider tuning in a loaded Chrome is guided, not blind. **Consolidation:** the four AI on-ramps collapsed to one coherent front door — the copilot `/admin/chat` is now the single **AI ✨** nav item (was 4: ai/chat/inject/agent → 2: chat/agent), with the paste flow (`/admin/ai`), the dictionary/game (`/admin/inject`), and the extension bridge (`/admin/agent`) as sub-tools reached from the hub's "עוד דרכים לבנות" card + back-links. **QA:** `smoke-roleplay` extended (media section injects/omits, warns, mediaCount) + `smoke-extension` (native-setter present, selector diagnostics); full aggregate green; browser-verified — media manifest threads into the pack end to end (real `/assets/` path), nav shows the single AI hub highlighting across all three pages, `/admin/ai` intact, zero console errors. Next: the BYOK media-write tier (upload/generate under roleplay rules), then carousel. | Ben (direction: BYOK for media / refine first) + Claude |
| v0.55-alpha | **The roleplay injection + copilot mission bridge — "make any chat roleplay BenTML at a press of a button" (BYOT).** Ben: "the injection which will be in the extension… we're going to make any chat roleplay BenTML using the dictionary in a press of a button, then the user tells the primed agent the game's rules." Ported the roleplay/"game" layer from the `grokTapuziel` language lab and grounded it on Tapuz's live registry. **The pack** (`src/pzn/agent-roleplay.js` + `agent-mission.js` + `syntax-dictionary.js`): one document that primes any LLM to be the **Site Builder agent** — role + the `bent-*` **tool inventory** (built live from `getCommandCatalog`, so it can't drift) + a worked example + the **completion contract** (reply with ONE fenced `html` doc ending in `</html>`, optional `PZN_READY`) + the full dictionary. Two on-ramps: **(1) the extension** — the floating BYOT panel now **injects** the pack straight into the chat composer ("① הזרק משחק + מילון"), watches the streamed reply, and **auto-publishes the moment a COMPLETE `.pzn` lands** (closed fence + `</html>` + ≥1 module — half-streams are refused); **(2) `/admin/chat`** — a copilot where you describe a page → it mints a **mission** (`src/mission-store.js`, gitignored `config/missions.json`) → the extension pulls it (bearer token) and injects teach→build→auto-publish. New endpoints: `GET /agent/v1/roleplay|dictionary`, `GET/POST /agent/v1/mission[/:id/step]`, plus `/admin/inject` (copy the game/card/dictionary) and the `/admin/api/mission/*` set. The **token stays only in the extension's background worker** (never in the content script/page) — the standing security invariant, re-asserted by the smoke. This is the "hand the dictionary once, then chat to build" flow Ben asked for since v0.51, now one-click. **QA:** new `smoke-roleplay` (20 checks — pack has role+contract+live tools, tabs/accordion in the inventory, child leaves hidden, per-provider coaching) + `smoke-mission` (the create→pull→advance lifecycle, isolated to a throwaway `TAPUZ_ROOT`) + upgraded `smoke-extension` (composer/send/streaming selectors, the `analyzeReply` completion gate, the new background handlers, token-only-in-background re-verified); full aggregate green; browser-verified end to end — `/admin/inject` renders the live 30-tool inventory + previews the pack, and `/admin/chat` turned a page description into a ready mission with the inject actions. The DOM inject/scrape selectors can't run headless (same caveat as the scrape bridge) — tuned per-provider in a loaded Chrome. Next: carousel/slider, then motion/scroll effects. | Ben (direction) + Claude |
| v0.54-alpha | **Interactive containers — the palette starts filling out (tabs + accordion).** Ben: "we should not be limited to byok… give people the power to inject BenTML into their agent and imagine their site — we will have the effects and containers ready to be built." First batch of the effects/containers library: **tabs** (pure-CSS via the radio hack — no JS) and **accordion** (native `<details>`). Both wired the FULL way — the BenTML language (`<bent-tabs>`/`<bent-tab>`, `<bent-accordion>`/`<bent-fold>` in the pzn registry), the bridge (block⇄BenTML, items ⇆ children like faq), `renderer.js` (published-site HTML), the Tapuz block-registry + `keywords.js` (TABS/ACCORDION graduated out of RESERVED) + `blocks.js`, the builder (toolbox modules + canvas previews + the list-item editor), CSS, and — crucially — the **auto-generated BenTML dictionary**, so any agent (any model, no key) now knows tabs+accordion and can imagine sites with them. Zero-JS by design (radio hack + `<details>`), so they stay fast and portable. **QA:** new `smoke-containers` (11 checks incl. the pure-CSS radio→label→panel adjacency the tabs depend on) + `smoke-registry` still green at 27 modules; full aggregate green; browser-verified on a LIVE published page — clicking the 2nd tab switched panels with no JS, the accordion toggled, the builder shows previews + both in the toolbox, zero console errors. Next batch: carousel/slider (`SLIDER` still reserved), timeline, steps. | Ben (direction) + Claude |
| v0.53-alpha | **Import BenTML from any AI — the in-builder bridge (BYOT).** Ben: "page builder properties with the ability to import BenTML from any agent… we bridge between llms to the system with BYOT style… hand the dictionary to the llms, one time, then chat for editing." New **🤖 ייבא מ‑AI** button in the builder opens a modal: **(1)** one-click "📋 העתק מילון BenTML ל‑AI" (the primer — the one-time handoff), **(2)** a paste box for the AI's reply (prose/fences fine — extracted), **(3)** replace-page or append. New non-saving endpoint `POST /admin/api/pzn/to-blocks` extracts → parses → (on failure) **repairs** → `toTapuzPage` → returns blocks the builder applies in place; imperfect agent output still lands (repaired), and the fixes are surfaced. BYOT: the AI runs on the customer's own subscription — Tapuziel only carries the reply, no keys. This brings the paste-flow's power inside the builder without leaving it (the standalone `/admin/ai` page and the Chrome extension remain the other two on-ramps). **QA:** new `smoke-import` (8 checks: clean import, prose-wrapped extraction, imperfect-→-repaired-into-blocks, graceful junk failure); full aggregate green; browser-verified end to end — pasted a prose-wrapped reply containing `<bent-paragraph>`, it extracted + repaired (paragraph→text) + rebuilt the page as hero+text+heading, modal closed, zero console errors. | Ben (direction) + Claude |
| v0.52-alpha | **Splash background on scroll — the page-level parallax photo.** Ben: "I want splash photo when scrolling… background in the page properties and moving photos like onepage does." New **רקע הדף · splash** section in page properties: a full-page background image (+ media picker), a dark-overlay slider, a **parallax toggle** ("תמונה נעה בגלילה כמו onepage"), and a fallback background colour. The renderer (`pageBackgroundStyle` in `renderer.js`) injects a page-scoped `<style>` + body class: when parallax is on it's a **fixed full-viewport layer the content scrolls over** (a fixed `::before` layer, smoother on mobile than `background-attachment:fixed`, with a touch fallback that pins it); off = a scrolling `absolute` layer. The image URL is CSS-hex-escaped (no `url()` breakout) and the colour is stripped of CSS-breakout chars. The builder shows a **live preview** of the splash on the canvas. **Bug caught by end-to-end verification:** static export (`export.js`) strips ALL inline `<style>` tags (theme CSS is externalized to `/css/main.css`) — which silently ate the page-bg style, shipping a body class with no background. Fixed by extracting `externalizeStyles()` that PRESERVES the `tapuz-page-bg` style across the strip. **QA:** new `smoke-page-bg` (15 checks incl. the CSS-escape security cases AND the export-preservation regression); full aggregate green; browser-verified end to end — set a parallax splash in the builder, published, and the live page renders a `position:fixed` background layer with the overlay. Stored in page `meta` (persists via the DB; `.pzn`-head portability is a follow-up). | Ben (direction) + Claude |
| v0.51-alpha | **Slug auto-rename + graduation UI + a calmer builder for customers.** Four asks from Ben in one pass. **Slug auto-follows the page name:** editing the title rewrites the address (spaces → dashes, Hebrew kept) while it's still title-derived; a hand-edited slug detaches (clear it to re-enable auto). The address is now a field in page properties and renaming a live page moves its `.pzn` files + updates the URL bar in place — and a taken address never blocks the content save (it just keeps the old address and flags it). **Graduation UI:** provisional `bent-html` blocks now render as a clear "raw HTML — provisional" card (content shown ESCAPED, never injected into the admin DOM) with a **"✨ convert to modules"** action; a new best-effort converter (`src/pzn/graduate.js`, `/admin/api/pzn/graduate`) maps h1-6→heading, p→text, img→image, ul/ol→list, a→button, blockquote→quote, hr→divider, descends through wrapper divs, and keeps anything unmappable as a smaller `bent-html` block so nothing is lost — the graduated blocks always form valid BenTML. **The code view is now ADVANCED:** the live BenTML dock under the canvas and the code tab are hidden behind a remembered "⚙ מתקדם" toggle, so a customer's default is a clean visual builder + chat (the raw code "looked odd" up front). **Terminology:** the language is **BenTML** (fixed in the primer, which now names it for the LLM); customer-facing copy is encouraging ("תיהנו מהזרימה"), "foolproof" stays internal-only. **QA:** new `smoke-graduate` (12 checks incl. graduated-blocks-are-valid-BenTML) added to the suite; full aggregate green; browser-verified end to end — auto-slug rename moves the page + URL, the advanced toggle hides/shows the dock, and clicking convert turned a provisional HTML block into heading+text+list+button in place, zero console errors. | Ben (direction) + Claude |
| v0.50-alpha | **The foolproof live builder.** Ben: "we must have auto slug for people who ain't familiar with sites… editing in the live build should be allowed, if I edit text on the live builder I should have the option to edit it… the side properties is the advanced part, we give full control but we are doing the live site fool proof." **Auto-slug:** `/admin/new` no longer forces a manual slug — it auto-derives one from the title as you type (Hebrew kept, editable, stops the moment you touch it), and the server fills+dedupes if left blank. A newcomer never has to know what a slug is. **Single-click inline editing:** editing on the canvas was hidden behind *double*-click and covered ~6 block types; now a SINGLE click on any text edits it in place, with a persistent faint-underline affordance so it's discoverable (no one has to guess). The commit path was generalized to `block.data[key]` — one code path for hero/heading/text/quote/testimonial AND the generic registry text fields, so **marquee and banner became editable on the canvas too**. Edit-start no longer re-renders the whole canvas when the block is already selected (no flicker, no lost caret), and only marks dirty when the text actually changed. **The side panel stays the advanced surface:** clicking text to edit ALSO selects the block, so its full control panel is right there — foolproof on the canvas, full power in the drawer. **Saves verified:** save-draft, publish, and publish+build all confirmed working end-to-end (edit inline → save → the change lands in both the draft and published `.pzn`). **QA:** full suite green; browser-verified end to end — auto-slug fills from a Hebrew title and stops on manual edit; single click makes hero/banner text editable and commits to the model; the side panel populates (10 fields) while editing; save+publish persist the inline edits; zero console errors. | Ben (direction) + Claude |
| v0.49-alpha | **The forgiving pipeline + the escape hatch — "saving works now".** Ben's report: saving edited/AI-authored BenTML failed constantly, the strict validator blocked designs he wanted, models didn't understand the syntax, and the extension "asked for a key" instead of giving tools. Root cause found: one bad tag rejected the WHOLE document, and there was no way to express an off-vocabulary design. **The escape hatch — `bent-html`:** a new registered module whose payload is arbitrary HTML, sanitized at compile/serve time (Ben's call: "both, with a graduation path" — it renders, is marked `provisional`, and the builder flags it to convert into real modules). The standard stays a registry; this is its pressure valve (like MDX→JSX). Wired across every layer (registry compile, bridge both ways, `renderer.js` published path) and round-trip-lossless (raw HTML lives HTML-escaped in a `content` attribute — no parser surgery). **The sanitizer is the guarantee, not the CSP** (`src/html-sanitize.js`): the published `script-src` allows `'unsafe-inline'`, so an entity-aware denylist scrubber strips script/handler/scheme vectors — 15-case adversarial battery incl. nested `<scr<script>ipt>`, `java&#115;cript:`, tab-obfuscated schemes, SVG `onload`, `data:text/html`, `@import`, `expression()`. **The repair engine (`src/pzn/repair.js`) — Ben's call: "auto-correct, then you apply":** a strict-save failure becomes a corrected document + a human-readable change list. It ALIASES model drift to real modules server-side (`bent-paragraph`→text, `bent-img`→image, `bent-h1`→heading level 1, ~50 mappings — "the server owns the vocabulary"), QUARANTINES the truly-unknown into provisional `bent-html`, WRAPS raw HTML in the body, fills/snaps/clamps bad props, dedupes ids, and hoists illegal nesting — every repaired doc parses AND validates clean. **Admin UX:** the paste flow now offers a one-click "🔧 תקן והחלף" when a reply is broken (dry-run `/admin/api/pzn/repair`, no save); the fix flows back into the existing preview→apply pipeline. **Extension:** the popup now LEADS with "תן ל‑AI את ערכת הכלים" (equip your AI) and folds the token into a one-time setup; the button falls back to the forgiving path so imperfect replies land as a reviewable DRAFT (never auto-published) with the change count shown. **Primer** now tells the model it needn't be perfect (the server repairs) and can fall back to plain HTML. **QA:** new `smoke-pzn-html` (20 checks) + `smoke-pzn-repair` (every failure mode → clean output) added to the suite; full aggregate green; end-to-end browser-verified — pasted a broken doc (raw div + alias + level=99), saw the 3-fix offer, clicked apply, and confirmed the stored draft is valid (`bent-html` + `bent-text` + `level="6"`, no raw div). | Ben (direction: both forks) + Claude |
| v0.48-alpha | **The unification — one core, no branches (it's an OS now).** The language core (`src/pzn`) and the whole security phase stop being feature branches and become THE trunk: every feature branch (v0.39-pzn-language-core, v0.45/46/47 work, feat/security-chrome-analytics) is now merged into `main` and deleted, local **and** remote — the repo is a single trunk going forward (`origin/main` only). **Dev ergonomics — the "I couldn't stand up an environment to test" fix:** new `scripts/seed-dev-admin.js` + `npm run seed:admin` mints an instant `admin`/`admin` login so you skip the first-run wizard for local testing. It is explicitly **not a backdoor** — writes only to gitignored `config/auth.json`, the app ships zero default account, refuses to run under `NODE_ENV=production`, and self-verifies the seeded hash before exiting. Extension first-run friction removed: `localhost`/`127.0.0.1` granted in the manifest, popup requests host permission for non-local CMS origins, ping failure now hints "is the server running?", README is a 5-step runbook (no Apache — Tapuziel *is* the server). **Fix:** `/agent/v1/ping` reported a hardcoded `version: '0.45'` → now reads `package.json`, so the extension popup shows the true version. Published spec regenerated (`docs/pzn-spec.md` + `pzn-schema.json` → `tapuziel@0.48.0-alpha`). **QA:** full smoke suite green (exit 0); `admin`/`admin` login verified end-to-end against a live server — HTTP 302 + signed session cookie, and a real browser login into the full admin dashboard. | Ben (direction) + Claude |
| v0.47-alpha | **Security phase + the `.pzn` standard published + extension load fix** (Phase 3 begins; Phase 4 groundwork). **Extension fix:** the Chrome extension wouldn't load — manifest referenced a phantom `icon.png`. Generated real 16/48/128 tangerine PNGs (scripts/gen-extension-icons.js), wired into manifest icons + action.default_icon; smoke-extension now asserts every manifest-referenced file exists (the gap that let it through). **Security — upload validation:** src/upload-validate.js sniffs MAGIC BYTES (never the client mime/filename), image allowlist only, 5MB cap — closes the `data:text/html` → stored-XSS-hosting and type-spoof vectors. **Security — the `.pzn` standard:** docs/pzn-spec.md (implementable standard) + docs/pzn-schema.json (machine catalog, generated from the live registry via src/pzn/spec.js) + public `GET /pzn-schema.json` — the Red Hat "publish your RPM" move; anyone can implement a reader/writer. **Security — CSP + hardening:** CSP on published pages, 512kb agent body cap, gitleaks CI (.github/workflows/security.yml + .gitleaks.toml). **A 19-agent adversarial review found 6 real holes in the security work itself — all fixed** (the review's most important catch yet): (1) regex SVG sanitizer bypassable (nested `<scr<script>ipt>`, `/onload`, SMIL, encoded schemes) → the load-bearing fix is the SERVE LAYER (uploaded SVGs now served with `sandbox` CSP + `Content-Disposition: attachment` + nosniff via express.static setHeaders, so a bypass can't execute), plus a ReDoS-safe best-effort scrubber; (2) that sandbox header was DEAD CODE (shadowed by the static mount) — fixed via setHeaders so it applies at serve time; (3) CSP exclusion prefix gap (`/admin*` matched public pages) → boundary-aware; (4) unauth stack-trace disclosure on the body-cap → terminal error handler returns generic JSON; (5) ReDoS in the sanitizer; (6) SVG external-ref. Browser + live-HTTP verified; regressions locked in smoke-uploads + smoke-agent-bridge; full suite green. **Deferred (still before beta):** DB encryption at rest (dependency + key-management change — its own version) | Ben (direction) + Claude + adversarial review fleet |
| v0.46-alpha | **The Grokin bridge CLIENT — the Chrome extension, zero keys, Claude included** (Phase 2 piece 3 complete, the ace is end-to-end). New `extension/` (MV3): `background.js` (holds the token, message router, CMS fetch), `content-bridge.js` (floating "🍊 → תפוזיאל" button that scrapes the latest assistant reply on the LLM site), `providers.js` (per-provider scrape selectors — **Claude first-class**, plus ChatGPT/Grok/Gemini), `extract.js` (pull .pzn from the reply), `popup.html/js` (set CMS URL + token, ping, copy primer, pick target page), README. Flow: paste primer into your own Claude/ChatGPT → ask for a page → bot replies .pzn → click the button → live page. Server: new write-scoped `/agent/v1/create-from-source` (the extension's main path; hardened deriveSlug, 409 on collision). **Security model (first-party):** the bearer token lives ONLY in the background worker (chrome.storage, read only there), used ONLY in `Authorization: Bearer` to the configured CMS — NEVER sent to a content script or web page; no `externally_connectable` so LLM pages can't message the worker; getConfig returns `hasToken` boolean only. **QA:** scripts/smoke-extension.js (26 checks: extraction, provider table incl claude.ai, MV3 manifest validity, and the token-exposure invariants — content script touches no storage/auth/fetch, popup write-only, no raw token in any response); smoke-agent-bridge extended with create-from-source (build/live/409/scope); manual token-leak scan confirmed both raw-token uses are Authorization headers. Full suite green. Note: DOM-scrape selectors + the real-LLM path need the extension loaded in Chrome (can't run headless) — Ben tunes selectors per provider as sites redesign | Ben (vision) + Claude |
| v0.45-alpha | **The Grokin bridge server contract — BYO-agent API, zero keys** (Phase 2 piece 3, North Star pillar 6 groundwork). New `/agent/v1/*` bearer-token API so an external agent (the coming Chrome extension) drives the CMS cross-origin: ping/primer/toolbox/pages/source(GET+POST)/ops/build. `src/agent-tokens.js` — scoped (read/write) bearer tokens, own gitignored config/agent-tokens.json, scrypt-hashed (reuses auth.js), prefix-lookup + full-token verify, shown once at mint. CORS `Allow-Origin:*` WITHOUT credentials (safe: bearer never auto-sent → no CSRF → no cookie riding); deliberately outside the /admin same-origin guard. `src/pzn/intent.js` — the Grokin trick: model emits a forgiving intent `{sections[]}`, SERVER owns the .pzn (14 section types incl hero/marquee/parallax/features/cta, validated). Admin `/admin/agent` page + token CRUD (session-authed). **QA: 24-check HTTP smoke (reference client) + a 26-agent adversarial security review (5 lenses) that found 7 real holes behind the green smoke — ALL FIXED**: (1) path-traversal arbitrary .pzn write via backslash/`..` in a bot-authored slug → hardened safeName + sanitizeFullPath + deriveSlug + resolved-path guard, across pzn-store/pages/export; (2) rate-limit lockout keyed on attacker-supplied token prefix → re-keyed to source IP; (3) /build silently clobbering an existing page → 409 unless update:true; (4) unbounded intent nesting DoS → depth(6)+count(250) caps; (5) `javascript:`/`data:`/`vbscript:` in href/url → new safeHref/safeUrl neutralize on BOTH the intent AND the render+compile chokepoints (button/embed/cta/hero/@LINK/logo — closes it for the paste flow too). Browser-verified token UI (mint→shown once→authenticates a real ping). Full suite green | Ben (vision) + Claude + adversarial review fleet |
| v0.44-alpha | **Signature visuals — the patterns the vision was named for** (Phase 2 piece 2). Full-stack: HERO `overlay` (0–80 darkening) + `parallax` (fixed bg) now STORED end-to-end (keywords→compile→JSON→renderer→decompile→pzn→bridge) — **the "parsed but never stored" drift from src/block-registry.js:23 that started the whole merge is closed; smoke-registry KNOWN_UNCOVERED is now empty and any future gap fails the smoke**. New block types: `marquee` (moving text, robust two-full-width-track loop = seamless at any length) and `parallax` (section with fixed bg + overlay + content scrolling over). New `animate` prop (fade/rise, scroll-driven via animation-timeline where supported) on heading/text. Theme + preview CSS with reduced-motion + touch-device (hover:none/pointer:coarse) fallbacks. Registry 23→25 types, pzn modules 30→32. **QA: 22-agent adversarial review workflow (4 lenses × verify) found 15 real bugs the green suite missed — all fixed**: CSS `url('…')` injection in hero+parallax (HTML-entity escaping is wrong for CSS-string context → new `cssUrl`/`escapeCssUrl` CSS-hex escaper), marquee/parallax className emitted as bare attribute, hero overlay-without-image emitting `url('')`, parallax dropping inline styles, hero parallax truthy-vs-strict mismatch, animate enum unvalidated, PARALLAX image required-vs-optional round-trip E306, marquee short-text pop, overlay ignoring border-radius, fixed-bg missing touch fallback. Browser-verified live (2 tracks, 100% min-width, RTL anim, 16px inherited overlay radius, injection neutralized). Round-trip tests 25 types, full suite green. Follow-ups chipped: builder-canvas parallax editing, legacy keyword-lang nested-id/whitespace quirks | Ben (vision) + Claude + adversarial review fleet |
| v0.43-alpha | **The paste flow — BYO AI, zero keys** (Phase 2 piece 1, North Star pillar 6 goes live). New `/admin/ai` screen: copy the primer → paste the bot's reply → live preview → apply. `src/pzn/agent-primer.js` generates the paste-into-any-AI primer from the live registry (all 30 modules, reply contract, worked example — can never drift). `src/pzn-extract.js` pulls the .pzn document out of prose/code fences (```/~~~/bare). API: GET `/admin/api/pzn/primer`, POST `/admin/api/pzn/preview` (compile without saving), `loose` flag on POST source, POST `/admin/api/pzn/create-from-source` (bot invents a page: slug from bent-slug, 409 on collision). Publish from the paste flow runs exportAll — "צפה בדף החי" is live immediately. **QA**: smoke-pzn-paste.js (15 checks) in test:smoke; full REAL-BROWSER E2E on a throwaway root — login, paste a bot reply with prose around the fence, live preview rendered, new page created+published, live page served with hero/features/stats/cta. Browser QA caught two real bugs pre-ship: pages API shape mismatch in the picker, and publish-without-export 404ing the live link | Ben (vision) + Claude |
| v0.42-alpha | **The canonical editing path — .pzn source + AST ops** (Phase 1 step 4, Phase 1 complete). pages.js gains `getPageSource` (canonical .pzn, legacy fallback), `savePageSource` (validate → save → author formatting preserved byte-for-byte → title/tags/teaser/cardImage/direction sync to DB index → optional publish), `applyPageOps` (builder-standard ops: insert/update/move/replace/duplicate/remove/document via src/pzn/builder/ops, serialized back through savePageSource). New admin API (behind auth like all admin routes): GET+POST `/admin/api/pzn/source`, POST `/admin/api/pzn/ops`, GET `/admin/api/pzn/toolbox` (module schemas for agents). The visual canvas's JSON is now formally a wire projection of the file (v0.40 bridge over v0.41 store); agents/tools edit the canonical form directly. **QA**: scripts/smoke-pzn-ops.js (22 checks: formatting preservation, invalid source rejected without touching the file, every op kind, revision trail) joined test:smoke; live boot verified new routes answer 401 unauth exactly like existing admin API; full suite green (88 tests + all smokes + wizard + 1138 registry checks). **Phase 1 (unification) done — one language, one file, one model.** Next: Phase 2 the moat — signature visuals (parallax/animated text), unified agent pack, Grokin-V2 BYO-subscription translator, copilot chat | Ben (direction) + Claude |
| v0.41-alpha | **The storage flip — the .pzn file IS the page** (Phase 1 step 3, the one-data-model moment). New src/pzn-store.js: every save writes `pages/drafts/<path>.pzn`, every publish writes `pages/published/<path>.pzn` (SITE_ROOT-relative, TAPUZ_ROOT-aware). parsePageRow overlays blocks from the file when present — every reader (admin, renderer, export, article cubes) is file-first automatically; DB JSON stays as index + legacy fallback. Revisions now store canonical .pzn text (legacy JSON revisions still readable); restore flows through the file. Rename moves files, delete removes them. scripts/migrate-pzn-store.js backfills files idempotently (existing files win — they are the truth). **QA**: scripts/smoke-pzn-store.js on a throwaway root proves the flip — 18 checks incl. "edit the file on disk → CMS serves the edit while the DB still holds the old value"; full suite green (88 pzn tests, all smokes incl. round-trip, wizard E2E, 1138 registry checks); joined test:smoke. Scope note: content is file-canonical; status/theme/extended meta (redirect) stay DB-owned until the .pzn head covers them. Next: v0.42 = builder edits the AST via ops | Ben (direction) + Claude |
| v0.40-alpha | **Registry unification — every Tapuz block speaks .pzn, losslessly** (Phase 1 step 2). The pzn module registry grew from 16 to 30 modules: all 10 missing Tapuz types added (testimonial, features+feature, card, map, cta, stats+stat, logos+logo, faq+qa, contact-info, banner) with compile HTML matching src/renderer.js contracts (theme CSS keeps working), plus prop parity on existing modules (heading/text/button align, text size/lead/dropcap/maxwidth, image width, hero image+height, gallery columns, columns ratio/collapse/valign + Tapuz gap vocab, spacer size, divider bentstyle). Bridge is now two-way: `toTapuzPage`/`moduleToBlock` join `fromTapuzPage`, rule = only carry what exists (no invented defaults/ids). **QA in the way**: gate 1 = 27 synthetic round-trip tests, all 23 types + edge cases (test/pzn/tapuz-roundtrip.test.js); gate 2 = scripts/smoke-pzn-roundtrip.js proves JSON→.pzn→JSON lossless on the real DB read-only AND on a full wizard-generated site (5 pages, 24 blocks); gate 3 = 88 pzn tests + full smoke suite + 1138 registry checks green. smoke-pzn-roundtrip joined test:smoke. Next: v0.41 flips canonical storage to the page file | Ben (direction) + Claude |
| v0.39-alpha | **The merge begins — Grok language core lands in Tapuz** (Phase 1 step 1 of the unification plan). `C:\Dev\grokTapuziel`'s `.pzn`/benTML implementation ported wholesale into `src/pzn/` (language: tokenizer/AST/parse/compile/validate/serialize; modules registry with i18n labels + prop schemas; immutable builder ops; site build/publish; tapuz-json bridge; loader). All 61 node:test tests ported to `test/pzn/` and green inside Tapuz (`npm run test:pzn`); example fixtures at `examples/pzn/` + demo site at `examples/pzn-site/`. Full existing smoke suite still green (registry 1138 checks, wizard, render, drafts, articles, bentml, map, analytics). Nothing wired into the CMS yet by design — v0.40 unifies the 23-block registry with the module defs and round-trips real pages via the bridge; v0.41 flips canonical storage to the page file; v0.42 points the visual builder at the AST ops. Grounding decision: `.pzn` (constrained-HTML benTML) is the canonical standard — one data model, Red Hat play, `.pzn` is our RPM | Ben (vision) + Grok (implementation) + Claude (port & verification) |
| v0.38-alpha | BenTML v0.1 spec (`docs/bentml-v0.md`, multi-agent designed + blind-verified), registry-driven builder, admin auth, site chrome, analytics | Ben + Claude |
| v0.37-alpha | **Wizard v2**: 4 guided steps (name → coloring that *teaches the theme creator* with palettes + live mini-preview → default pages home/about/contact/articles with article-list + sample article → menu step with page picks + external links). Setup engine extracted to src/setup.js (runSetup — agents/CLI can bootstrap a site in one call). **TAPUZ_ROOT** env var via new src/paths.js — all data (db/config/public) can live in any directory (npm-publish blocker step 1). Fix: home exported only as index.html so menu links to /home.html 404'd — now writes both. Docs ecosystem aligned to version (README, next-steps, mvp-scope, implementation-plan, cli-agent-support, package.json 0.37.0-alpha). smoke-wizard E2E (32 checks, runs on throwaway root) | Ben (spec) + Claude |
| v0.36-alpha | **article-list module** (cubes: image + title + teaser, tag-driven, 1-4 columns, newest first), page properties in side panel (article toggle, manual teaser + card image with auto-extraction fallback), live article preview in builder canvas via new /admin/api/articles, canvas background click = deselect → page properties, tags/meta flow through save/publish, article-cubes theme CSS (responsive grid), smoke-articles E2E (15 checks), block-schemas.md documents article-list for agents | Ben (spec) + Claude |
| v0.32-alpha | Draft/Publish split + automatic revision backups, page navigator modal (search/jump), theme overrides system (colors, fonts, logo, menu placement) with live preview + API, menu entity in DB with editor, publish flow in builder, revisions modal with restore | Ben + Grok |
| v0.35-alpha | Renamed to **Tapuziel** (Tapuz + Shaltiel's ־יאל — free on npm), npm packaging (bin: tapuziel + tapuz alias, engines, repository, npm pack verified 81 files), full E2E: wizard → published site → static serve all verified. TODO before real npm publish: cwd-based paths refactor (db/config/public should live in the user's project dir, not inside node_modules) | Ben + Claude |
| v0.34-alpha | Admin section identity (subtle accent color per area: pages blue, menus purple, theme green, sitemap orange), first-run setup wizard for newcomers (3 questions → site + home page + menu + build), security phase spec'd in roadmap (deferred by design) | Ben + Claude |
| v0.33-alpha | Typed menu links (existing page picker, custom URL, tel: click-to-call, mailto:, anchor), /admin/sitemap tree derived from menus with published/draft/missing badges + orphan-page detection, redirect pages via meta.redirect, menu editor rewrite with live URL preview | Ben + Claude |
| v0.32-alpha | Draft/publish separation (save keeps draft, publish snapshots), auto revisions (30/page) with restore, pages navigator with search + unpublished badges, menu entity in DB with nesting, theme overrides (colors/fonts/layout/menu top-side) + theme & menus admin pages. Fixes: db.js circular-dependency (schema silently failing), revision restore loaded published instead of draft, removed leftover patch script | Grok + Ben, review & fixes Claude |
| v0.31-alpha | DB-wired media library (folders, Explorer UI, right-click delete/copy, create folder), gallery module with multi-select picker, toolbox buttons for list/video/gallery | Ben + Claude |
| v0.30-alpha | Undo/redo, autosave + unsaved guard, list & YouTube modules, Hebrew toasts, XSS fix, semantic HTML5 output, skip-link + aria, theme comfort pass | Claude + Ben |
| v0.2 | Smart module replace, clearer RTL UX | Ben |
| v0.1 | Initial CMS: Elementor import, RTL visual builder, static export | Ben |

## Idea Pool (unscheduled)

- Column width ratios with visual handles
- Keyboard: Ctrl+D duplicate, Alt+arrows move block
- Block templates: save a group as reusable "section"
- Inline text editing on canvas
- SEO panel per page (meta, og:image, slug)
- Auto sitemap.xml + RSS on build
- Scheduled publish
- Dark mode (prefers-color-scheme)
- Second theme (cyberpunk grokskin-style)
- Elementor import: map more widget types
- Markdown import (folder of .md → pages)
- Media: alt-text editing, image resize on upload, drag file into folder

### Ben's ideas
- **Palette from image**: in the theme maker, drop in images that *feel like the color you want* — extract a palette (+ gradients) from them
- **Gradient support** in theme coloring, not just flat colors
- **`.pzn` format**: single-file import/export container (site / theme / plugin), shares the import pipeline with WordPress/Elementor exports
- **Theme import type**: anyone can package a theme as an import and share it; selling allowed, no DRM, no piracy liability on us
- **Plugin install**: install plugins from a file (`.pzn`) — third-party module API is the foundation (see Community section)
- **Import skips wizard steps**: importing a site auto-answers wizard questions it can (homepage, pages, menu) — wizard only asks what's still missing
- **Generic homepage scheme**: from-scratch sites get a homepage pre-cut into page-builder sections (hero / content / CTA…) so there's never a blank page
- **Agent mini-API**: endpoint/spec that lets agents identify page-builder blocks and edit them safely — make Tapuziel the most AI-friendly CMS
- **Copilot chatbot in admin**: ChatGPT-style chat that operates the builder for real (halve page, make hero, color text, place the co-written article) — human skin over the agent mini-API
- **Select-text AI actions**: correct / expand / shrink / rewrite on selected text — secondary entry point alongside the chatbot
- **Side options panel**: module options appear in a side panel when a placed module is selected; schema-driven so every new module gets its panel for free
- **BYO AI subscription**: user's own AI subscription powers the in-CMS assistant (no token middleman); port the Grokin-V2 bot + Chrome extension functions (`C:\Dev\Grokin-V2`), universal across providers
- **Syntax primer + translator**: user pastes a primer into their own bot to teach it Tapuziel/`.pzn` syntax; the bot replies in our syntax and a translator in Tapuziel parses + executes via the mini-API — BYO subscription with zero API keys (the Grokin trick)

### Grok's ideas
- (add here)
